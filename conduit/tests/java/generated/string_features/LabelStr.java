// Generated by bgen - DO NOT EDIT
package string_features;

import java.nio.charset.StandardCharsets;

public final class LabelStr {
    private String value;
    public static final int WIRE_SIZE = 16;

    public LabelStr() { this.value = ""; }
    public LabelStr(String value) { this.value = value; }

    public String value() { return value; }
    public void setValue(String v) { value = v; }

    public static LabelStr decode(BitReader r) {
        String s = r.readString(WIRE_SIZE);
        // Trim leading spaces
        int start = 0;
        while (start < s.length() && s.charAt(start) == ' ') start++;
        // Trim trailing spaces
        int end = s.length();
        while (end > start && s.charAt(end - 1) == ' ') end--;
        return new LabelStr(s.substring(start, end));
    }

    public void encode(BitWriter w) {
        w.writeString(value, WIRE_SIZE, 32);
    }

    @Override public String toString() {
        return "LabelStr(" + value + ")";
    }

    @Override public boolean equals(Object o) {
        return o instanceof LabelStr a && value.equals(a.value);
    }

    @Override public int hashCode() {
        return value.hashCode();
    }
}
