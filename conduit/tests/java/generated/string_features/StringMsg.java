// Generated by bgen - DO NOT EDIT
package string_features;

import string_features.codec.*;
import string_features.BoundedStr;
import string_features.LabelStr;
import string_features.NameStr;
import string_features.PackedStr;
import string_features.TermStr;
import java.util.*;

public final class StringMsg {

    public int id = 0;
    public NameStr name = null;
    public LabelStr label = null;
    public BoundedStr bounded = null;
    public PackedStr packed = null;
    public TermStr term = null;

    public static StringMsg decode(BitReader r) {
        StringMsg result = new StringMsg();
        result.id = r.readU16(true);
        result.name = NameStr.decode(r);
        result.label = LabelStr.decode(r);
        result.bounded = BoundedStr.decode(r);
        result.packed = PackedStr.decode(r);
        result.term = TermStr.decode(r);
        return result;
    }

    public static StringMsg decodeBytes(byte[] data) { return decode(new BitReader(data)); }

    public void encode(BitWriter w) {
        w.writeU16(this.id, true);
        this.name.encode(w);
        this.label.encode(w);
        this.bounded.encode(w);
        this.packed.encode(w);
        this.term.encode(w);
    }

    public byte[] encodeBytes() { BitWriter w = new BitWriter(); encode(w); return w.toBytes(); }

    @Override public String toString() {
        return "StringMsg(" + "id=" + id + ", " + "name=" + name + ", " + "label=" + label + ", " + "bounded=" + bounded + ", " + "packed=" + packed + ", " + "term=" + term + ")";
    }
}
