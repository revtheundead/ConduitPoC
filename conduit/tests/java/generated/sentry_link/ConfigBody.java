// Generated by bgen - DO NOT EDIT
package sentry_link;

import java.util.*;

public final class ConfigBody {
    public static final long TYPE_ID = 0xe2792bc0a389ea7fL;
    public static final String TYPE_NAME = "ConfigBody";
    public static final int ID_VALUE = 3;

    public String deviceName = "";
    public FirmwareVersion firmware = null;
    public DeviceMode mode = null;
    public int logLevel = 0;
    public int autoReport = 0;
    public int compression = 0;
    public int sampleRate = 0;

    public static ConfigBody decode(BitReader r) {
        ConfigBody result = new ConfigBody();
        result.deviceName = r.readString(16);
        while (result.deviceName.endsWith("\0")) result.deviceName = result.deviceName.substring(0, result.deviceName.length()-1);
        result.firmware = FirmwareVersion.decode(r);
        result.mode = DeviceMode.decode(r);
        result.logLevel = (int) r.readBits(3);
        result.autoReport = (int) r.readBits(1);
        result.compression = (int) r.readBits(1);
        r.skipBits(1);
        result.sampleRate = (int) r.readU16(true);
        return result;
    }

    public static ConfigBody decodeBytes(byte[] data) { return decode(new BitReader(data)); }

    public void encode(BitWriter w) {
        w.writeString(this.deviceName, 16, 0);
        this.firmware.encode(w);
        this.mode.encode(w);
        w.writeBits(this.logLevel, 3);
        w.writeBits(this.autoReport, 1);
        w.writeBits(this.compression, 1);
        w.writeBits(0, 1);
        w.writeU16((int)this.sampleRate, true);
    }

    public byte[] encodeBytes() { BitWriter w = new BitWriter(); encode(w); return w.toBytes(); }

    @Override public String toString() {
        return "ConfigBody(" + "deviceName=" + deviceName + ", " + "firmware=" + firmware + ", " + "mode=" + mode + ", " + "logLevel=" + logLevel + ", " + "autoReport=" + autoReport + ", " + "compression=" + compression + ", " + "sampleRate=" + sampleRate + ")";
    }
}
