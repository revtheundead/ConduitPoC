// Generated by bgen - DO NOT EDIT
package arrays_choices;

import java.util.*;

public final class Typed {

    public Object detail = null;

    public static Typed decode(BitReader r, int subType) {
        Typed result = new Typed();
        if (subType == Constants.SUB_X) {
            result.detail = Alpha.decode(r);
        } else if (subType == Constants.SUB_Y) {
            result.detail = Beta.decode(r);
        }
        return result;
    }

    public void encode(BitWriter w) {
        if (this.detail instanceof Alpha _cv) {
            _cv.encode(w);
        } else if (this.detail instanceof Beta _cv) {
            _cv.encode(w);
        }
    }

    public byte[] encodeBytes() { BitWriter w = new BitWriter(); encode(w); return w.toBytes(); }

    @Override public String toString() {
        return "Typed(" + "detail=" + detail + ")";
    }
}
