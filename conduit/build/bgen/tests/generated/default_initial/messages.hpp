// Generated by bgen - DO NOT EDIT
#pragma once

#include "structs.hpp"
#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <any>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string_view>
#include <variant>
#include <vector>

namespace default_initial {

class DefaultMsg;
class InitialMsg;

class DefaultMsg {
public:
    static constexpr uint64_t TYPE_ID = 0x34b181e66baa4bebULL;
    static constexpr std::string_view TYPE_NAME = "DefaultMsg";

    const uint8& version() const { return version_; }
    uint8& mutable_version() { return version_; }
    void set_version(const uint8& v) { version_ = v; }

    const uint8& priority() const { return priority_; }
    uint8& mutable_priority() { return priority_; }
    void set_priority(const uint8& v) { priority_ = v; }

    const uint32& data() const { return data_; }
    uint32& mutable_data() { return data_; }
    void set_data(const uint32& v) { data_ = v; }

    static constexpr size_t WIRE_SIZE = 6;

    bool operator==(const DefaultMsg&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(version_));
        w.write_u8(static_cast<uint8_t>(priority_));
        w.write_u32(static_cast<uint32_t>(data_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<DefaultMsg> decode(conduit::io::BitReader& r) {
        DefaultMsg result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'version'"));
            result.version_ = static_cast<uint8>(*val);
        }
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'priority'"));
            result.priority_ = static_cast<uint8>(*val);
        }
        {
            auto val = r.read_u32(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'data'"));
            result.data_ = static_cast<uint32>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "DefaultMsg{"
            << "version=" << +(version_)
            << ", priority=" << +(priority_)
            << ", data=" << +(data_)
            << "}";
        return oss.str();
    }

private:
    uint8 version_{};
    uint8 priority_{};
    uint32 data_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<DefaultMsg> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

class InitialMsg {
public:
    static constexpr uint64_t TYPE_ID = 0xfb75771cfe7bc814ULL;
    static constexpr std::string_view TYPE_NAME = "InitialMsg";

    const uint16& counter() const { return counter_; }
    uint16& mutable_counter() { return counter_; }
    void set_counter(const uint16& v) { counter_ = v; }

    const uint8& status() const { return status_; }
    uint8& mutable_status() { return status_; }
    void set_status(const uint8& v) { status_ = v; }

    const uint32& payload() const { return payload_; }
    uint32& mutable_payload() { return payload_; }
    void set_payload(const uint32& v) { payload_ = v; }

    static constexpr size_t WIRE_SIZE = 7;

    bool operator==(const InitialMsg&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u16(static_cast<uint16_t>(counter_), conduit::io::Endian::Big);
        w.write_u8(static_cast<uint8_t>(status_));
        w.write_u32(static_cast<uint32_t>(payload_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<InitialMsg> decode(conduit::io::BitReader& r) {
        InitialMsg result;
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'counter'"));
            result.counter_ = static_cast<uint16>(*val);
        }
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'status'"));
            result.status_ = static_cast<uint8>(*val);
        }
        {
            auto val = r.read_u32(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'payload'"));
            result.payload_ = static_cast<uint32>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "InitialMsg{"
            << "counter=" << +(counter_)
            << ", status=" << +(status_)
            << ", payload=" << +(payload_)
            << "}";
        return oss.str();
    }

private:
    uint16 counter_{100};
    uint8 status_{0};
    uint32 payload_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<InitialMsg> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

} // namespace default_initial

