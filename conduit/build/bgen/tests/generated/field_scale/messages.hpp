// Generated by bgen - DO NOT EDIT
#pragma once

#include "structs.hpp"
#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <any>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string_view>
#include <variant>
#include <vector>

namespace field_scale {

class ScaleMsg;

class ScaleMsg {
public:
    static constexpr uint64_t TYPE_ID = 0x045c717e058a66e6ULL;
    static constexpr std::string_view TYPE_NAME = "ScaleMsg";

    const double& fl() const { return fl_; }
    double& mutable_fl() { return fl_; }
    void set_fl(const double& v) { fl_ = v; }

    const double& temp() const { return temp_; }
    double& mutable_temp() { return temp_; }
    void set_temp(const double& v) { temp_ = v; }

    const uint8& plain() const { return plain_; }
    uint8& mutable_plain() { return plain_; }
    void set_plain(const uint8& v) { plain_ = v; }

    static constexpr size_t WIRE_SIZE = 5;

    bool operator==(const ScaleMsg&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_signed_bits(static_cast<int16_t>(fl_ / 0.25), 12);
        w.write_bits(static_cast<uint16_t>((temp_ - -40) / 0.01), 16);
        w.write_bits(plain_, 8);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<ScaleMsg> decode(conduit::io::BitReader& r) {
        ScaleMsg result;
        {
            auto val = r.read_signed_bits(12);
            if (!val) return std::unexpected(val.error().with_context("field 'fl'"));
            result.fl_ = static_cast<double>(*val) * 0.25 + 0;
        }
        {
            auto val = r.read_bits(16);
            if (!val) return std::unexpected(val.error().with_context("field 'temp'"));
            result.temp_ = static_cast<double>(*val) * 0.01 + -40;
        }
        {
            auto val = r.read_bits(8);
            if (!val) return std::unexpected(val.error().with_context("field 'plain'"));
            result.plain_ = static_cast<uint8>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "ScaleMsg{"
            << "fl=" << +(fl_)
            << ", temp=" << +(temp_)
            << ", plain=" << +(plain_)
            << "}";
        return oss.str();
    }

private:
    double fl_{};
    double temp_{};
    uint8 plain_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<ScaleMsg> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

} // namespace field_scale

