// Generated by bgen - DO NOT EDIT
#pragma once

#include "structs.hpp"
#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <any>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string_view>
#include <variant>
#include <vector>

namespace length_arith {

class HalfLenMsg;
class OffsetLenMsg;
class DoubleLenMsg;
class FieldOpMsg;

class HalfLenMsg {
public:
    static constexpr uint64_t TYPE_ID = 0x8d2d1cc7d2a8f43cULL;
    static constexpr std::string_view TYPE_NAME = "HalfLenMsg";

    const uint8& tag() const { return tag_; }
    uint8& mutable_tag() { return tag_; }
    void set_tag(const uint8& v) { tag_ = v; }

    const uint8& half_len() const { return half_len_; }
    uint8& mutable_half_len() { return half_len_; }
    void set_half_len(const uint8& v) { half_len_ = v; }

    const std::vector<uint8_t>& data() const { return data_; }
    std::vector<uint8_t>& mutable_data() { return data_; }
    void set_data(const std::vector<uint8_t>& v) { data_ = v; }

    const uint8& suffix() const { return suffix_; }
    uint8& mutable_suffix() { return suffix_; }
    void set_suffix(const uint8& v) { suffix_ = v; }

    bool operator==(const HalfLenMsg&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(tag_));
        auto data_length_pos_ = w.size_bytes();
        w.write_u8(static_cast<uint8_t>(0));
        auto data_start_ = w.size_bytes();
        w.write_bytes(std::span<const uint8_t>(data_.data(), data_.size()));
        if (!w.patch_u8(data_length_pos_, static_cast<uint8_t>(((w.size_bytes() - data_start_) / 2))))
            return std::unexpected(conduit::Error(conduit::ErrorCode::InvalidArgument, "failed to patch auto-length(field)"));
        w.write_u8(static_cast<uint8_t>(suffix_));
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<HalfLenMsg> decode(conduit::io::BitReader& r) {
        HalfLenMsg result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'tag'"));
            result.tag_ = static_cast<uint8>(*val);
        }
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'half-len'"));
            result.half_len_ = static_cast<uint8>(*val);
        }
        {
            auto nbytes_ = static_cast<size_t>((result.half_len_ * 2));
            auto span = r.read_bytes(nbytes_);
            if (!span) return std::unexpected(span.error().with_context("field 'data'"));
            result.data_.assign(span->begin(), span->end());
        }
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'suffix'"));
            result.suffix_ = static_cast<uint8>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "HalfLenMsg{"
            << "tag=" << +(tag_)
            << ", half-len=" << +(half_len_)
            << ", data=[bytes]"
            << ", suffix=" << +(suffix_)
            << "}";
        return oss.str();
    }

private:
    uint8 tag_{};
    uint8 half_len_{};
    std::vector<uint8_t> data_{};
    uint8 suffix_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<HalfLenMsg> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

class OffsetLenMsg {
public:
    static constexpr uint64_t TYPE_ID = 0x037d919423bee316ULL;
    static constexpr std::string_view TYPE_NAME = "OffsetLenMsg";

    const uint8& tag() const { return tag_; }
    uint8& mutable_tag() { return tag_; }
    void set_tag(const uint8& v) { tag_ = v; }

    const uint8& len_plus_one() const { return len_plus_one_; }
    uint8& mutable_len_plus_one() { return len_plus_one_; }
    void set_len_plus_one(const uint8& v) { len_plus_one_ = v; }

    const std::vector<uint8_t>& data() const { return data_; }
    std::vector<uint8_t>& mutable_data() { return data_; }
    void set_data(const std::vector<uint8_t>& v) { data_ = v; }

    const uint8& suffix() const { return suffix_; }
    uint8& mutable_suffix() { return suffix_; }
    void set_suffix(const uint8& v) { suffix_ = v; }

    bool operator==(const OffsetLenMsg&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(tag_));
        auto data_length_pos_ = w.size_bytes();
        w.write_u8(static_cast<uint8_t>(0));
        auto data_start_ = w.size_bytes();
        w.write_bytes(std::span<const uint8_t>(data_.data(), data_.size()));
        if (!w.patch_u8(data_length_pos_, static_cast<uint8_t>(((w.size_bytes() - data_start_) + 1))))
            return std::unexpected(conduit::Error(conduit::ErrorCode::InvalidArgument, "failed to patch auto-length(field)"));
        w.write_u8(static_cast<uint8_t>(suffix_));
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<OffsetLenMsg> decode(conduit::io::BitReader& r) {
        OffsetLenMsg result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'tag'"));
            result.tag_ = static_cast<uint8>(*val);
        }
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'len-plus-one'"));
            result.len_plus_one_ = static_cast<uint8>(*val);
        }
        {
            auto nbytes_ = static_cast<size_t>((result.len_plus_one_ - 1));
            auto span = r.read_bytes(nbytes_);
            if (!span) return std::unexpected(span.error().with_context("field 'data'"));
            result.data_.assign(span->begin(), span->end());
        }
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'suffix'"));
            result.suffix_ = static_cast<uint8>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "OffsetLenMsg{"
            << "tag=" << +(tag_)
            << ", len-plus-one=" << +(len_plus_one_)
            << ", data=[bytes]"
            << ", suffix=" << +(suffix_)
            << "}";
        return oss.str();
    }

private:
    uint8 tag_{};
    uint8 len_plus_one_{};
    std::vector<uint8_t> data_{};
    uint8 suffix_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<OffsetLenMsg> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

class DoubleLenMsg {
public:
    static constexpr uint64_t TYPE_ID = 0xbb1531b7d0479548ULL;
    static constexpr std::string_view TYPE_NAME = "DoubleLenMsg";

    const uint8& tag() const { return tag_; }
    uint8& mutable_tag() { return tag_; }
    void set_tag(const uint8& v) { tag_ = v; }

    const uint8& double_len() const { return double_len_; }
    uint8& mutable_double_len() { return double_len_; }
    void set_double_len(const uint8& v) { double_len_ = v; }

    const std::vector<uint8_t>& data() const { return data_; }
    std::vector<uint8_t>& mutable_data() { return data_; }
    void set_data(const std::vector<uint8_t>& v) { data_ = v; }

    const uint8& suffix() const { return suffix_; }
    uint8& mutable_suffix() { return suffix_; }
    void set_suffix(const uint8& v) { suffix_ = v; }

    bool operator==(const DoubleLenMsg&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(tag_));
        auto data_length_pos_ = w.size_bytes();
        w.write_u8(static_cast<uint8_t>(0));
        auto data_start_ = w.size_bytes();
        w.write_bytes(std::span<const uint8_t>(data_.data(), data_.size()));
        if (!w.patch_u8(data_length_pos_, static_cast<uint8_t>(((w.size_bytes() - data_start_) * 2))))
            return std::unexpected(conduit::Error(conduit::ErrorCode::InvalidArgument, "failed to patch auto-length(field)"));
        w.write_u8(static_cast<uint8_t>(suffix_));
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<DoubleLenMsg> decode(conduit::io::BitReader& r) {
        DoubleLenMsg result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'tag'"));
            result.tag_ = static_cast<uint8>(*val);
        }
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'double-len'"));
            result.double_len_ = static_cast<uint8>(*val);
        }
        {
            auto nbytes_ = static_cast<size_t>((result.double_len_ / 2));
            auto span = r.read_bytes(nbytes_);
            if (!span) return std::unexpected(span.error().with_context("field 'data'"));
            result.data_.assign(span->begin(), span->end());
        }
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'suffix'"));
            result.suffix_ = static_cast<uint8>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "DoubleLenMsg{"
            << "tag=" << +(tag_)
            << ", double-len=" << +(double_len_)
            << ", data=[bytes]"
            << ", suffix=" << +(suffix_)
            << "}";
        return oss.str();
    }

private:
    uint8 tag_{};
    uint8 double_len_{};
    std::vector<uint8_t> data_{};
    uint8 suffix_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<DoubleLenMsg> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

class FieldOpMsg {
public:
    static constexpr uint64_t TYPE_ID = 0xdb88ad61afa6a5a9ULL;
    static constexpr std::string_view TYPE_NAME = "FieldOpMsg";

    const uint8& overhead() const { return overhead_; }
    uint8& mutable_overhead() { return overhead_; }
    void set_overhead(const uint8& v) { overhead_ = v; }

    const uint8& adjusted_len() const { return adjusted_len_; }
    uint8& mutable_adjusted_len() { return adjusted_len_; }
    void set_adjusted_len(const uint8& v) { adjusted_len_ = v; }

    const std::vector<uint8_t>& data() const { return data_; }
    std::vector<uint8_t>& mutable_data() { return data_; }
    void set_data(const std::vector<uint8_t>& v) { data_ = v; }

    bool operator==(const FieldOpMsg&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(overhead_));
        auto data_length_pos_ = w.size_bytes();
        w.write_u8(static_cast<uint8_t>(0));
        auto data_start_ = w.size_bytes();
        w.write_bytes(std::span<const uint8_t>(data_.data(), data_.size()));
        if (!w.patch_u8(data_length_pos_, static_cast<uint8_t>(((w.size_bytes() - data_start_) - overhead_))))
            return std::unexpected(conduit::Error(conduit::ErrorCode::InvalidArgument, "failed to patch auto-length(field)"));
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<FieldOpMsg> decode(conduit::io::BitReader& r) {
        FieldOpMsg result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'overhead'"));
            result.overhead_ = static_cast<uint8>(*val);
        }
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'adjusted-len'"));
            result.adjusted_len_ = static_cast<uint8>(*val);
        }
        {
            auto nbytes_ = static_cast<size_t>((result.adjusted_len_ + result.overhead_));
            auto span = r.read_bytes(nbytes_);
            if (!span) return std::unexpected(span.error().with_context("field 'data'"));
            result.data_.assign(span->begin(), span->end());
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "FieldOpMsg{"
            << "overhead=" << +(overhead_)
            << ", adjusted-len=" << +(adjusted_len_)
            << ", data=[bytes]"
            << "}";
        return oss.str();
    }

private:
    uint8 overhead_{};
    uint8 adjusted_len_{};
    std::vector<uint8_t> data_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<FieldOpMsg> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

} // namespace length_arith

