// Generated by bgen - DO NOT EDIT
#pragma once

#include "structs.hpp"
#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <any>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string_view>
#include <variant>
#include <vector>

namespace auto_count {

class CountMsg;

class CountMsg {
public:
    static constexpr uint64_t TYPE_ID = 0x65c9c268e0f0d3cdULL;
    static constexpr std::string_view TYPE_NAME = "CountMsg";

    const uint8& id() const { return id_; }
    uint8& mutable_id() { return id_; }
    void set_id(const uint8& v) { id_ = v; }

    const uint16& num_entries() const { return num_entries_; }
    uint16& mutable_num_entries() { return num_entries_; }
    void set_num_entries(const uint16& v) { num_entries_ = v; }

    const std::vector<uint16_t>& entries() const { return entries_; }
    std::vector<uint16_t>& mutable_entries() { return entries_; }
    void set_entries(const std::vector<uint16_t>& v) { entries_ = v; }

    bool operator==(const CountMsg&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(id_));
        w.write_u16(static_cast<uint16_t>(static_cast<uint16>(entries_.size())), conduit::io::Endian::Big);
        for (const auto& elem : entries_) {
            w.write_u16(static_cast<uint16_t>(elem), conduit::io::Endian::Big);
        }
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<CountMsg> decode(conduit::io::BitReader& r) {
        CountMsg result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'id'"));
            result.id_ = static_cast<uint8>(*val);
        }
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'num-entries'"));
            result.num_entries_ = static_cast<uint16>(*val);
        }
        {
            auto count = static_cast<size_t>(result.num_entries_);
            if (count > static_cast<size_t>(INT32_MAX)) return std::unexpected(conduit::Error(conduit::ErrorCode::InvalidArgument, "invalid array count").with_context("array 'entries'"));
            result.entries_.reserve(count);
            for (size_t i = 0; i < count; i++) {
                auto elem = r.read_u16(conduit::io::Endian::Big);
                if (!elem) return std::unexpected(elem.error().with_context("array 'entries'"));
                result.entries_.push_back(static_cast<uint16_t>(*elem));
            }
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "CountMsg{"
            << "id=" << +(id_)
            << ", num-entries=" << +(num_entries_)
            << ", entries=[" << entries_.size() << " items]"
            << "}";
        return oss.str();
    }

private:
    uint8 id_{};
    uint16 num_entries_{};
    std::vector<uint16_t> entries_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<CountMsg> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

} // namespace auto_count

