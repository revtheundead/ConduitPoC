// Generated by bgen - DO NOT EDIT
#pragma once

#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string>
#include <variant>
#include <vector>

namespace auto_count {

class Record;
class Container;

class Record {
public:
    const uint16& value() const { return value_; }
    uint16& mutable_value() { return value_; }
    void set_value(const uint16& v) { value_ = v; }

    static constexpr size_t WIRE_SIZE = 2;

    bool operator==(const Record&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u16(static_cast<uint16_t>(value_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<Record> decode(conduit::io::BitReader& r) {
        Record result;
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'value'"));
            result.value_ = static_cast<uint16>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "Record{"
            << "value=" << +(value_)
            << "}";
        return oss.str();
    }

private:
    uint16 value_{};
};

class Container {
public:
    const uint8& tag() const { return tag_; }
    uint8& mutable_tag() { return tag_; }
    void set_tag(const uint8& v) { tag_ = v; }

    const uint8& count() const { return count_; }
    uint8& mutable_count() { return count_; }
    void set_count(const uint8& v) { count_ = v; }

    const std::vector<Record>& items() const { return items_; }
    std::vector<Record>& mutable_items() { return items_; }
    void set_items(const std::vector<Record>& v) { items_ = v; }

    bool operator==(const Container&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(tag_));
        w.write_u8(static_cast<uint8_t>(static_cast<uint8>(items_.size())));
        for (const auto& elem : items_) {
            CONDUIT_TRY(elem.encode(w));
        }
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<Container> decode(conduit::io::BitReader& r) {
        Container result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'tag'"));
            result.tag_ = static_cast<uint8>(*val);
        }
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'count'"));
            result.count_ = static_cast<uint8>(*val);
        }
        {
            auto count = static_cast<size_t>(result.count_);
            if (count > static_cast<size_t>(INT32_MAX)) return std::unexpected(conduit::Error(conduit::ErrorCode::InvalidArgument, "invalid array count").with_context("array 'items'"));
            result.items_.reserve(count);
            for (size_t i = 0; i < count; i++) {
                auto elem = Record::decode(r);
                if (!elem) return std::unexpected(elem.error().with_context("array 'items'"));
                result.items_.push_back(std::move(*elem));
            }
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "Container{"
            << "tag=" << +(tag_)
            << ", count=" << +(count_)
            << ", items=["
            ;
            for (size_t i = 0; i < items_.size(); ++i) {
                if (i > 0) oss << ", ";
                oss << items_[i].to_string();
            }
            oss << "]"
            << "}";
        return oss.str();
    }

private:
    uint8 tag_{};
    uint8 count_{};
    std::vector<Record> items_{};
};

} // namespace auto_count

