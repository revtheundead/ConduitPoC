// Generated by bgen - DO NOT EDIT
#pragma once

#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string>
#include <variant>
#include <vector>

namespace bitmap_advanced {

class AdvancedBitmap;

class AdvancedBitmap {
public:
    bool has_status() const { return status_.has_value(); }
    const status_code& status() const { return status_.value(); }
    status_code& mutable_status() { if (!status_) status_.emplace(); return *status_; }
    void set_status(const status_code& v) { status_ = v; }
    void clear_status() { status_.reset(); }

    bool has_label() const { return label_.has_value(); }
    const std::string& label() const { return label_.value(); }
    std::string& mutable_label() { if (!label_) label_.emplace(); return *label_; }
    void set_label(const std::string& v) { label_ = v; }
    void clear_label() { label_.reset(); }

    bool has_data() const { return data_.has_value(); }
    const std::array<uint8_t, 4>& data() const { return data_.value(); }
    std::array<uint8_t, 4>& mutable_data() { if (!data_) data_.emplace(); return *data_; }
    void set_data(const std::array<uint8_t, 4>& v) { data_ = v; }
    void clear_data() { data_.reset(); }

    bool has_bcd_field() const { return bcd_field_.has_value(); }
    const bcd_value& bcd_field() const { return bcd_field_.value(); }
    bcd_value& mutable_bcd_field() { if (!bcd_field_) bcd_field_.emplace(); return *bcd_field_; }
    void set_bcd_field(const bcd_value& v) { bcd_field_ = v; }
    void clear_bcd_field() { bcd_field_.reset(); }

    bool has_counter() const { return counter_.has_value(); }
    const uint16& counter() const { return counter_.value(); }
    uint16& mutable_counter() { if (!counter_) counter_.emplace(); return *counter_; }
    void set_counter(const uint16& v) { counter_ = v; }
    void clear_counter() { counter_.reset(); }

    bool operator==(const AdvancedBitmap&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        std::array<uint8_t, 1> fspec{};
        if (status_.has_value()) fspec[0] |= (1 << 0);
        if (label_.has_value()) fspec[0] |= (1 << 1);
        if (data_.has_value()) fspec[0] |= (1 << 2);
        if (bcd_field_.has_value()) fspec[0] |= (1 << 3);
        if (counter_.has_value()) fspec[0] |= (1 << 4);
        w.write_bytes(std::span<const uint8_t>(fspec.data(), 1));
        if (counter_.has_value()) {
            w.write_bits(*counter_, 16);
        }
        if (bcd_field_.has_value()) {
            w.write_bcd(*bcd_field_, 8);
        }
        if (data_.has_value()) {
            w.write_bytes(std::span<const uint8_t>(data_->data(), 4));
        }
        if (label_.has_value()) {
            w.write_string(*label_, 8);
        }
        if (status_.has_value()) {
            CONDUIT_TRY(encode_status_code(*status_, w));
        }
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<AdvancedBitmap> decode(conduit::io::BitReader& r) {
        AdvancedBitmap result;

        // Read FSPEC bitmap
        std::array<uint8_t, 1> fspec{};
        size_t fspec_len = 0;
        for (size_t i = 0; i < 1; i++) {
            auto byte = r.read_u8();
            if (!byte) return std::unexpected(byte.error());
            fspec[i] = *byte;
        }
        fspec_len = 1;

        if (fspec_len > 0 && (fspec[0] & (1 << 4))) {
            auto val = r.read_bits(16);
            if (!val) return std::unexpected(val.error());
            result.counter_ = static_cast<uint16>(*val);
        }
        if (fspec_len > 0 && (fspec[0] & (1 << 3))) {
            auto val = r.read_bcd(8);
            if (!val) return std::unexpected(val.error());
            result.bcd_field_ = static_cast<bcd_value>(*val);
        }
        if (fspec_len > 0 && (fspec[0] & (1 << 2))) {
            {
                auto val = r.read_bytes(4);
                if (!val) return std::unexpected(val.error());
                result.data_.emplace();
                std::copy(val->begin(), val->end(), result.data_->begin());
            }
        }
        if (fspec_len > 0 && (fspec[0] & (1 << 1))) {
            {
                auto val = r.read_string(8);
                if (!val) return std::unexpected(val.error());
                result.label_ = std::move(*val);
                while (!(*result.label_).empty() && ((*result.label_).back() == '\0' || (*result.label_).back() == ' '))
                    (*result.label_).pop_back();
            }
        }
        if (fspec_len > 0 && (fspec[0] & (1 << 0))) {
            {
                auto val = decode_status_code(r);
                if (!val) return std::unexpected(val.error());
                result.status_ = std::move(*val);
            }
        }

        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "AdvancedBitmap{";
        bool first = true;
        if (status_.has_value()) {
            if (!first) oss << ", ";
            first = false;
            oss << "status=" << ::bitmap_advanced::to_string(*status_);
        }
        if (label_.has_value()) {
            if (!first) oss << ", ";
            first = false;
            oss << "label=" << *label_;
        }
        if (data_.has_value()) {
            if (!first) oss << ", ";
            first = false;
            oss << "data=[bytes]";
        }
        if (bcd_field_.has_value()) {
            if (!first) oss << ", ";
            first = false;
            oss << "bcd-field=" << static_cast<uint64_t>(*bcd_field_);
        }
        if (counter_.has_value()) {
            if (!first) oss << ", ";
            first = false;
            oss << "counter=" << static_cast<uint64_t>(*counter_);
        }
        oss << "}";
        return oss.str();
    }

private:
    std::optional<status_code> status_;
    std::optional<std::string> label_;
    std::optional<std::array<uint8_t, 4>> data_;
    std::optional<bcd_value> bcd_field_;
    std::optional<uint16> counter_;
};

} // namespace bitmap_advanced

