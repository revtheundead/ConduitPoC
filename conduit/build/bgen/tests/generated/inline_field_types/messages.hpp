// Generated by bgen - DO NOT EDIT
#pragma once

#include "structs.hpp"
#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <conduit/string/encoding.hpp>
#include <algorithm>
#include <any>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string_view>
#include <variant>
#include <vector>

namespace inline_field_types {

class InlineMsg;

class InlineMsg {
public:
    static constexpr uint64_t TYPE_ID = 0x27ce85f67367f9edULL;
    static constexpr std::string_view TYPE_NAME = "InlineMsg";

    const float& temperature() const { return temperature_; }
    float& mutable_temperature() { return temperature_; }
    void set_temperature(const float& v) { temperature_ = v; }

    const double& latitude() const { return latitude_; }
    double& mutable_latitude() { return latitude_; }
    void set_latitude(const double& v) { latitude_ = v; }

    const int16_t& offset() const { return offset_; }
    int16_t& mutable_offset() { return offset_; }
    void set_offset(const int16_t& v) { offset_ = v; }

    const std::string& callsign() const { return callsign_; }
    std::string& mutable_callsign() { return callsign_; }
    void set_callsign(const std::string& v) { callsign_ = v; }

    const std::array<uint8_t, 4>& raw_data() const { return raw_data_; }
    std::array<uint8_t, 4>& mutable_raw_data() { return raw_data_; }
    void set_raw_data(const std::array<uint8_t, 4>& v) { raw_data_ = v; }

    const uint8_t& active() const { return active_; }
    uint8_t& mutable_active() { return active_; }
    void set_active(const uint8_t& v) { active_ = v; }

    const uint16_t& mode3a() const { return mode3a_; }
    uint16_t& mutable_mode3a() { return mode3a_; }
    void set_mode3a(const uint16_t& v) { mode3a_ = v; }

    const uint8& tag() const { return tag_; }
    uint8& mutable_tag() { return tag_; }
    void set_tag(const uint8& v) { tag_ = v; }

    bool operator==(const InlineMsg&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_f32(temperature_, conduit::io::Endian::Big);
        w.write_f64(latitude_, conduit::io::Endian::Big);
        w.write_signed_bits(offset_, 16);
        {
            auto wire = conduit::string::from_ascii(callsign_, conduit::string::Encoding::IA5);
            w.write_string(wire, 8, ' ');
        }
        w.write_bytes(std::span<const uint8_t>(raw_data_.data(), raw_data_.size()));
        w.write_bits(active_, 1);
        w.write_bits(mode3a_, 12);
        w.write_bits(0, 3);
        w.write_u8(static_cast<uint8_t>(tag_));
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<InlineMsg> decode(conduit::io::BitReader& r) {
        InlineMsg result;
        {
            auto val = r.read_f32(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'temperature'"));
            result.temperature_ = static_cast<float>(*val);
        }
        {
            auto val = r.read_f64(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'latitude'"));
            result.latitude_ = static_cast<double>(*val);
        }
        {
            auto val = ([&]() -> conduit::Result<int16_t> { auto v = r.read_u16(conduit::io::Endian::Big); if (!v) return std::unexpected(v.error()); return static_cast<int16_t>(*v); })();
            if (!val) return std::unexpected(val.error().with_context("field 'offset'"));
            result.offset_ = static_cast<int16_t>(*val);
        }
        {
            auto val = r.read_string(8);
            if (!val) return std::unexpected(val.error().with_context("field 'callsign'"));
            result.callsign_ = conduit::string::to_ascii(*val, conduit::string::Encoding::IA5);
            while (!result.callsign_.empty() && (result.callsign_.back() == '\0' || result.callsign_.back() == ' '))
                result.callsign_.pop_back();
        }
        {
            auto span = r.read_bytes(4);
            if (!span) return std::unexpected(span.error().with_context("field 'raw-data'"));
            std::copy(span->begin(), span->end(), result.raw_data_.begin());
        }
        {
            auto val = r.read_bits(1);
            if (!val) return std::unexpected(val.error().with_context("field 'active'"));
            result.active_ = static_cast<uint8_t>(*val);
        }
        {
            auto val = r.read_bits(12);
            if (!val) return std::unexpected(val.error().with_context("field 'mode3a'"));
            result.mode3a_ = static_cast<uint16_t>(*val);
        }
        CONDUIT_TRY(r.skip_bits(3));
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'tag'"));
            result.tag_ = static_cast<uint8>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "InlineMsg{"
            << "temperature=" << +(temperature_)
            << ", latitude=" << +(latitude_)
            << ", offset=" << +(offset_)
            << ", callsign=" << callsign_
            << ", raw-data=[bytes]"
            << ", active=" << +(active_)
            << ", mode3a=0" << std::oct << static_cast<uint64_t>(mode3a_) << std::dec
            << ", tag=" << +(tag_)
            << "}";
        return oss.str();
    }

private:
    float temperature_{};
    double latitude_{};
    int16_t offset_{};
    std::string callsign_{};
    std::array<uint8_t, 4> raw_data_{};
    uint8_t active_{};
    uint16_t mode3a_{};
    uint8 tag_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<InlineMsg> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

} // namespace inline_field_types

