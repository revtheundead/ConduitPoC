// Generated by bgen - DO NOT EDIT
#pragma once

#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string>
#include <variant>
#include <vector>

namespace present_when_complex {

class Packet;

class Packet_TypeA {
public:
    const uint16& a_val() const { return a_val_; }
    uint16& mutable_a_val() { return a_val_; }
    void set_a_val(const uint16& v) { a_val_ = v; }

    bool operator==(const Packet_TypeA&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u16(static_cast<uint16_t>(a_val_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<Packet_TypeA> decode(conduit::io::BitReader& r) {
        Packet_TypeA result;
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'a-val'"));
            result.a_val_ = static_cast<uint16>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "Packet_TypeA{"
            << "a-val=" << +(a_val_)
            << "}";
        return oss.str();
    }

private:
    uint16 a_val_{};
};

class Packet_TypeB {
public:
    const uint8& b_val() const { return b_val_; }
    uint8& mutable_b_val() { return b_val_; }
    void set_b_val(const uint8& v) { b_val_ = v; }

    bool operator==(const Packet_TypeB&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(b_val_));
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<Packet_TypeB> decode(conduit::io::BitReader& r) {
        Packet_TypeB result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'b-val'"));
            result.b_val_ = static_cast<uint8>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "Packet_TypeB{"
            << "b-val=" << +(b_val_)
            << "}";
        return oss.str();
    }

private:
    uint8 b_val_{};
};

using extraVariant = std::variant<Packet_TypeA, Packet_TypeB>;
class Packet {
public:
    const uint8& flags() const { return flags_; }
    uint8& mutable_flags() { return flags_; }
    void set_flags(const uint8& v) { flags_ = v; }

    const uint8& count() const { return count_; }
    uint8& mutable_count() { return count_; }
    void set_count(const uint8& v) { count_ = v; }

    bool has_items() const { return items_.has_value(); }
    const std::vector<uint16_t>& items() const { return items_.value(); }
    std::vector<uint16_t>& mutable_items() { if (!items_) items_.emplace(); return *items_; }
    void set_items(const std::vector<uint16_t>& v) { items_ = v; }
    void clear_items() { items_.reset(); }

    bool has_extra() const { return extra_.has_value(); }
    const extraVariant& extra() const { return extra_.value(); }
    extraVariant& mutable_extra() { if (!extra_) extra_.emplace(); return *extra_; }
    void set_extra(const extraVariant& v) { extra_ = v; }
    void clear_extra() { extra_.reset(); }

    const uint8& trailer() const { return trailer_; }
    uint8& mutable_trailer() { return trailer_; }
    void set_trailer(const uint8& v) { trailer_ = v; }

    bool operator==(const Packet&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(flags_));
        w.write_u8(static_cast<uint8_t>(count_));
        if (items_.has_value()) {
            for (const auto& elem : *items_) {
                w.write_u16(static_cast<uint16_t>(elem), conduit::io::Endian::Big);
            }
        }
        if (extra_.has_value()) {
            {
                auto _enc_r = std::visit([&w](const auto& v) -> conduit::VoidResult { return v.encode(w); }, *extra_);
                if (!_enc_r) return std::unexpected(_enc_r.error());
            }
        }
        w.write_u8(static_cast<uint8_t>(trailer_));
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<Packet> decode(conduit::io::BitReader& r) {
        Packet result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'flags'"));
            result.flags_ = static_cast<uint8>(*val);
        }
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'count'"));
            result.count_ = static_cast<uint8>(*val);
        }
        if ((result.flags_ & 1)) {
            result.items_.emplace();
            {
                auto count = static_cast<size_t>(result.count_);
                if (count > static_cast<size_t>(INT32_MAX)) return std::unexpected(conduit::Error(conduit::ErrorCode::InvalidArgument, "invalid array count").with_context("array 'items'"));
                (*result.items_).reserve(count);
                for (size_t i = 0; i < count; i++) {
                    auto elem = r.read_u16(conduit::io::Endian::Big);
                    if (!elem) return std::unexpected(elem.error().with_context("array 'items'"));
                    (*result.items_).push_back(static_cast<uint16_t>(*elem));
                }
            }
        }
        if ((result.flags_ & 2)) {
            {
                auto switch_val = (result.flags_ & 240);
                if (switch_val == static_cast<decltype(switch_val)>(0x10)) {
                    auto val = Packet_TypeA::decode(r);
                    if (!val) return std::unexpected(val.error().with_context("choice 'extra'"));
                    result.extra_ = std::move(*val);
                } else if (switch_val == static_cast<decltype(switch_val)>(0x20)) {
                    auto val = Packet_TypeB::decode(r);
                    if (!val) return std::unexpected(val.error().with_context("choice 'extra'"));
                    result.extra_ = std::move(*val);
                } else {
                    return std::unexpected(conduit::Error(conduit::ErrorCode::UnknownDiscriminator,
                        "choice 'extra': no case matched switch value"));
                }
            }
        }
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'trailer'"));
            result.trailer_ = static_cast<uint8>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "Packet{"
            << "flags=" << +(flags_)
            << ", count=" << +(count_)
            << ", items=" << (items_.has_value() ? "set" : "<none>")
            << ", extra=" << (extra_.has_value() ? std::visit([](const auto& v) { return v.to_string(); }, *extra_) : std::string("<none>"))
            << ", trailer=" << +(trailer_)
            << "}";
        return oss.str();
    }

private:
    uint8 flags_{};
    uint8 count_{};
    std::optional<std::vector<uint16_t>> items_;
    std::optional<extraVariant> extra_;
    uint8 trailer_{};
};

} // namespace present_when_complex

