// Generated by bgen - DO NOT EDIT
#pragma once

#include "structs.hpp"
#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <any>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string_view>
#include <variant>
#include <vector>

namespace bitmap_fx {

class Category;

class Category {
public:
    static constexpr uint64_t TYPE_ID = 0x079a052719914d71ULL;
    static constexpr std::string_view TYPE_NAME = "Category";

    const BitmapItems& items() const { return items_; }
    BitmapItems& mutable_items() { return items_; }
    void set_items(const BitmapItems& v) { items_ = v; }

    bool operator==(const Category&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        CONDUIT_TRY(items_.encode(w));
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<Category> decode(conduit::io::BitReader& r) {
        Category result;
        {
            auto val = BitmapItems::decode(r);
            if (!val) return std::unexpected(val.error().with_context("field 'items'"));
            result.items_ = std::move(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "Category{"
            << "items=" << items_.to_string()
            << "}";
        return oss.str();
    }

private:
    BitmapItems items_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<Category> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

} // namespace bitmap_fx

