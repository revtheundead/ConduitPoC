// Generated by bgen - DO NOT EDIT
#pragma once

#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string>
#include <variant>
#include <vector>

namespace fx_advanced {

class FxSubStruct;

class FxSubStruct {
public:
    const uint8& a() const { return a_; }
    uint8& mutable_a() { return a_; }
    void set_a(const uint8& v) { a_ = v; }

    const uint8& b() const { return b_; }
    uint8& mutable_b() { return b_; }
    void set_b(const uint8& v) { b_ = v; }

    static constexpr size_t WIRE_SIZE = 2;

    bool operator==(const FxSubStruct&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(a_));
        w.write_u8(static_cast<uint8_t>(b_));
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<FxSubStruct> decode(conduit::io::BitReader& r) {
        FxSubStruct result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'a'"));
            result.a_ = static_cast<uint8>(*val);
        }
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'b'"));
            result.b_ = static_cast<uint8>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "FxSubStruct{"
            << "a=" << +(a_)
            << ", b=" << +(b_)
            << "}";
        return oss.str();
    }

private:
    uint8 a_{};
    uint8 b_{};
};

} // namespace fx_advanced

