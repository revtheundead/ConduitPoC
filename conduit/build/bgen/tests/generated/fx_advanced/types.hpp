// Generated by bgen - DO NOT EDIT
#pragma once

#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <conduit/io/endian.hpp>
#include <cstdint>
#include <string>
#include <string_view>

namespace fx_advanced {

using uint8 = uint8_t;
using uint16 = uint16_t;
using int16 = int16_t;
enum class fx_status : uint8_t {
    idle = 0,
    active = 1,
    complete = 2,
};

inline std::string_view to_string(fx_status v) {
    switch (v) {
        case fx_status::idle: return "idle";
        case fx_status::active: return "active";
        case fx_status::complete: return "complete";
    }
    return "unknown";
}

inline conduit::Result<fx_status> decode_fx_status(conduit::io::BitReader& r) {
    auto raw = r.read_bits(8);
    if (!raw) return std::unexpected(raw.error());
    auto val = static_cast<fx_status>(*raw);
    switch (val) {
        case fx_status::idle:
        case fx_status::active:
        case fx_status::complete:
            return val;
    }
    return std::unexpected(conduit::Error(conduit::ErrorCode::UnknownEnumValue,
        "unknown fx_status value: " + std::to_string(static_cast<uint8_t>(val))));
}

inline conduit::VoidResult encode_fx_status(fx_status v, conduit::io::BitWriter& w) {
    switch (v) {
        case fx_status::idle:
        case fx_status::active:
        case fx_status::complete:
            break;
        default:
            return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                "invalid fx_status enum value: " + std::to_string(static_cast<uint8_t>(v))));
    }
    w.write_bits(static_cast<uint8_t>(v), 8);
    if (w.has_error()) return std::unexpected(w.error());
    return {};
}


} // namespace fx_advanced

