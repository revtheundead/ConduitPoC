// Generated by bgen - DO NOT EDIT
#pragma once

#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string>
#include <variant>
#include <vector>

namespace bitmap_wide_fixed {

class WideBitmap;

class WideBitmap {
public:
    bool has_alpha() const { return alpha_.has_value(); }
    const uint8& alpha() const { return alpha_.value(); }
    uint8& mutable_alpha() { if (!alpha_) alpha_.emplace(); return *alpha_; }
    void set_alpha(const uint8& v) { alpha_ = v; }
    void clear_alpha() { alpha_.reset(); }

    bool has_beta() const { return beta_.has_value(); }
    const uint16& beta() const { return beta_.value(); }
    uint16& mutable_beta() { if (!beta_) beta_.emplace(); return *beta_; }
    void set_beta(const uint16& v) { beta_ = v; }
    void clear_beta() { beta_.reset(); }

    bool has_gamma() const { return gamma_.has_value(); }
    const uint32& gamma() const { return gamma_.value(); }
    uint32& mutable_gamma() { if (!gamma_) gamma_.emplace(); return *gamma_; }
    void set_gamma(const uint32& v) { gamma_ = v; }
    void clear_gamma() { gamma_.reset(); }

    bool operator==(const WideBitmap&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        std::array<uint8_t, 3> fspec{};
        if (alpha_.has_value()) fspec[0] |= (1 << 0);
        if (beta_.has_value()) fspec[1] |= (1 << 0);
        if (gamma_.has_value()) fspec[2] |= (1 << 0);
        w.write_bytes(std::span<const uint8_t>(fspec.data(), 3));
        if (alpha_.has_value()) {
            w.write_bits(*alpha_, 8);
        }
        if (beta_.has_value()) {
            w.write_bits(*beta_, 16);
        }
        if (gamma_.has_value()) {
            w.write_bits(*gamma_, 32);
        }
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<WideBitmap> decode(conduit::io::BitReader& r) {
        WideBitmap result;

        // Read FSPEC bitmap
        std::array<uint8_t, 3> fspec{};
        size_t fspec_len = 0;
        for (size_t i = 0; i < 3; i++) {
            auto byte = r.read_u8();
            if (!byte) return std::unexpected(byte.error());
            fspec[i] = *byte;
        }
        fspec_len = 3;

        if (fspec_len > 0 && (fspec[0] & (1 << 0))) {
            auto val = r.read_bits(8);
            if (!val) return std::unexpected(val.error());
            result.alpha_ = static_cast<uint8>(*val);
        }
        if (fspec_len > 1 && (fspec[1] & (1 << 0))) {
            auto val = r.read_bits(16);
            if (!val) return std::unexpected(val.error());
            result.beta_ = static_cast<uint16>(*val);
        }
        if (fspec_len > 2 && (fspec[2] & (1 << 0))) {
            auto val = r.read_bits(32);
            if (!val) return std::unexpected(val.error());
            result.gamma_ = static_cast<uint32>(*val);
        }

        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "WideBitmap{";
        bool first = true;
        if (alpha_.has_value()) {
            if (!first) oss << ", ";
            first = false;
            oss << "alpha=" << static_cast<uint64_t>(*alpha_);
        }
        if (beta_.has_value()) {
            if (!first) oss << ", ";
            first = false;
            oss << "beta=" << static_cast<uint64_t>(*beta_);
        }
        if (gamma_.has_value()) {
            if (!first) oss << ", ";
            first = false;
            oss << "gamma=" << static_cast<uint64_t>(*gamma_);
        }
        oss << "}";
        return oss.str();
    }

private:
    std::optional<uint8> alpha_;
    std::optional<uint16> beta_;
    std::optional<uint32> gamma_;
};

} // namespace bitmap_wide_fixed

