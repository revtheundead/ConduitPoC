// Generated by bgen - DO NOT EDIT
#pragma once

#include "structs.hpp"
#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <any>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string_view>
#include <variant>
#include <vector>

namespace string_features {

class StringMsg;
class TermStringMsg;
class MaxLenMsg;
class InlineStringMsg;

class StringMsg {
public:
    static constexpr uint64_t TYPE_ID = 0x588b3c6370465949ULL;
    static constexpr std::string_view TYPE_NAME = "StringMsg";

    const uint16& id() const { return id_; }
    uint16& mutable_id() { return id_; }
    void set_id(const uint16& v) { id_ = v; }

    const name_str& name() const { return name_; }
    name_str& mutable_name() { return name_; }
    void set_name(const name_str& v) { name_ = v; }

    const label_str& label() const { return label_; }
    label_str& mutable_label() { return label_; }
    void set_label(const label_str& v) { label_ = v; }

    const bounded_str& bounded() const { return bounded_; }
    bounded_str& mutable_bounded() { return bounded_; }
    void set_bounded(const bounded_str& v) { bounded_ = v; }

    const packed_str& packed() const { return packed_; }
    packed_str& mutable_packed() { return packed_; }
    void set_packed(const packed_str& v) { packed_ = v; }

    const term_str& term() const { return term_; }
    term_str& mutable_term() { return term_; }
    void set_term(const term_str& v) { term_ = v; }

    static constexpr size_t WIRE_SIZE = 140;

    bool operator==(const StringMsg&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u16(static_cast<uint16_t>(id_), conduit::io::Endian::Big);
        CONDUIT_TRY(name_.encode(w));
        CONDUIT_TRY(label_.encode(w));
        CONDUIT_TRY(bounded_.encode(w));
        CONDUIT_TRY(packed_.encode(w));
        CONDUIT_TRY(term_.encode(w));
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<StringMsg> decode(conduit::io::BitReader& r) {
        StringMsg result;
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'id'"));
            result.id_ = static_cast<uint16>(*val);
        }
        {
            auto val = name_str::decode(r);
            if (!val) return std::unexpected(val.error().with_context("field 'name'"));
            result.name_ = std::move(*val);
        }
        {
            auto val = label_str::decode(r);
            if (!val) return std::unexpected(val.error().with_context("field 'label'"));
            result.label_ = std::move(*val);
        }
        {
            auto val = bounded_str::decode(r);
            if (!val) return std::unexpected(val.error().with_context("field 'bounded'"));
            result.bounded_ = std::move(*val);
        }
        {
            auto val = packed_str::decode(r);
            if (!val) return std::unexpected(val.error().with_context("field 'packed'"));
            result.packed_ = std::move(*val);
        }
        {
            auto val = term_str::decode(r);
            if (!val) return std::unexpected(val.error().with_context("field 'term'"));
            result.term_ = std::move(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "StringMsg{"
            << "id=" << +(id_)
            << ", name=" << name_.value()
            << ", label=" << label_.value()
            << ", bounded=" << bounded_.value()
            << ", packed=" << packed_.value()
            << ", term=" << term_.value()
            << "}";
        return oss.str();
    }

private:
    uint16 id_{};
    name_str name_{};
    label_str label_{};
    bounded_str bounded_{};
    packed_str packed_{};
    term_str term_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<StringMsg> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

class TermStringMsg {
public:
    static constexpr uint64_t TYPE_ID = 0x401c9bc627232dd7ULL;
    static constexpr std::string_view TYPE_NAME = "TermStringMsg";

    const uint8& id() const { return id_; }
    uint8& mutable_id() { return id_; }
    void set_id(const uint8& v) { id_ = v; }

    const std::string& null_term() const { return null_term_; }
    std::string& mutable_null_term() { return null_term_; }
    void set_null_term(const std::string& v) { null_term_ = v; }

    const std::string& newline_term() const { return newline_term_; }
    std::string& mutable_newline_term() { return newline_term_; }
    void set_newline_term(const std::string& v) { newline_term_ = v; }

    const std::string& crlf_term() const { return crlf_term_; }
    std::string& mutable_crlf_term() { return crlf_term_; }
    void set_crlf_term(const std::string& v) { crlf_term_ = v; }

    bool operator==(const TermStringMsg&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(id_));
        w.write_string(null_term_, null_term_.size());
        w.write_u8(0x00);
        w.write_string(newline_term_, newline_term_.size());
        w.write_u8(0x0A);
        w.write_string(crlf_term_, crlf_term_.size());
        w.write_u8(0x0D);
        w.write_u8(0x0A);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<TermStringMsg> decode(conduit::io::BitReader& r) {
        TermStringMsg result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'id'"));
            result.id_ = static_cast<uint8>(*val);
        }
        {
            std::string s;
            s.reserve(64);
            for (size_t i = 0; i < 65535; i++) {
                auto b = r.read_u8();
                if (!b) return std::unexpected(b.error().with_context("field 'null-term'"));
                if (*b == 0x00) break;
                s += static_cast<char>(*b);
            }
            result.null_term_ = std::move(s);
            while (!result.null_term_.empty() && (result.null_term_.back() == '\0' || result.null_term_.back() == ' '))
                result.null_term_.pop_back();
        }
        {
            std::string s;
            s.reserve(64);
            for (size_t i = 0; i < 65535; i++) {
                auto b = r.read_u8();
                if (!b) return std::unexpected(b.error().with_context("field 'newline-term'"));
                if (*b == 0x0A) break;
                s += static_cast<char>(*b);
            }
            result.newline_term_ = std::move(s);
            while (!result.newline_term_.empty() && (result.newline_term_.back() == '\0' || result.newline_term_.back() == ' '))
                result.newline_term_.pop_back();
        }
        {
            std::string s;
            s.reserve(64);
            for (size_t i = 0; i < 65535; i++) {
                auto b = r.read_u8();
                if (!b) return std::unexpected(b.error().with_context("field 'crlf-term'"));
                if (*b == 0x0D) {
                    auto b2 = r.read_u8();
                    if (!b2) return std::unexpected(b2.error().with_context("field 'crlf-term'"));
                    if (*b2 == 0x0A) break;
                    s += static_cast<char>(*b);
                    s += static_cast<char>(*b2);
                } else {
                    s += static_cast<char>(*b);
                }
            }
            result.crlf_term_ = std::move(s);
            while (!result.crlf_term_.empty() && (result.crlf_term_.back() == '\0' || result.crlf_term_.back() == ' '))
                result.crlf_term_.pop_back();
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "TermStringMsg{"
            << "id=" << +(id_)
            << ", null-term=" << null_term_
            << ", newline-term=" << newline_term_
            << ", crlf-term=" << crlf_term_
            << "}";
        return oss.str();
    }

private:
    uint8 id_{};
    std::string null_term_{};
    std::string newline_term_{};
    std::string crlf_term_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<TermStringMsg> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

class MaxLenMsg {
public:
    static constexpr uint64_t TYPE_ID = 0xe8b55abe19cef021ULL;
    static constexpr std::string_view TYPE_NAME = "MaxLenMsg";

    const uint8& id() const { return id_; }
    uint8& mutable_id() { return id_; }
    void set_id(const uint8& v) { id_ = v; }

    const std::string& data() const { return data_; }
    std::string& mutable_data() { return data_; }
    [[nodiscard]] conduit::VoidResult set_data(const std::string& v) {
        if (v.size() > 16)
            return std::unexpected(conduit::Error(conduit::ErrorCode::StringTooLong,
                "data exceeds max length 16"));
        data_ = v;
        return {};
    }

    static constexpr size_t WIRE_SIZE = 33;

    bool operator==(const MaxLenMsg&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(id_));
        if (data_.size() > 16)
            return std::unexpected(conduit::Error(conduit::ErrorCode::StringTooLong,
                "data exceeds max length 16"));
        w.write_string(data_, 32, '\0');
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<MaxLenMsg> decode(conduit::io::BitReader& r) {
        MaxLenMsg result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'id'"));
            result.id_ = static_cast<uint8>(*val);
        }
        {
            auto val = r.read_string(32);
            if (!val) return std::unexpected(val.error().with_context("field 'data'"));
            result.data_ = std::move(*val);
            while (!result.data_.empty() && (result.data_.back() == '\0' || result.data_.back() == ' '))
                result.data_.pop_back();
        }
        if (result.data_.size() > 16) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "data exceeds max length 16"));
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "MaxLenMsg{"
            << "id=" << +(id_)
            << ", data=" << data_
            << "}";
        return oss.str();
    }

private:
    uint8 id_{};
    std::string data_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<MaxLenMsg> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

class InlineStringMsg {
public:
    static constexpr uint64_t TYPE_ID = 0x615b88d7aa2856f2ULL;
    static constexpr std::string_view TYPE_NAME = "InlineStringMsg";

    const uint8& id() const { return id_; }
    uint8& mutable_id() { return id_; }
    void set_id(const uint8& v) { id_ = v; }

    const name_str& inline_name() const { return inline_name_; }
    name_str& mutable_inline_name() { return inline_name_; }
    void set_inline_name(const name_str& v) { inline_name_ = v; }

    const uint8& prefix_str() const { return prefix_str_; }
    uint8& mutable_prefix_str() { return prefix_str_; }
    void set_prefix_str(const uint8& v) { prefix_str_ = v; }

    static constexpr size_t WIRE_SIZE = 22;

    bool operator==(const InlineStringMsg&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(id_));
        CONDUIT_TRY(inline_name_.encode(w));
        w.write_u8(static_cast<uint8_t>(prefix_str_));
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<InlineStringMsg> decode(conduit::io::BitReader& r) {
        InlineStringMsg result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'id'"));
            result.id_ = static_cast<uint8>(*val);
        }
        {
            auto val = name_str::decode(r);
            if (!val) return std::unexpected(val.error().with_context("field 'inline-name'"));
            result.inline_name_ = std::move(*val);
        }
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'prefix-str'"));
            result.prefix_str_ = static_cast<uint8>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "InlineStringMsg{"
            << "id=" << +(id_)
            << ", inline-name=" << inline_name_.value()
            << ", prefix-str=" << +(prefix_str_)
            << "}";
        return oss.str();
    }

private:
    uint8 id_{};
    name_str inline_name_{};
    uint8 prefix_str_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<InlineStringMsg> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

} // namespace string_features

