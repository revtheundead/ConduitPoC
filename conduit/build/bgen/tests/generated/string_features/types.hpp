// Generated by bgen - DO NOT EDIT
#pragma once

#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <conduit/io/endian.hpp>
#include <cstdint>
#include <string>
#include <string_view>

namespace string_features {

using uint8 = uint8_t;
using uint16 = uint16_t;
// name_str
class name_str {
public:
    static constexpr size_t WIRE_SIZE = 20;

    std::string value() const { return value_; }
    void set_value(const std::string& v) { value_ = v; }

    bool operator==(const name_str&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_string(value_, 20, '\0');
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }
    static conduit::Result<name_str> decode(conduit::io::BitReader& r) {
        auto s = r.read_string(20);
        if (!s) return std::unexpected(s.error());
        name_str result;
        result.value_ = std::move(*s);
        // Trim trailing padding
        while (!result.value_.empty() && (result.value_.back() == '\0' || result.value_.back() == ' '))
            result.value_.pop_back();
        return result;
    }
private:
    std::string value_;
};

// label_str
class label_str {
public:
    static constexpr size_t WIRE_SIZE = 16;

    std::string value() const { return value_; }
    void set_value(const std::string& v) { value_ = v; }

    bool operator==(const label_str&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_string(value_, 16, ' ');
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }
    static conduit::Result<label_str> decode(conduit::io::BitReader& r) {
        auto s = r.read_string(16);
        if (!s) return std::unexpected(s.error());
        label_str result;
        result.value_ = std::move(*s);
        // Trim leading padding
        {
            auto start = result.value_.find_first_not_of(" \0");
            if (start == std::string::npos) result.value_.clear();
            else if (start > 0) result.value_.erase(0, start);
        }
        // Trim trailing padding
        while (!result.value_.empty() && (result.value_.back() == '\0' || result.value_.back() == ' '))
            result.value_.pop_back();
        return result;
    }
private:
    std::string value_;
};

// bounded_str
class bounded_str {
public:
    static constexpr size_t WIRE_SIZE = 32;

    std::string value() const { return value_; }
    void set_value(const std::string& v) { value_ = v; }

    bool operator==(const bounded_str&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_string(value_, 32, '\0');
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }
    static conduit::Result<bounded_str> decode(conduit::io::BitReader& r) {
        auto s = r.read_string(32);
        if (!s) return std::unexpected(s.error());
        bounded_str result;
        result.value_ = std::move(*s);
        // Trim trailing padding
        while (!result.value_.empty() && (result.value_.back() == '\0' || result.value_.back() == ' '))
            result.value_.pop_back();
        return result;
    }
private:
    std::string value_;
};

// packed_str
class packed_str {
public:
    static constexpr size_t CHAR_COUNT = 8;
    static constexpr int CHAR_BITS = 6;
    static constexpr size_t WIRE_SIZE = 6;

    std::string value() const { return value_; }
    void set_value(const std::string& v) { value_ = v; }

    bool operator==(const packed_str&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        for (size_t i = 0; i < CHAR_COUNT; i++) {
            uint8_t ch = (i < value_.size()) ? static_cast<uint8_t>(value_[i]) : 0x00;
            w.write_bits(ch & ((1 << CHAR_BITS) - 1), CHAR_BITS);
        }
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }
    static conduit::Result<packed_str> decode(conduit::io::BitReader& r) {
        packed_str result;
        result.value_.reserve(CHAR_COUNT);
        for (size_t i = 0; i < CHAR_COUNT; i++) {
            auto bits = r.read_bits(CHAR_BITS);
            if (!bits) return std::unexpected(bits.error());
            uint8_t raw = static_cast<uint8_t>(*bits & ((1 << CHAR_BITS) - 1));
            char ch = (raw == 0) ? '\0' : static_cast<char>(raw < 32 ? raw + 0x40 : raw);
            result.value_ += ch;
        }
        // Trim trailing padding
        while (!result.value_.empty() && (result.value_.back() == '\0' || result.value_.back() == ' '))
            result.value_.pop_back();
        return result;
    }
private:
    std::string value_;
};

// term_str
class term_str {
public:
    static constexpr size_t WIRE_SIZE = 64;

    std::string value() const { return value_; }
    void set_value(const std::string& v) { value_ = v; }

    bool operator==(const term_str&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_string(value_, 64, '\0');
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }
    static conduit::Result<term_str> decode(conduit::io::BitReader& r) {
        auto s = r.read_string(64);
        if (!s) return std::unexpected(s.error());
        term_str result;
        result.value_ = std::move(*s);
        // Trim trailing padding
        while (!result.value_.empty() && (result.value_.back() == '\0' || result.value_.back() == ' '))
            result.value_.pop_back();
        return result;
    }
private:
    std::string value_;
};


} // namespace string_features

