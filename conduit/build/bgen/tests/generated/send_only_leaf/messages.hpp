// Generated by bgen - DO NOT EDIT
#pragma once

#include "structs.hpp"
#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <any>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string_view>
#include <variant>
#include <vector>

namespace send_only {

class SendBody;
class RecvBody;

class SendBody {
public:
    static constexpr uint64_t TYPE_ID = 0x8321194a495c5d87ULL;
    static constexpr std::string_view TYPE_NAME = "SendBody";
    static constexpr uint8 ID_VALUE = 1;

    uint8 tag() const { return tag_; }
    [[deprecated("auto-managed: value is set automatically during frame encoding")]]
    void set_tag(uint8 v) { tag_ = v; }

    const uint16& x() const { return x_; }
    uint16& mutable_x() { return x_; }
    void set_x(const uint16& v) { x_ = v; }

    static constexpr size_t WIRE_SIZE = 2;

    bool operator==(const SendBody&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u16(static_cast<uint16_t>(x_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<SendBody> decode(conduit::io::BitReader& r) {
        SendBody result;
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'x'"));
            result.x_ = static_cast<uint16>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "SendBody{"
            << "tag=" << +(tag_)
            << ", x=" << +(x_)
            << "}";
        return oss.str();
    }

private:
    friend class Frame;

    uint8 tag_{};
    uint16 x_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<SendBody> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

class RecvBody {
public:
    static constexpr uint64_t TYPE_ID = 0xb7642cab94a1eb7dULL;
    static constexpr std::string_view TYPE_NAME = "RecvBody";
    static constexpr uint8 ID_VALUE = 2;

    uint8 tag() const { return tag_; }
    [[deprecated("auto-managed: value is set automatically during frame encoding")]]
    void set_tag(uint8 v) { tag_ = v; }

    const uint16& y() const { return y_; }
    uint16& mutable_y() { return y_; }
    void set_y(const uint16& v) { y_ = v; }

    static constexpr size_t WIRE_SIZE = 2;

    bool operator==(const RecvBody&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u16(static_cast<uint16_t>(y_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<RecvBody> decode(conduit::io::BitReader& r) {
        RecvBody result;
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'y'"));
            result.y_ = static_cast<uint16>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "RecvBody{"
            << "tag=" << +(tag_)
            << ", y=" << +(y_)
            << "}";
        return oss.str();
    }

private:
    friend class Frame;

    uint8 tag_{};
    uint16 y_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<RecvBody> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

class Frame {
public:
    using PayloadVariant = std::variant<SendBody, RecvBody>;

    uint8 tag() const { return tag_; }
    void set_tag(uint8 v) { tag_ = v; }

    const PayloadVariant& payload() const { return payload_; }
    PayloadVariant& payload() { return payload_; }

    static Frame wrap(const SendBody& msg) {
        Frame frame;
        frame.tag_ = SendBody::ID_VALUE;
        frame.payload_ = msg;
        return frame;
    }

    static Frame wrap(const RecvBody& msg) {
        Frame frame;
        frame.tag_ = RecvBody::ID_VALUE;
        frame.payload_ = msg;
        return frame;
    }

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(tag_));
        CONDUIT_TRY(std::visit([&w](const auto& m) -> conduit::VoidResult {
            return m.encode(w);
        }, payload_));
        return {};
    }

    static conduit::Result<Frame> decode(conduit::io::BitReader& r) {
        Frame result;
        auto tag_val_ = r.read_u8();
        if (!tag_val_) return std::unexpected(tag_val_.error());
        result.tag_ = *tag_val_;
        switch (static_cast<uint8>(result.tag_)) {
            case 1: {
                auto payload_val_ = SendBody::decode(r);
                if (!payload_val_) return std::unexpected(payload_val_.error());
                payload_val_->tag_ = result.tag_;
                result.payload_ = std::move(*payload_val_);
                break;
            }
            case 2: {
                auto payload_val_ = RecvBody::decode(r);
                if (!payload_val_) return std::unexpected(payload_val_.error());
                payload_val_->tag_ = result.tag_;
                result.payload_ = std::move(*payload_val_);
                break;
            }
            default:
                return std::unexpected(conduit::Error(conduit::ErrorCode::UnknownDiscriminator,
                    "unknown frame message id: " + std::to_string(static_cast<int>(result.tag_))));
        }
        return result;
    }

    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<Frame> decode_bytes(std::span<const uint8_t> data) {
        conduit::io::BitReader r(data);
        return decode(r);
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "Frame{"
            << "tag=" << +(tag_)
            << ", payload=" << std::visit([](const auto& m) { return m.to_string(); }, payload_)
            << "}";
        return oss.str();
    }

private:
    uint8 tag_{};
    PayloadVariant payload_;
};

} // namespace send_only

