// Generated by bgen - DO NOT EDIT
#pragma once

#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string>
#include <variant>
#include <vector>

namespace expr_features {

class ItemA;
class ItemB;

class ItemA {
public:
    const uint16& val() const { return val_; }
    uint16& mutable_val() { return val_; }
    void set_val(const uint16& v) { val_ = v; }

    static constexpr size_t WIRE_SIZE = 2;

    bool operator==(const ItemA&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u16(static_cast<uint16_t>(val_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<ItemA> decode(conduit::io::BitReader& r) {
        ItemA result;
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'val'"));
            result.val_ = static_cast<uint16>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "ItemA{"
            << "val=" << +(val_)
            << "}";
        return oss.str();
    }

private:
    uint16 val_{};
};

class ItemB {
public:
    const uint32& tag() const { return tag_; }
    uint32& mutable_tag() { return tag_; }
    void set_tag(const uint32& v) { tag_ = v; }

    static constexpr size_t WIRE_SIZE = 4;

    bool operator==(const ItemB&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u32(static_cast<uint32_t>(tag_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<ItemB> decode(conduit::io::BitReader& r) {
        ItemB result;
        {
            auto val = r.read_u32(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'tag'"));
            result.tag_ = static_cast<uint32>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "ItemB{"
            << "tag=" << +(tag_)
            << "}";
        return oss.str();
    }

private:
    uint32 tag_{};
};

} // namespace expr_features

