// Generated by bgen - DO NOT EDIT
#pragma once

#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <conduit/io/endian.hpp>
#include <conduit/string/encoding.hpp>
#include <cstdint>
#include <string>
#include <string_view>

namespace asterix {

using uint8 = uint8_t;
using uint16 = uint16_t;
using uint24 = uint32_t;
using uint32 = uint32_t;
using int8 = int8_t;
using int16 = int16_t;
using int32 = int32_t;
// Time of day in 1/128 second units since midnight UTC
class time_of_day {
public:
    static constexpr double SCALE = 0.0078125;
    double value() const { return static_cast<double>(raw_) * SCALE; }
    uint32_t raw() const { return raw_; }
    void set_value(double v) { raw_ = static_cast<uint32_t>(v / SCALE); }
    void set_raw(uint32_t v) { raw_ = v; }

    bool operator==(const time_of_day&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_bits(raw_, 24);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }
    static conduit::Result<time_of_day> decode(conduit::io::BitReader& r) {
        auto raw = r.read_bits(24);
        if (!raw) return std::unexpected(raw.error());
        time_of_day result;
        result.raw_ = static_cast<uint32_t>(*raw);
        return result;
    }
private:
    uint32_t raw_ = 0;
};

// WGS-84 coordinate with 180/2^23 degree resolution
class wgs84_coarse {
public:
    static constexpr double SCALE = 2.1457672119140625e-05;
    double value() const { return static_cast<double>(raw_) * SCALE; }
    int32_t raw() const { return raw_; }
    void set_value(double v) { raw_ = static_cast<int32_t>(v / SCALE); }
    void set_raw(int32_t v) { raw_ = v; }

    bool operator==(const wgs84_coarse&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_signed_bits(raw_, 24);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }
    static conduit::Result<wgs84_coarse> decode(conduit::io::BitReader& r) {
        auto raw = r.read_signed_bits(24);
        if (!raw) return std::unexpected(raw.error());
        wgs84_coarse result;
        result.raw_ = static_cast<int32_t>(*raw);
        return result;
    }
private:
    int32_t raw_ = 0;
};

// WGS-84 coordinate with 180/2^31 degree resolution
class wgs84_fine {
public:
    static constexpr double SCALE = 8.381903171539307e-08;
    double value() const { return static_cast<double>(raw_) * SCALE; }
    int32_t raw() const { return raw_; }
    void set_value(double v) { raw_ = static_cast<int32_t>(v / SCALE); }
    void set_raw(int32_t v) { raw_ = v; }

    bool operator==(const wgs84_fine&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_signed_bits(raw_, 32);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }
    static conduit::Result<wgs84_fine> decode(conduit::io::BitReader& r) {
        auto raw = r.read_signed_bits(32);
        if (!raw) return std::unexpected(raw.error());
        wgs84_fine result;
        result.raw_ = static_cast<int32_t>(*raw);
        return result;
    }
private:
    int32_t raw_ = 0;
};

// Flight level in 1/4 FL units
class flight_level {
public:
    static constexpr double SCALE = 0.25;
    double value() const { return static_cast<double>(raw_) * SCALE; }
    int16_t raw() const { return raw_; }
    void set_value(double v) { raw_ = static_cast<int16_t>(v / SCALE); }
    void set_raw(int16_t v) { raw_ = v; }

    bool operator==(const flight_level&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_signed_bits(raw_, 16);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }
    static conduit::Result<flight_level> decode(conduit::io::BitReader& r) {
        auto raw = r.read_signed_bits(16);
        if (!raw) return std::unexpected(raw.error());
        flight_level result;
        result.raw_ = static_cast<int16_t>(*raw);
        return result;
    }
private:
    int16_t raw_ = 0;
};

using mode3a_code = uint16_t;
// callsign
class callsign {
public:
    static constexpr size_t WIRE_SIZE = 8;

    std::string value() const { return value_; }
    void set_value(const std::string& v) { value_ = v; }

    bool operator==(const callsign&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        auto wire = conduit::string::from_ascii(value_, conduit::string::Encoding::IA5);
        w.write_string(wire, 8, ' ');
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }
    static conduit::Result<callsign> decode(conduit::io::BitReader& r) {
        auto s = r.read_string(8);
        if (!s) return std::unexpected(s.error());
        callsign result;
        result.value_ = conduit::string::to_ascii(*s, conduit::string::Encoding::IA5);
        // Trim trailing padding
        while (!result.value_.empty() && (result.value_.back() == '\0' || result.value_.back() == ' '))
            result.value_.pop_back();
        return result;
    }
private:
    std::string value_;
};

// ICAO aircraft identification (8 chars × 6 bits = 48 bits = 6 bytes)
class aircraft_ident {
public:
    static constexpr size_t CHAR_COUNT = 8;
    static constexpr int CHAR_BITS = 6;
    static constexpr size_t WIRE_SIZE = 6;

    std::string value() const { return value_; }
    void set_value(const std::string& v) { value_ = v; }

    bool operator==(const aircraft_ident&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        for (size_t i = 0; i < CHAR_COUNT; i++) {
            uint8_t ch = (i < value_.size()) ? static_cast<uint8_t>(value_[i]) : 0x20;
            w.write_bits(ch & ((1 << CHAR_BITS) - 1), CHAR_BITS);
        }
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }
    static conduit::Result<aircraft_ident> decode(conduit::io::BitReader& r) {
        aircraft_ident result;
        result.value_.reserve(CHAR_COUNT);
        for (size_t i = 0; i < CHAR_COUNT; i++) {
            auto bits = r.read_bits(CHAR_BITS);
            if (!bits) return std::unexpected(bits.error());
            uint8_t raw = static_cast<uint8_t>(*bits & ((1 << CHAR_BITS) - 1));
            char ch = (raw == 0) ? '\0' : static_cast<char>(raw < 32 ? raw + 0x40 : raw);
            result.value_ += ch;
        }
        // Trim trailing padding
        while (!result.value_.empty() && (result.value_.back() == '\0' || result.value_.back() == ' '))
            result.value_.pop_back();
        return result;
    }
private:
    std::string value_;
};

using aircraft_address = uint32_t;
using track_number = uint16_t;
enum class cat001_report_type : uint8_t {
    plot = 0,
    track = 1,
};

inline std::string_view to_string(cat001_report_type v) {
    switch (v) {
        case cat001_report_type::plot: return "plot";
        case cat001_report_type::track: return "track";
    }
    return "unknown";
}

inline conduit::Result<cat001_report_type> decode_cat001_report_type(conduit::io::BitReader& r) {
    auto raw = r.read_bits(8);
    if (!raw) return std::unexpected(raw.error());
    auto val = static_cast<cat001_report_type>(*raw);
    switch (val) {
        case cat001_report_type::plot:
        case cat001_report_type::track:
            return val;
    }
    return std::unexpected(conduit::Error(conduit::ErrorCode::UnknownEnumValue,
        "unknown cat001_report_type value: " + std::to_string(static_cast<uint8_t>(val))));
}

inline conduit::VoidResult encode_cat001_report_type(cat001_report_type v, conduit::io::BitWriter& w) {
    switch (v) {
        case cat001_report_type::plot:
        case cat001_report_type::track:
            break;
        default:
            return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                "invalid cat001_report_type enum value: " + std::to_string(static_cast<uint8_t>(v))));
    }
    w.write_bits(static_cast<uint8_t>(v), 8);
    if (w.has_error()) return std::unexpected(w.error());
    return {};
}

enum class cat253_msg_type : uint8_t {
    reserved = 0,
    status = 1,
    data = 2,
    alert = 3,
    config = 4,
    heartbeat = 5,
    request = 6,
    response = 7,
    extended_data = 8,
};

inline std::string_view to_string(cat253_msg_type v) {
    switch (v) {
        case cat253_msg_type::reserved: return "reserved";
        case cat253_msg_type::status: return "status";
        case cat253_msg_type::data: return "data";
        case cat253_msg_type::alert: return "alert";
        case cat253_msg_type::config: return "config";
        case cat253_msg_type::heartbeat: return "heartbeat";
        case cat253_msg_type::request: return "request";
        case cat253_msg_type::response: return "response";
        case cat253_msg_type::extended_data: return "extended-data";
    }
    return "unknown";
}

inline conduit::Result<cat253_msg_type> decode_cat253_msg_type(conduit::io::BitReader& r) {
    auto raw = r.read_bits(6);
    if (!raw) return std::unexpected(raw.error());
    auto val = static_cast<cat253_msg_type>(*raw);
    switch (val) {
        case cat253_msg_type::reserved:
        case cat253_msg_type::status:
        case cat253_msg_type::data:
        case cat253_msg_type::alert:
        case cat253_msg_type::config:
        case cat253_msg_type::heartbeat:
        case cat253_msg_type::request:
        case cat253_msg_type::response:
        case cat253_msg_type::extended_data:
            return val;
    }
    return std::unexpected(conduit::Error(conduit::ErrorCode::UnknownEnumValue,
        "unknown cat253_msg_type value: " + std::to_string(static_cast<uint8_t>(val))));
}

inline conduit::VoidResult encode_cat253_msg_type(cat253_msg_type v, conduit::io::BitWriter& w) {
    switch (v) {
        case cat253_msg_type::reserved:
        case cat253_msg_type::status:
        case cat253_msg_type::data:
        case cat253_msg_type::alert:
        case cat253_msg_type::config:
        case cat253_msg_type::heartbeat:
        case cat253_msg_type::request:
        case cat253_msg_type::response:
        case cat253_msg_type::extended_data:
            break;
        default:
            return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                "invalid cat253_msg_type enum value: " + std::to_string(static_cast<uint8_t>(v))));
    }
    w.write_bits(static_cast<uint8_t>(v), 6);
    if (w.has_error()) return std::unexpected(w.error());
    return {};
}


} // namespace asterix

