// Generated by bgen - DO NOT EDIT
#pragma once

#include "structs.hpp"
#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <any>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string_view>
#include <variant>
#include <vector>

namespace direction_qualified {

class UplinkPayload;
class DownlinkPayload;
class CommonPayload;

class UplinkPayload {
public:
    static constexpr uint64_t TYPE_ID = 0x43eb816dac19f83eULL;
    static constexpr std::string_view TYPE_NAME = "UplinkPayload";
    static constexpr uint8 ID_VALUE = 1;

    uint8 tag() const { return tag_; }
    [[deprecated("auto-managed: value is set automatically during frame encoding")]]
    void set_tag(uint8 v) { tag_ = v; }

    const uint16& tx_data() const { return tx_data_; }
    uint16& mutable_tx_data() { return tx_data_; }
    void set_tx_data(const uint16& v) { tx_data_ = v; }

    static constexpr size_t WIRE_SIZE = 2;

    bool operator==(const UplinkPayload&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u16(static_cast<uint16_t>(tx_data_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<UplinkPayload> decode(conduit::io::BitReader& r) {
        UplinkPayload result;
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'tx-data'"));
            result.tx_data_ = static_cast<uint16>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "UplinkPayload{"
            << "tag=" << +(tag_)
            << ", tx-data=" << +(tx_data_)
            << "}";
        return oss.str();
    }

private:
    friend class Frame;

    uint8 tag_{};
    uint16 tx_data_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<UplinkPayload> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

class DownlinkPayload {
public:
    static constexpr uint64_t TYPE_ID = 0x1b0d6d132ec96e89ULL;
    static constexpr std::string_view TYPE_NAME = "DownlinkPayload";
    static constexpr uint8 ID_VALUE = 1;

    uint8 tag() const { return tag_; }
    [[deprecated("auto-managed: value is set automatically during frame encoding")]]
    void set_tag(uint8 v) { tag_ = v; }

    const uint32& rx_data() const { return rx_data_; }
    uint32& mutable_rx_data() { return rx_data_; }
    void set_rx_data(const uint32& v) { rx_data_ = v; }

    static constexpr size_t WIRE_SIZE = 4;

    bool operator==(const DownlinkPayload&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u32(static_cast<uint32_t>(rx_data_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<DownlinkPayload> decode(conduit::io::BitReader& r) {
        DownlinkPayload result;
        {
            auto val = r.read_u32(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'rx-data'"));
            result.rx_data_ = static_cast<uint32>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "DownlinkPayload{"
            << "tag=" << +(tag_)
            << ", rx-data=" << +(rx_data_)
            << "}";
        return oss.str();
    }

private:
    friend class Frame;

    uint8 tag_{};
    uint32 rx_data_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<DownlinkPayload> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

class CommonPayload {
public:
    static constexpr uint64_t TYPE_ID = 0xd99b6c6485bfb782ULL;
    static constexpr std::string_view TYPE_NAME = "CommonPayload";
    static constexpr uint8 ID_VALUE = 2;

    uint8 tag() const { return tag_; }
    [[deprecated("auto-managed: value is set automatically during frame encoding")]]
    void set_tag(uint8 v) { tag_ = v; }

    const uint8& common_data() const { return common_data_; }
    uint8& mutable_common_data() { return common_data_; }
    void set_common_data(const uint8& v) { common_data_ = v; }

    static constexpr size_t WIRE_SIZE = 1;

    bool operator==(const CommonPayload&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(common_data_));
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<CommonPayload> decode(conduit::io::BitReader& r) {
        CommonPayload result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'common-data'"));
            result.common_data_ = static_cast<uint8>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "CommonPayload{"
            << "tag=" << +(tag_)
            << ", common-data=" << +(common_data_)
            << "}";
        return oss.str();
    }

private:
    friend class Frame;

    uint8 tag_{};
    uint8 common_data_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<CommonPayload> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

class Frame {
public:
    using PayloadVariant = std::variant<UplinkPayload, DownlinkPayload, CommonPayload>;

    uint8 tag() const { return tag_; }
    void set_tag(uint8 v) { tag_ = v; }

    const PayloadVariant& payload() const { return payload_; }
    PayloadVariant& payload() { return payload_; }

    static Frame wrap(const UplinkPayload& msg) {
        Frame frame;
        frame.tag_ = UplinkPayload::ID_VALUE;
        frame.payload_ = msg;
        return frame;
    }

    static Frame wrap(const DownlinkPayload& msg) {
        Frame frame;
        frame.tag_ = DownlinkPayload::ID_VALUE;
        frame.payload_ = msg;
        return frame;
    }

    static Frame wrap(const CommonPayload& msg) {
        Frame frame;
        frame.tag_ = CommonPayload::ID_VALUE;
        frame.payload_ = msg;
        return frame;
    }

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(tag_));
        CONDUIT_TRY(std::visit([&w](const auto& m) -> conduit::VoidResult {
            return m.encode(w);
        }, payload_));
        return {};
    }

    static conduit::Result<Frame> decode(conduit::io::BitReader& r) {
        Frame result;
        auto tag_val_ = r.read_u8();
        if (!tag_val_) return std::unexpected(tag_val_.error());
        result.tag_ = *tag_val_;
        switch (static_cast<uint8>(result.tag_)) {
            case 1: {
                auto payload_val_ = DownlinkPayload::decode(r);
                if (!payload_val_) return std::unexpected(payload_val_.error());
                payload_val_->tag_ = result.tag_;
                result.payload_ = std::move(*payload_val_);
                break;
            }
            case 2: {
                auto payload_val_ = CommonPayload::decode(r);
                if (!payload_val_) return std::unexpected(payload_val_.error());
                payload_val_->tag_ = result.tag_;
                result.payload_ = std::move(*payload_val_);
                break;
            }
            default:
                return std::unexpected(conduit::Error(conduit::ErrorCode::UnknownDiscriminator,
                    "unknown frame message id: " + std::to_string(static_cast<int>(result.tag_))));
        }
        return result;
    }

    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<Frame> decode_bytes(std::span<const uint8_t> data) {
        conduit::io::BitReader r(data);
        return decode(r);
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "Frame{"
            << "tag=" << +(tag_)
            << ", payload=" << std::visit([](const auto& m) { return m.to_string(); }, payload_)
            << "}";
        return oss.str();
    }

private:
    uint8 tag_{};
    PayloadVariant payload_;
};

} // namespace direction_qualified

