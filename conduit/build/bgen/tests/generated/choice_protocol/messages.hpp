// Generated by bgen - DO NOT EDIT
#pragma once

#include "structs.hpp"
#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <any>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string_view>
#include <variant>
#include <vector>

namespace choice_test {

class AlphaBody;
class BetaBody;

class AlphaBody {
public:
    static constexpr uint64_t TYPE_ID = 0x24395aaf5388c853ULL;
    static constexpr std::string_view TYPE_NAME = "AlphaBody";
    static constexpr uint8 ID_VALUE = 1;

    uint16 sync() const { return sync_; }
    void set_sync(uint16 v) { sync_ = v; }
    uint8 message_type() const { return message_type_; }
    [[deprecated("auto-managed: value is set automatically during frame encoding")]]
    void set_message_type(uint8 v) { message_type_ = v; }
    uint16 length() const { return length_; }
    [[deprecated("auto-managed: value is set automatically during frame encoding")]]
    void set_length(uint16 v) { length_ = v; }

    const uint16& x() const { return x_; }
    uint16& mutable_x() { return x_; }
    void set_x(const uint16& v) { x_ = v; }

    const uint16& y() const { return y_; }
    uint16& mutable_y() { return y_; }
    void set_y(const uint16& v) { y_ = v; }

    static constexpr size_t WIRE_SIZE = 4;

    bool operator==(const AlphaBody&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u16(static_cast<uint16_t>(x_), conduit::io::Endian::Big);
        w.write_u16(static_cast<uint16_t>(y_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<AlphaBody> decode(conduit::io::BitReader& r) {
        AlphaBody result;
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'x'"));
            result.x_ = static_cast<uint16>(*val);
        }
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'y'"));
            result.y_ = static_cast<uint16>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "AlphaBody{"
            << "sync=" << +(sync_)
            << ", message-type=" << +(message_type_)
            << ", length=" << +(length_)
            << ", x=" << +(x_)
            << ", y=" << +(y_)
            << "}";
        return oss.str();
    }

private:
    friend class Frame;

    uint16 sync_{};
    uint8 message_type_{};
    uint16 length_{};
    uint16 x_{};
    uint16 y_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<AlphaBody> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

class BetaBody {
public:
    static constexpr uint64_t TYPE_ID = 0x8b968b89bfd16bffULL;
    static constexpr std::string_view TYPE_NAME = "BetaBody";
    static constexpr uint8 ID_VALUE = 2;

    uint16 sync() const { return sync_; }
    void set_sync(uint16 v) { sync_ = v; }
    uint8 message_type() const { return message_type_; }
    [[deprecated("auto-managed: value is set automatically during frame encoding")]]
    void set_message_type(uint8 v) { message_type_ = v; }
    uint16 length() const { return length_; }
    [[deprecated("auto-managed: value is set automatically during frame encoding")]]
    void set_length(uint16 v) { length_ = v; }

    const uint8& payload_size() const { return payload_size_; }
    uint8& mutable_payload_size() { return payload_size_; }
    void set_payload_size(const uint8& v) { payload_size_ = v; }

    const uint32& tag() const { return tag_; }
    uint32& mutable_tag() { return tag_; }
    void set_tag(const uint32& v) { tag_ = v; }

    static constexpr size_t WIRE_SIZE = 5;

    bool operator==(const BetaBody&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(payload_size_));
        w.write_u32(static_cast<uint32_t>(tag_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<BetaBody> decode(conduit::io::BitReader& r) {
        BetaBody result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'payload-size'"));
            result.payload_size_ = static_cast<uint8>(*val);
        }
        {
            auto val = r.read_u32(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'tag'"));
            result.tag_ = static_cast<uint32>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "BetaBody{"
            << "sync=" << +(sync_)
            << ", message-type=" << +(message_type_)
            << ", length=" << +(length_)
            << ", payload-size=" << +(payload_size_)
            << ", tag=" << +(tag_)
            << "}";
        return oss.str();
    }

private:
    friend class Frame;

    uint16 sync_{};
    uint8 message_type_{};
    uint16 length_{};
    uint8 payload_size_{};
    uint32 tag_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<BetaBody> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

class Frame {
public:
    using PayloadVariant = std::variant<AlphaBody, BetaBody>;

    uint16 sync() const { return sync_; }
    void set_sync(uint16 v) { sync_ = v; }
    uint8 message_type() const { return message_type_; }
    void set_message_type(uint8 v) { message_type_ = v; }
    uint16 length() const { return length_; }
    void set_length(uint16 v) { length_ = v; }

    const PayloadVariant& payload() const { return payload_; }
    PayloadVariant& payload() { return payload_; }

    static Frame wrap(const AlphaBody& msg) {
        Frame frame;
        frame.sync_ = static_cast<uint16>(SYNC);
        frame.message_type_ = AlphaBody::ID_VALUE;
        frame.payload_ = msg;
        return frame;
    }

    static Frame wrap(const BetaBody& msg) {
        Frame frame;
        frame.sync_ = static_cast<uint16>(SYNC);
        frame.message_type_ = BetaBody::ID_VALUE;
        frame.payload_ = msg;
        return frame;
    }

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        auto frame_start_pos_ = w.size_bytes();
        w.write_u16(static_cast<uint16_t>(static_cast<uint16_t>(SYNC)), conduit::io::Endian::Big);
        w.write_u8(static_cast<uint8_t>(message_type_));
        auto length_byte_pos_ = w.size_bytes();
        w.write_u16(static_cast<uint16_t>(0), conduit::io::Endian::Big);
        CONDUIT_TRY(std::visit([&w](const auto& m) -> conduit::VoidResult {
            return m.encode(w);
        }, payload_));
        if (!w.patch_u16(length_byte_pos_, static_cast<uint16_t>(w.size_bytes() - frame_start_pos_), conduit::io::Endian::Big))
            return std::unexpected(conduit::Error(conduit::ErrorCode::InvalidArgument, "failed to patch frame length"));
        return {};
    }

    static conduit::Result<Frame> decode(conduit::io::BitReader& r) {
        Frame result;
        auto sync_val_ = r.read_u16(conduit::io::Endian::Big);
        if (!sync_val_) return std::unexpected(sync_val_.error());
        result.sync_ = *sync_val_;
        auto message_type_val_ = r.read_u8();
        if (!message_type_val_) return std::unexpected(message_type_val_.error());
        result.message_type_ = *message_type_val_;
        auto length_val_ = r.read_u16(conduit::io::Endian::Big);
        if (!length_val_) return std::unexpected(length_val_.error());
        result.length_ = *length_val_;
        switch (static_cast<uint8>(result.message_type_)) {
            case 1: {
                auto payload_val_ = AlphaBody::decode(r);
                if (!payload_val_) return std::unexpected(payload_val_.error());
                payload_val_->sync_ = result.sync_;
                payload_val_->message_type_ = result.message_type_;
                payload_val_->length_ = result.length_;
                result.payload_ = std::move(*payload_val_);
                break;
            }
            case 2: {
                auto payload_val_ = BetaBody::decode(r);
                if (!payload_val_) return std::unexpected(payload_val_.error());
                payload_val_->sync_ = result.sync_;
                payload_val_->message_type_ = result.message_type_;
                payload_val_->length_ = result.length_;
                result.payload_ = std::move(*payload_val_);
                break;
            }
            default:
                return std::unexpected(conduit::Error(conduit::ErrorCode::UnknownDiscriminator,
                    "unknown frame message id: " + std::to_string(static_cast<int>(result.message_type_))));
        }
        return result;
    }

    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<Frame> decode_bytes(std::span<const uint8_t> data) {
        conduit::io::BitReader r(data);
        return decode(r);
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "Frame{"
            << "sync=" << +(sync_)
            << ", message-type=" << +(message_type_)
            << ", length=" << +(length_)
            << ", payload=" << std::visit([](const auto& m) { return m.to_string(); }, payload_)
            << "}";
        return oss.str();
    }

private:
    uint16 sync_{};
    uint8 message_type_{};
    uint16 length_{};
    PayloadVariant payload_;
};

} // namespace choice_test

