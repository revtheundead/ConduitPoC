// Generated by bgen - DO NOT EDIT
#pragma once

#include "structs.hpp"
#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <any>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string_view>
#include <variant>
#include <vector>

namespace constraint_tighten {

class TightenedMsg;

class TightenedMsg {
public:
    static constexpr uint64_t TYPE_ID = 0x45986ac83a3ab88eULL;
    static constexpr std::string_view TYPE_NAME = "TightenedMsg";

    const uint16& narrow() const { return narrow_; }
    uint16& mutable_narrow() { return narrow_; }
    [[nodiscard]] conduit::VoidResult set_narrow(const uint16& v) {
        if (v > 200)
            return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                "narrow exceeds max 200"));
        if (v < 10)
            return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                "narrow below min 10"));
        narrow_ = v;
        return {};
    }

    const uint16& wide() const { return wide_; }
    uint16& mutable_wide() { return wide_; }
    [[nodiscard]] conduit::VoidResult set_wide(const uint16& v) {
        if (v > 1000)
            return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                "wide exceeds max 1000"));
        wide_ = v;
        return {};
    }

    const uint8& tag() const { return tag_; }
    uint8& mutable_tag() { return tag_; }
    void set_tag(const uint8& v) { tag_ = v; }

    static constexpr size_t WIRE_SIZE = 5;

    bool operator==(const TightenedMsg&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        if (narrow_ > 200)
            return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                "narrow exceeds max 200"));
        if (narrow_ < 10)
            return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                "narrow below min 10"));
        w.write_u16(static_cast<uint16_t>(narrow_), conduit::io::Endian::Big);
        if (wide_ > 1000)
            return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                "wide exceeds max 1000"));
        w.write_u16(static_cast<uint16_t>(wide_), conduit::io::Endian::Big);
        w.write_u8(static_cast<uint8_t>(tag_));
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<TightenedMsg> decode(conduit::io::BitReader& r) {
        TightenedMsg result;
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'narrow'"));
            result.narrow_ = static_cast<uint16>(*val);
            if (result.narrow_ > 200) {
                return std::unexpected(conduit::Error(conduit::ErrorCode::ConstraintViolation,
                    "narrow exceeds max 200"));
            }
            if (result.narrow_ < 10) {
                return std::unexpected(conduit::Error(conduit::ErrorCode::ConstraintViolation,
                    "narrow below min 10"));
            }
        }
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'wide'"));
            result.wide_ = static_cast<uint16>(*val);
            if (result.wide_ > 1000) {
                return std::unexpected(conduit::Error(conduit::ErrorCode::ConstraintViolation,
                    "wide exceeds max 1000"));
            }
        }
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'tag'"));
            result.tag_ = static_cast<uint8>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "TightenedMsg{"
            << "narrow=" << +(narrow_)
            << ", wide=" << +(wide_)
            << ", tag=" << +(tag_)
            << "}";
        return oss.str();
    }

private:
    uint16 narrow_{};
    uint16 wide_{};
    uint8 tag_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<TightenedMsg> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

} // namespace constraint_tighten

