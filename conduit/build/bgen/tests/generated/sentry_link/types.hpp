// Generated by bgen - DO NOT EDIT
#pragma once

#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <conduit/io/endian.hpp>
#include <cstdint>
#include <string>
#include <string_view>

namespace sentry_link {

using uint8 = uint8_t;
using uint16 = uint16_t;
using uint32 = uint32_t;
using int32 = int32_t;
enum class device_status : uint8_t {
    online = 0,
    standby = 1,
    maintenance = 2,
    error = 3,
};

inline std::string_view to_string(device_status v) {
    switch (v) {
        case device_status::online: return "online";
        case device_status::standby: return "standby";
        case device_status::maintenance: return "maintenance";
        case device_status::error: return "error";
    }
    return "unknown";
}

inline conduit::Result<device_status> decode_device_status(conduit::io::BitReader& r) {
    auto raw = r.read_bits(8);
    if (!raw) return std::unexpected(raw.error());
    auto val = static_cast<device_status>(*raw);
    switch (val) {
        case device_status::online:
        case device_status::standby:
        case device_status::maintenance:
        case device_status::error:
            return val;
    }
    return std::unexpected(conduit::Error(conduit::ErrorCode::UnknownEnumValue,
        "unknown device_status value: " + std::to_string(static_cast<uint8_t>(val))));
}

inline conduit::VoidResult encode_device_status(device_status v, conduit::io::BitWriter& w) {
    switch (v) {
        case device_status::online:
        case device_status::standby:
        case device_status::maintenance:
        case device_status::error:
            break;
        default:
            return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                "invalid device_status enum value: " + std::to_string(static_cast<uint8_t>(v))));
    }
    w.write_bits(static_cast<uint8_t>(v), 8);
    if (w.has_error()) return std::unexpected(w.error());
    return {};
}

enum class device_mode : uint8_t {
    active = 0,
    passive = 1,
    calibration = 2,
    test = 3,
};

inline std::string_view to_string(device_mode v) {
    switch (v) {
        case device_mode::active: return "active";
        case device_mode::passive: return "passive";
        case device_mode::calibration: return "calibration";
        case device_mode::test: return "test";
    }
    return "unknown";
}

inline conduit::Result<device_mode> decode_device_mode(conduit::io::BitReader& r) {
    auto raw = r.read_bits(2);
    if (!raw) return std::unexpected(raw.error());
    auto val = static_cast<device_mode>(*raw);
    switch (val) {
        case device_mode::active:
        case device_mode::passive:
        case device_mode::calibration:
        case device_mode::test:
            return val;
    }
    return std::unexpected(conduit::Error(conduit::ErrorCode::UnknownEnumValue,
        "unknown device_mode value: " + std::to_string(static_cast<uint8_t>(val))));
}

inline conduit::VoidResult encode_device_mode(device_mode v, conduit::io::BitWriter& w) {
    switch (v) {
        case device_mode::active:
        case device_mode::passive:
        case device_mode::calibration:
        case device_mode::test:
            break;
        default:
            return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                "invalid device_mode enum value: " + std::to_string(static_cast<uint8_t>(v))));
    }
    w.write_bits(static_cast<uint8_t>(v), 2);
    if (w.has_error()) return std::unexpected(w.error());
    return {};
}

enum class severity_level : uint8_t {
    info = 0,
    notice = 1,
    warning = 2,
    error = 3,
    critical = 4,
};

inline std::string_view to_string(severity_level v) {
    switch (v) {
        case severity_level::info: return "info";
        case severity_level::notice: return "notice";
        case severity_level::warning: return "warning";
        case severity_level::error: return "error";
        case severity_level::critical: return "critical";
    }
    return "unknown";
}

inline conduit::Result<severity_level> decode_severity_level(conduit::io::BitReader& r) {
    auto raw = r.read_bits(3);
    if (!raw) return std::unexpected(raw.error());
    auto val = static_cast<severity_level>(*raw);
    switch (val) {
        case severity_level::info:
        case severity_level::notice:
        case severity_level::warning:
        case severity_level::error:
        case severity_level::critical:
            return val;
    }
    return std::unexpected(conduit::Error(conduit::ErrorCode::UnknownEnumValue,
        "unknown severity_level value: " + std::to_string(static_cast<uint8_t>(val))));
}

inline conduit::VoidResult encode_severity_level(severity_level v, conduit::io::BitWriter& w) {
    switch (v) {
        case severity_level::info:
        case severity_level::notice:
        case severity_level::warning:
        case severity_level::error:
        case severity_level::critical:
            break;
        default:
            return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                "invalid severity_level enum value: " + std::to_string(static_cast<uint8_t>(v))));
    }
    w.write_bits(static_cast<uint8_t>(v), 3);
    if (w.has_error()) return std::unexpected(w.error());
    return {};
}


} // namespace sentry_link

