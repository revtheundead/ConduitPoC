// Generated by bgen - DO NOT EDIT
#pragma once

#include "structs.hpp"
#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <any>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string_view>
#include <variant>
#include <vector>

namespace inline_case_collision {

class MsgAlpha;
class MsgBeta;

class MsgAlpha_TypeA {
public:
    const uint32& alpha_val() const { return alpha_val_; }
    uint32& mutable_alpha_val() { return alpha_val_; }
    void set_alpha_val(const uint32& v) { alpha_val_ = v; }

    bool operator==(const MsgAlpha_TypeA&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u32(static_cast<uint32_t>(alpha_val_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<MsgAlpha_TypeA> decode(conduit::io::BitReader& r) {
        MsgAlpha_TypeA result;
        {
            auto val = r.read_u32(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'alpha-val'"));
            result.alpha_val_ = static_cast<uint32>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "MsgAlpha_TypeA{"
            << "alpha-val=" << +(alpha_val_)
            << "}";
        return oss.str();
    }

private:
    uint32 alpha_val_{};
};

class MsgAlpha_TypeB {
public:
    const uint8& alpha_flag() const { return alpha_flag_; }
    uint8& mutable_alpha_flag() { return alpha_flag_; }
    void set_alpha_flag(const uint8& v) { alpha_flag_ = v; }

    bool operator==(const MsgAlpha_TypeB&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(alpha_flag_));
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<MsgAlpha_TypeB> decode(conduit::io::BitReader& r) {
        MsgAlpha_TypeB result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'alpha-flag'"));
            result.alpha_flag_ = static_cast<uint8>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "MsgAlpha_TypeB{"
            << "alpha-flag=" << +(alpha_flag_)
            << "}";
        return oss.str();
    }

private:
    uint8 alpha_flag_{};
};

using payloadVariant = std::variant<MsgAlpha_TypeA, MsgAlpha_TypeB>;
class MsgAlpha {
public:
    static constexpr uint64_t TYPE_ID = 0x0183c103e94bcbfaULL;
    static constexpr std::string_view TYPE_NAME = "MsgAlpha";

    const uint8& tag() const { return tag_; }
    uint8& mutable_tag() { return tag_; }
    void set_tag(const uint8& v) { tag_ = v; }

    const payloadVariant& payload() const { return payload_; }
    payloadVariant& mutable_payload() { return payload_; }
    void set_payload(const payloadVariant& v) { payload_ = v; }

    bool operator==(const MsgAlpha&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(tag_));
        {
            auto _sw = tag_;
            auto _idx = (payload_).index();
            if (_idx == 0) {
                if (_sw != static_cast<decltype(_sw)>(1))
                    return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                        "choice 'payload': variant does not match discriminator"));
            } else if (_idx == 1) {
                if (_sw != static_cast<decltype(_sw)>(2))
                    return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                        "choice 'payload': variant does not match discriminator"));
            }
        }
        {
            auto _enc_r = std::visit([&w](const auto& v) -> conduit::VoidResult { return v.encode(w); }, payload_);
            if (!_enc_r) return std::unexpected(_enc_r.error());
        }
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<MsgAlpha> decode(conduit::io::BitReader& r) {
        MsgAlpha result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'tag'"));
            result.tag_ = static_cast<uint8>(*val);
        }
        {
            auto switch_val = result.tag_;
            if (switch_val == static_cast<decltype(switch_val)>(1)) {
                auto val = MsgAlpha_TypeA::decode(r);
                if (!val) return std::unexpected(val.error().with_context("choice 'payload'"));
                result.payload_ = std::move(*val);
            } else if (switch_val == static_cast<decltype(switch_val)>(2)) {
                auto val = MsgAlpha_TypeB::decode(r);
                if (!val) return std::unexpected(val.error().with_context("choice 'payload'"));
                result.payload_ = std::move(*val);
            } else {
                return std::unexpected(conduit::Error(conduit::ErrorCode::UnknownDiscriminator,
                    "choice 'payload': no case matched switch value"));
            }
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "MsgAlpha{"
            << "tag=" << +(tag_)
            << ", payload=" << std::visit([](const auto& v) { return v.to_string(); }, payload_)
            << "}";
        return oss.str();
    }

private:
    uint8 tag_{};
    payloadVariant payload_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<MsgAlpha> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

class MsgBeta_TypeA {
public:
    const uint16& beta_x() const { return beta_x_; }
    uint16& mutable_beta_x() { return beta_x_; }
    void set_beta_x(const uint16& v) { beta_x_ = v; }

    const uint16& beta_y() const { return beta_y_; }
    uint16& mutable_beta_y() { return beta_y_; }
    void set_beta_y(const uint16& v) { beta_y_ = v; }

    bool operator==(const MsgBeta_TypeA&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u16(static_cast<uint16_t>(beta_x_), conduit::io::Endian::Big);
        w.write_u16(static_cast<uint16_t>(beta_y_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<MsgBeta_TypeA> decode(conduit::io::BitReader& r) {
        MsgBeta_TypeA result;
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'beta-x'"));
            result.beta_x_ = static_cast<uint16>(*val);
        }
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'beta-y'"));
            result.beta_y_ = static_cast<uint16>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "MsgBeta_TypeA{"
            << "beta-x=" << +(beta_x_)
            << ", beta-y=" << +(beta_y_)
            << "}";
        return oss.str();
    }

private:
    uint16 beta_x_{};
    uint16 beta_y_{};
};

class MsgBeta_TypeB {
public:
    const uint16& beta_code() const { return beta_code_; }
    uint16& mutable_beta_code() { return beta_code_; }
    void set_beta_code(const uint16& v) { beta_code_ = v; }

    bool operator==(const MsgBeta_TypeB&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u16(static_cast<uint16_t>(beta_code_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<MsgBeta_TypeB> decode(conduit::io::BitReader& r) {
        MsgBeta_TypeB result;
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'beta-code'"));
            result.beta_code_ = static_cast<uint16>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "MsgBeta_TypeB{"
            << "beta-code=" << +(beta_code_)
            << "}";
        return oss.str();
    }

private:
    uint16 beta_code_{};
};

using MsgBeta_payloadVariant = std::variant<MsgBeta_TypeA, MsgBeta_TypeB>;
class MsgBeta {
public:
    static constexpr uint64_t TYPE_ID = 0xa4b6f2cd450da0f0ULL;
    static constexpr std::string_view TYPE_NAME = "MsgBeta";

    const uint8& tag() const { return tag_; }
    uint8& mutable_tag() { return tag_; }
    void set_tag(const uint8& v) { tag_ = v; }

    const MsgBeta_payloadVariant& payload() const { return payload_; }
    MsgBeta_payloadVariant& mutable_payload() { return payload_; }
    void set_payload(const MsgBeta_payloadVariant& v) { payload_ = v; }

    bool operator==(const MsgBeta&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(tag_));
        {
            auto _sw = tag_;
            auto _idx = (payload_).index();
            if (_idx == 0) {
                if (_sw != static_cast<decltype(_sw)>(1))
                    return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                        "choice 'payload': variant does not match discriminator"));
            } else if (_idx == 1) {
                if (_sw != static_cast<decltype(_sw)>(2))
                    return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                        "choice 'payload': variant does not match discriminator"));
            }
        }
        {
            auto _enc_r = std::visit([&w](const auto& v) -> conduit::VoidResult { return v.encode(w); }, payload_);
            if (!_enc_r) return std::unexpected(_enc_r.error());
        }
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<MsgBeta> decode(conduit::io::BitReader& r) {
        MsgBeta result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'tag'"));
            result.tag_ = static_cast<uint8>(*val);
        }
        {
            auto switch_val = result.tag_;
            if (switch_val == static_cast<decltype(switch_val)>(1)) {
                auto val = MsgBeta_TypeA::decode(r);
                if (!val) return std::unexpected(val.error().with_context("choice 'payload'"));
                result.payload_ = std::move(*val);
            } else if (switch_val == static_cast<decltype(switch_val)>(2)) {
                auto val = MsgBeta_TypeB::decode(r);
                if (!val) return std::unexpected(val.error().with_context("choice 'payload'"));
                result.payload_ = std::move(*val);
            } else {
                return std::unexpected(conduit::Error(conduit::ErrorCode::UnknownDiscriminator,
                    "choice 'payload': no case matched switch value"));
            }
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "MsgBeta{"
            << "tag=" << +(tag_)
            << ", payload=" << std::visit([](const auto& v) { return v.to_string(); }, payload_)
            << "}";
        return oss.str();
    }

private:
    uint8 tag_{};
    MsgBeta_payloadVariant payload_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<MsgBeta> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

} // namespace inline_case_collision

