// Generated by bgen - DO NOT EDIT
#pragma once

#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string>
#include <variant>
#include <vector>

namespace arrays_choices {

class Point;
class SubX;
class SubY;
class TypeABody;
class TypeBBody;
class FallbackBody;

class Point {
public:
    const uint16& x() const { return x_; }
    uint16& mutable_x() { return x_; }
    void set_x(const uint16& v) { x_ = v; }

    const uint16& y() const { return y_; }
    uint16& mutable_y() { return y_; }
    void set_y(const uint16& v) { y_ = v; }

    static constexpr size_t WIRE_SIZE = 4;

    bool operator==(const Point&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u16(static_cast<uint16_t>(x_), conduit::io::Endian::Big);
        w.write_u16(static_cast<uint16_t>(y_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<Point> decode(conduit::io::BitReader& r) {
        Point result;
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'x'"));
            result.x_ = static_cast<uint16>(*val);
        }
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'y'"));
            result.y_ = static_cast<uint16>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "Point{"
            << "x=" << +(x_)
            << ", y=" << +(y_)
            << "}";
        return oss.str();
    }

private:
    uint16 x_{};
    uint16 y_{};
};

class SubX {
public:
    const uint32& val() const { return val_; }
    uint32& mutable_val() { return val_; }
    void set_val(const uint32& v) { val_ = v; }

    static constexpr size_t WIRE_SIZE = 4;

    bool operator==(const SubX&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u32(static_cast<uint32_t>(val_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<SubX> decode(conduit::io::BitReader& r) {
        SubX result;
        {
            auto val = r.read_u32(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'val'"));
            result.val_ = static_cast<uint32>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "SubX{"
            << "val=" << +(val_)
            << "}";
        return oss.str();
    }

private:
    uint32 val_{};
};

class SubY {
public:
    const uint16& a() const { return a_; }
    uint16& mutable_a() { return a_; }
    void set_a(const uint16& v) { a_ = v; }

    const uint16& b() const { return b_; }
    uint16& mutable_b() { return b_; }
    void set_b(const uint16& v) { b_ = v; }

    static constexpr size_t WIRE_SIZE = 4;

    bool operator==(const SubY&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u16(static_cast<uint16_t>(a_), conduit::io::Endian::Big);
        w.write_u16(static_cast<uint16_t>(b_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<SubY> decode(conduit::io::BitReader& r) {
        SubY result;
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'a'"));
            result.a_ = static_cast<uint16>(*val);
        }
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'b'"));
            result.b_ = static_cast<uint16>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "SubY{"
            << "a=" << +(a_)
            << ", b=" << +(b_)
            << "}";
        return oss.str();
    }

private:
    uint16 a_{};
    uint16 b_{};
};

using sub_bodyVariant = std::variant<SubX, SubY>;
class TypeABody {
public:
    const uint8& sub_type() const { return sub_type_; }
    uint8& mutable_sub_type() { return sub_type_; }
    void set_sub_type(const uint8& v) { sub_type_ = v; }

    const sub_bodyVariant& sub_body() const { return sub_body_; }
    sub_bodyVariant& mutable_sub_body() { return sub_body_; }
    void set_sub_body(const sub_bodyVariant& v) { sub_body_ = v; }

    bool operator==(const TypeABody&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(sub_type_));
        {
            auto _sw = sub_type_;
            auto _idx = (sub_body_).index();
            if (_idx == 0) {
                if (_sw != static_cast<decltype(_sw)>(SUB_X))
                    return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                        "choice 'sub-body': variant does not match discriminator"));
            } else if (_idx == 1) {
                if (_sw != static_cast<decltype(_sw)>(SUB_Y))
                    return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                        "choice 'sub-body': variant does not match discriminator"));
            }
        }
        {
            auto _enc_r = std::visit([&w](const auto& v) -> conduit::VoidResult { return v.encode(w); }, sub_body_);
            if (!_enc_r) return std::unexpected(_enc_r.error());
        }
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<TypeABody> decode(conduit::io::BitReader& r) {
        TypeABody result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'sub-type'"));
            result.sub_type_ = static_cast<uint8>(*val);
        }
        {
            auto switch_val = result.sub_type_;
            if (switch_val == static_cast<decltype(switch_val)>(SUB_X)) {
                auto val = SubX::decode(r);
                if (!val) return std::unexpected(val.error().with_context("choice 'sub-body'"));
                result.sub_body_ = std::move(*val);
            } else if (switch_val == static_cast<decltype(switch_val)>(SUB_Y)) {
                auto val = SubY::decode(r);
                if (!val) return std::unexpected(val.error().with_context("choice 'sub-body'"));
                result.sub_body_ = std::move(*val);
            } else {
                return std::unexpected(conduit::Error(conduit::ErrorCode::UnknownDiscriminator,
                    "choice 'sub-body': no case matched switch value"));
            }
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "TypeABody{"
            << "sub-type=" << +(sub_type_)
            << ", sub-body=" << std::visit([](const auto& v) { return v.to_string(); }, sub_body_)
            << "}";
        return oss.str();
    }

private:
    uint8 sub_type_{};
    sub_bodyVariant sub_body_{};
};

class TypeBBody {
public:
    const uint32& tag() const { return tag_; }
    uint32& mutable_tag() { return tag_; }
    void set_tag(const uint32& v) { tag_ = v; }

    static constexpr size_t WIRE_SIZE = 4;

    bool operator==(const TypeBBody&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u32(static_cast<uint32_t>(tag_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<TypeBBody> decode(conduit::io::BitReader& r) {
        TypeBBody result;
        {
            auto val = r.read_u32(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'tag'"));
            result.tag_ = static_cast<uint32>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "TypeBBody{"
            << "tag=" << +(tag_)
            << "}";
        return oss.str();
    }

private:
    uint32 tag_{};
};

class FallbackBody {
public:
    const uint32& raw() const { return raw_; }
    uint32& mutable_raw() { return raw_; }
    void set_raw(const uint32& v) { raw_ = v; }

    static constexpr size_t WIRE_SIZE = 4;

    bool operator==(const FallbackBody&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u32(static_cast<uint32_t>(raw_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<FallbackBody> decode(conduit::io::BitReader& r) {
        FallbackBody result;
        {
            auto val = r.read_u32(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'raw'"));
            result.raw_ = static_cast<uint32>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "FallbackBody{"
            << "raw=" << +(raw_)
            << "}";
        return oss.str();
    }

private:
    uint32 raw_{};
};

} // namespace arrays_choices

