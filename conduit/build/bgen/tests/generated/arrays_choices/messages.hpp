// Generated by bgen - DO NOT EDIT
#pragma once

#include "structs.hpp"
#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <any>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string_view>
#include <variant>
#include <vector>

namespace arrays_choices {

class FixedArrayMsg;
class CountFromArrayMsg;
class ChoiceMsg;

class FixedArrayMsg {
public:
    static constexpr uint64_t TYPE_ID = 0xfc9d796cd8ac6997ULL;
    static constexpr std::string_view TYPE_NAME = "FixedArrayMsg";

    const std::vector<Point>& points() const { return points_; }
    std::vector<Point>& mutable_points() { return points_; }
    void set_points(const std::vector<Point>& v) { points_ = v; }

    static constexpr size_t WIRE_SIZE = 12;

    bool operator==(const FixedArrayMsg&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        for (const auto& elem : points_) {
            CONDUIT_TRY(elem.encode(w));
        }
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<FixedArrayMsg> decode(conduit::io::BitReader& r) {
        FixedArrayMsg result;
        result.points_.reserve(3);
        for (int i = 0; i < 3; i++) {
            auto elem = Point::decode(r);
            if (!elem) return std::unexpected(elem.error().with_context("array 'points'"));
            result.points_.push_back(std::move(*elem));
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "FixedArrayMsg{"
            << "points=["
            ;
            for (size_t i = 0; i < points_.size(); ++i) {
                if (i > 0) oss << ", ";
                oss << points_[i].to_string();
            }
            oss << "]"
            << "}";
        return oss.str();
    }

private:
    std::vector<Point> points_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<FixedArrayMsg> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

class CountFromArrayMsg {
public:
    static constexpr uint64_t TYPE_ID = 0x5cef21c4d3debaa6ULL;
    static constexpr std::string_view TYPE_NAME = "CountFromArrayMsg";

    const uint8& num_items() const { return num_items_; }
    uint8& mutable_num_items() { return num_items_; }
    void set_num_items(const uint8& v) { num_items_ = v; }

    const std::vector<Point>& items() const { return items_; }
    std::vector<Point>& mutable_items() { return items_; }
    void set_items(const std::vector<Point>& v) { items_ = v; }

    bool operator==(const CountFromArrayMsg&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(num_items_));
        for (const auto& elem : items_) {
            CONDUIT_TRY(elem.encode(w));
        }
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<CountFromArrayMsg> decode(conduit::io::BitReader& r) {
        CountFromArrayMsg result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'num-items'"));
            result.num_items_ = static_cast<uint8>(*val);
        }
        {
            auto count = static_cast<size_t>(result.num_items_);
            if (count > static_cast<size_t>(INT32_MAX)) return std::unexpected(conduit::Error(conduit::ErrorCode::InvalidArgument, "invalid array count").with_context("array 'items'"));
            result.items_.reserve(count);
            for (size_t i = 0; i < count; i++) {
                auto elem = Point::decode(r);
                if (!elem) return std::unexpected(elem.error().with_context("array 'items'"));
                result.items_.push_back(std::move(*elem));
            }
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "CountFromArrayMsg{"
            << "num-items=" << +(num_items_)
            << ", items=["
            ;
            for (size_t i = 0; i < items_.size(); ++i) {
                if (i > 0) oss << ", ";
                oss << items_[i].to_string();
            }
            oss << "]"
            << "}";
        return oss.str();
    }

private:
    uint8 num_items_{};
    std::vector<Point> items_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<CountFromArrayMsg> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

using bodyVariant = std::variant<TypeABody, TypeBBody, FallbackBody>;
class ChoiceMsg {
public:
    static constexpr uint64_t TYPE_ID = 0x6fcac4a79a52e083ULL;
    static constexpr std::string_view TYPE_NAME = "ChoiceMsg";

    const uint8& msg_type() const { return msg_type_; }
    uint8& mutable_msg_type() { return msg_type_; }
    void set_msg_type(const uint8& v) { msg_type_ = v; }

    const uint16& length() const { return length_; }
    uint16& mutable_length() { return length_; }
    void set_length(const uint16& v) { length_ = v; }

    const bodyVariant& body() const { return body_; }
    bodyVariant& mutable_body() { return body_; }
    void set_body(const bodyVariant& v) { body_ = v; }

    bool operator==(const ChoiceMsg&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(msg_type_));
        w.write_u16(static_cast<uint16_t>(length_), conduit::io::Endian::Big);
        {
            auto _sw = msg_type_;
            auto _idx = (body_).index();
            if (_idx == 0) {
                if (_sw != static_cast<decltype(_sw)>(TYPE_A))
                    return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                        "choice 'body': variant does not match discriminator"));
            } else if (_idx == 1) {
                if (_sw != static_cast<decltype(_sw)>(TYPE_B))
                    return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                        "choice 'body': variant does not match discriminator"));
            } else if (_idx == 2) {
                if (_sw == static_cast<decltype(_sw)>(TYPE_A) || _sw == static_cast<decltype(_sw)>(TYPE_B))
                    return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                        "choice 'body': variant does not match discriminator"));
            }
        }
        {
            auto _enc_r = std::visit([&w](const auto& v) -> conduit::VoidResult { return v.encode(w); }, body_);
            if (!_enc_r) return std::unexpected(_enc_r.error());
        }
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<ChoiceMsg> decode(conduit::io::BitReader& r) {
        ChoiceMsg result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'msg-type'"));
            result.msg_type_ = static_cast<uint8>(*val);
        }
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'length'"));
            result.length_ = static_cast<uint16>(*val);
        }
        {
            auto sub_len = static_cast<size_t>(result.length_);
            auto sub = r.sub_reader(sub_len);
            if (!sub) return std::unexpected(sub.error().with_context("choice 'body'"));
            auto& cr = *sub;
            {
                auto switch_val = result.msg_type_;
                if (switch_val == static_cast<decltype(switch_val)>(TYPE_A)) {
                    auto val = TypeABody::decode(cr);
                    if (!val) return std::unexpected(val.error().with_context("choice 'body'"));
                    result.body_ = std::move(*val);
                } else if (switch_val == static_cast<decltype(switch_val)>(TYPE_B)) {
                    auto val = TypeBBody::decode(cr);
                    if (!val) return std::unexpected(val.error().with_context("choice 'body'"));
                    result.body_ = std::move(*val);
                } else {
                    auto val = FallbackBody::decode(cr);
                    if (!val) return std::unexpected(val.error().with_context("choice 'body'"));
                    result.body_ = std::move(*val);
                }
            }
            if (!cr.at_end()) {
                return std::unexpected(conduit::Error(conduit::ErrorCode::ExactConsumptionFailed,
                    "bounded choice has unconsumed bytes"));
            }
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "ChoiceMsg{"
            << "msg-type=" << +(msg_type_)
            << ", length=" << +(length_)
            << ", body=" << std::visit([](const auto& v) { return v.to_string(); }, body_)
            << "}";
        return oss.str();
    }

private:
    uint8 msg_type_{};
    uint16 length_{};
    bodyVariant body_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<ChoiceMsg> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

} // namespace arrays_choices

