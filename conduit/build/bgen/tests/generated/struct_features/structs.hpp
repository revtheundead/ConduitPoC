// Generated by bgen - DO NOT EDIT
#pragma once

#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string>
#include <variant>
#include <vector>

namespace struct_features {

class GpsCoord;

class GpsCoord {
public:
    const uint32& latitude() const { return latitude_; }
    uint32& mutable_latitude() { return latitude_; }
    void set_latitude(const uint32& v) { latitude_ = v; }

    const uint32& longitude() const { return longitude_; }
    uint32& mutable_longitude() { return longitude_; }
    void set_longitude(const uint32& v) { longitude_ = v; }

    static constexpr size_t WIRE_SIZE = 8;

    bool operator==(const GpsCoord&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u32(static_cast<uint32_t>(latitude_), conduit::io::Endian::Big);
        w.write_u32(static_cast<uint32_t>(longitude_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<GpsCoord> decode(conduit::io::BitReader& r) {
        GpsCoord result;
        {
            auto val = r.read_u32(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'latitude'"));
            result.latitude_ = static_cast<uint32>(*val);
        }
        {
            auto val = r.read_u32(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'longitude'"));
            result.longitude_ = static_cast<uint32>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "GpsCoord{"
            << "latitude=" << +(latitude_)
            << ", longitude=" << +(longitude_)
            << "}";
        return oss.str();
    }

private:
    uint32 latitude_{};
    uint32 longitude_{};
};

} // namespace struct_features

