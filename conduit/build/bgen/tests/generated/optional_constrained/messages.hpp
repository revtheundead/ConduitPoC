// Generated by bgen - DO NOT EDIT
#pragma once

#include "structs.hpp"
#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <any>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string_view>
#include <variant>
#include <vector>

namespace optional_constrained {

class OptConstMsg;

class OptConstMsg {
public:
    static constexpr uint64_t TYPE_ID = 0x2fbbe0cda71c4da6ULL;
    static constexpr std::string_view TYPE_NAME = "OptConstMsg";

    const uint8& flags() const { return flags_; }
    uint8& mutable_flags() { return flags_; }
    void set_flags(const uint8& v) { flags_ = v; }

    bool has_quality() const { return quality_.has_value(); }
    const uint8& quality() const { return quality_.value(); }
    uint8& mutable_quality() { if (!quality_) quality_.emplace(); return *quality_; }
    [[nodiscard]] conduit::VoidResult set_quality(const uint8& v) {
        if (v > 100)
            return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                "quality exceeds max 100"));
        quality_ = v;
        return {};
    }
    void clear_quality() { quality_.reset(); }

    bool has_priority() const { return priority_.has_value(); }
    const uint8& priority() const { return priority_.value(); }
    uint8& mutable_priority() { if (!priority_) priority_.emplace(); return *priority_; }
    void set_priority(const uint8& v) { priority_ = v; }
    void clear_priority() { priority_.reset(); }

    const uint16& data() const { return data_; }
    uint16& mutable_data() { return data_; }
    void set_data(const uint16& v) { data_ = v; }

    bool operator==(const OptConstMsg&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(flags_));
        if (quality_.has_value()) {
            if (*quality_ > 100)
                return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                    "quality exceeds max 100"));
            w.write_u8(static_cast<uint8_t>(*quality_));
        }
        if (priority_.has_value()) {
            if (*priority_ > 10)
                return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                    "priority exceeds max 10"));
            if (*priority_ < 1)
                return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                    "priority below min 1"));
            w.write_u8(static_cast<uint8_t>(*priority_));
        }
        w.write_u16(static_cast<uint16_t>(data_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<OptConstMsg> decode(conduit::io::BitReader& r) {
        OptConstMsg result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'flags'"));
            result.flags_ = static_cast<uint8>(*val);
        }
        if ((result.flags_ & 1)) {
            {
                auto val = r.read_u8();
                if (!val) return std::unexpected(val.error().with_context("field 'quality'"));
                result.quality_ = static_cast<uint8>(*val);
                if (result.quality_ > 100) {
                    return std::unexpected(conduit::Error(conduit::ErrorCode::ConstraintViolation,
                        "quality exceeds max 100"));
                }
            }
        }
        if ((result.flags_ & 2)) {
            {
                auto val = r.read_u8();
                if (!val) return std::unexpected(val.error().with_context("field 'priority'"));
                result.priority_ = static_cast<uint8>(*val);
            }
        }
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'data'"));
            result.data_ = static_cast<uint16>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "OptConstMsg{"
            << "flags=" << +(flags_)
            << ", quality=" << (quality_.has_value() ? std::to_string(*quality_) : std::string("<none>"))
            << ", priority=" << (priority_.has_value() ? std::to_string(*priority_) : std::string("<none>"))
            << ", data=" << +(data_)
            << "}";
        return oss.str();
    }

    conduit::VoidResult validate() const {
        if (priority_.has_value()) {
            if (*priority_ > 10) {
                return std::unexpected(conduit::Error(conduit::ErrorCode::ConstraintViolationDeferred,
                    "priority exceeds max 10"));
            }
            if (*priority_ < 1) {
                return std::unexpected(conduit::Error(conduit::ErrorCode::ConstraintViolationDeferred,
                    "priority below min 1"));
            }
        }
        return {};
    }

private:
    uint8 flags_{};
    std::optional<uint8> quality_;
    std::optional<uint8> priority_;
    uint16 data_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<OptConstMsg> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

} // namespace optional_constrained

