// Generated by bgen - DO NOT EDIT
#pragma once

#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <conduit/io/endian.hpp>
#include <cstdint>
#include <string>
#include <string_view>

namespace all_types {

using uint8 = uint8_t;
using uint16 = uint16_t;
using uint32 = uint32_t;
using uint64 = uint64_t;
using int8 = int8_t;
using int16 = int16_t;
using int32 = int32_t;
using float32 = float;
using float64 = double;
using bool8 = bool;
// ascii_str
class ascii_str {
public:
    static constexpr size_t WIRE_SIZE = 10;

    std::string value() const { return value_; }
    void set_value(const std::string& v) { value_ = v; }

    bool operator==(const ascii_str&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_string(value_, 10, '\0');
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }
    static conduit::Result<ascii_str> decode(conduit::io::BitReader& r) {
        auto s = r.read_string(10);
        if (!s) return std::unexpected(s.error());
        ascii_str result;
        result.value_ = std::move(*s);
        // Trim trailing padding
        while (!result.value_.empty() && (result.value_.back() == '\0' || result.value_.back() == ' '))
            result.value_.pop_back();
        return result;
    }
private:
    std::string value_;
};

// utf8_str
class utf8_str {
public:
    static constexpr size_t WIRE_SIZE = 16;

    std::string value() const { return value_; }
    void set_value(const std::string& v) { value_ = v; }

    bool operator==(const utf8_str&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_string(value_, 16, ' ');
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }
    static conduit::Result<utf8_str> decode(conduit::io::BitReader& r) {
        auto s = r.read_string(16);
        if (!s) return std::unexpected(s.error());
        utf8_str result;
        result.value_ = std::move(*s);
        // Trim trailing padding
        while (!result.value_.empty() && (result.value_.back() == '\0' || result.value_.back() == ' '))
            result.value_.pop_back();
        return result;
    }
private:
    std::string value_;
};

using le_uint16 = uint16_t;
using le_uint32 = uint32_t;
class scaled_temp {
public:
    static constexpr double SCALE = 0.01;
    static constexpr double OFFSET = -40;
    double value() const { return static_cast<double>(raw_) * SCALE + OFFSET; }
    uint16_t raw() const { return raw_; }
    void set_value(double v) { raw_ = static_cast<uint16_t>((v - OFFSET) / SCALE); }
    void set_raw(uint16_t v) { raw_ = v; }

    bool operator==(const scaled_temp&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_bits(raw_, 16);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }
    static conduit::Result<scaled_temp> decode(conduit::io::BitReader& r) {
        auto raw = r.read_bits(16);
        if (!raw) return std::unexpected(raw.error());
        scaled_temp result;
        result.raw_ = static_cast<uint16_t>(*raw);
        return result;
    }
private:
    uint16_t raw_ = 0;
};

using hex_id = uint32_t;
enum class color_enum : uint8_t {
    red = 1,
    green = 2,
    blue = 3,
};

inline std::string_view to_string(color_enum v) {
    switch (v) {
        case color_enum::red: return "red";
        case color_enum::green: return "green";
        case color_enum::blue: return "blue";
    }
    return "unknown";
}

inline conduit::Result<color_enum> decode_color_enum(conduit::io::BitReader& r) {
    auto raw = r.read_bits(8);
    if (!raw) return std::unexpected(raw.error());
    auto val = static_cast<color_enum>(*raw);
    switch (val) {
        case color_enum::red:
        case color_enum::green:
        case color_enum::blue:
            return val;
    }
    return std::unexpected(conduit::Error(conduit::ErrorCode::UnknownEnumValue,
        "unknown color_enum value: " + std::to_string(static_cast<uint8_t>(val))));
}

inline conduit::VoidResult encode_color_enum(color_enum v, conduit::io::BitWriter& w) {
    switch (v) {
        case color_enum::red:
        case color_enum::green:
        case color_enum::blue:
            break;
        default:
            return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                "invalid color_enum enum value: " + std::to_string(static_cast<uint8_t>(v))));
    }
    w.write_bits(static_cast<uint8_t>(v), 8);
    if (w.has_error()) return std::unexpected(w.error());
    return {};
}

class status_flags {
public:
    bool active() const { return (raw_ >> 0) & 1; }
    void set_active(bool v) { if (v) raw_ |= (static_cast<uint8_t>(1) << 0); else raw_ &= static_cast<uint8_t>(~(static_cast<uint8_t>(1) << 0)); }
    bool error() const { return (raw_ >> 1) & 1; }
    void set_error(bool v) { if (v) raw_ |= (static_cast<uint8_t>(1) << 1); else raw_ &= static_cast<uint8_t>(~(static_cast<uint8_t>(1) << 1)); }
    bool ready() const { return (raw_ >> 2) & 1; }
    void set_ready(bool v) { if (v) raw_ |= (static_cast<uint8_t>(1) << 2); else raw_ &= static_cast<uint8_t>(~(static_cast<uint8_t>(1) << 2)); }

    uint8_t raw() const { return raw_; }
    void set_raw(uint8_t v) { raw_ = v; }

    bool operator==(const status_flags&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_bits(raw_, 8);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }
    static conduit::Result<status_flags> decode(conduit::io::BitReader& r) {
        auto raw = r.read_bits(8);
        if (!raw) return std::unexpected(raw.error());
        status_flags result;
        result.raw_ = static_cast<uint8_t>(*raw);
        return result;
    }
private:
    uint8_t raw_ = 0;
};


} // namespace all_types

