// Generated by bgen - DO NOT EDIT
#pragma once

#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <conduit/io/endian.hpp>
#include <cstdint>
#include <string>
#include <string_view>

namespace boundary_types {

using uint1 = uint8_t;
using uint3 = uint8_t;
using uint7 = uint8_t;
using uint8 = uint8_t;
using uint16 = uint16_t;
using uint32 = uint32_t;
using uint64 = uint64_t;
using int8 = int8_t;
using int16 = int16_t;
using int32 = int32_t;
using int64 = int64_t;
class scaled_temp {
public:
    static constexpr double SCALE = 0.1;
    static constexpr double OFFSET = -40;
    double value() const { return static_cast<double>(raw_) * SCALE + OFFSET; }
    uint16_t raw() const { return raw_; }
    void set_value(double v) { raw_ = static_cast<uint16_t>((v - OFFSET) / SCALE); }
    void set_raw(uint16_t v) { raw_ = v; }

    bool operator==(const scaled_temp&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_bits(raw_, 16);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }
    static conduit::Result<scaled_temp> decode(conduit::io::BitReader& r) {
        auto raw = r.read_bits(16);
        if (!raw) return std::unexpected(raw.error());
        scaled_temp result;
        result.raw_ = static_cast<uint16_t>(*raw);
        return result;
    }
private:
    uint16_t raw_ = 0;
};

using uint12 = uint16_t;
using uint20 = uint32_t;
using int12 = int16_t;
enum class nybble_enum : uint8_t {
    off = 0,
    low = 1,
    mid = 2,
    high = 3,
};

inline std::string_view to_string(nybble_enum v) {
    switch (v) {
        case nybble_enum::off: return "off";
        case nybble_enum::low: return "low";
        case nybble_enum::mid: return "mid";
        case nybble_enum::high: return "high";
    }
    return "unknown";
}

inline conduit::Result<nybble_enum> decode_nybble_enum(conduit::io::BitReader& r) {
    auto raw = r.read_bits(4);
    if (!raw) return std::unexpected(raw.error());
    auto val = static_cast<nybble_enum>(*raw);
    switch (val) {
        case nybble_enum::off:
        case nybble_enum::low:
        case nybble_enum::mid:
        case nybble_enum::high:
            return val;
    }
    return std::unexpected(conduit::Error(conduit::ErrorCode::UnknownEnumValue,
        "unknown nybble_enum value: " + std::to_string(static_cast<uint8_t>(val))));
}

inline conduit::VoidResult encode_nybble_enum(nybble_enum v, conduit::io::BitWriter& w) {
    switch (v) {
        case nybble_enum::off:
        case nybble_enum::low:
        case nybble_enum::mid:
        case nybble_enum::high:
            break;
        default:
            return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                "invalid nybble_enum enum value: " + std::to_string(static_cast<uint8_t>(v))));
    }
    w.write_bits(static_cast<uint8_t>(v), 4);
    if (w.has_error()) return std::unexpected(w.error());
    return {};
}


} // namespace boundary_types

