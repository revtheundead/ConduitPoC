// Generated by bgen - DO NOT EDIT
#pragma once

#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string>
#include <variant>
#include <vector>

namespace outer_scope {

class DataA;
class DataB;
class Packet;

class DataA {
public:
    const uint8& x() const { return x_; }
    uint8& mutable_x() { return x_; }
    void set_x(const uint8& v) { x_ = v; }

    const uint8& y() const { return y_; }
    uint8& mutable_y() { return y_; }
    void set_y(const uint8& v) { y_ = v; }

    static constexpr size_t WIRE_SIZE = 2;

    bool operator==(const DataA&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(x_));
        w.write_u8(static_cast<uint8_t>(y_));
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<DataA> decode(conduit::io::BitReader& r) {
        DataA result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'x'"));
            result.x_ = static_cast<uint8>(*val);
        }
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'y'"));
            result.y_ = static_cast<uint8>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "DataA{"
            << "x=" << +(x_)
            << ", y=" << +(y_)
            << "}";
        return oss.str();
    }

private:
    uint8 x_{};
    uint8 y_{};
};

class DataB {
public:
    const uint16& value() const { return value_; }
    uint16& mutable_value() { return value_; }
    void set_value(const uint16& v) { value_ = v; }

    static constexpr size_t WIRE_SIZE = 2;

    bool operator==(const DataB&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u16(static_cast<uint16_t>(value_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<DataB> decode(conduit::io::BitReader& r) {
        DataB result;
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'value'"));
            result.value_ = static_cast<uint16>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "DataB{"
            << "value=" << +(value_)
            << "}";
        return oss.str();
    }

private:
    uint16 value_{};
};

class Packet_payloadOtherwise {
public:
    const std::vector<uint8_t>& data() const { return data_; }
    std::vector<uint8_t>& mutable_data() { return data_; }
    void set_data(const std::vector<uint8_t>& v) { data_ = v; }

    bool operator==(const Packet_payloadOtherwise&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_bytes(std::span<const uint8_t>(data_.data(), data_.size()));
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<Packet_payloadOtherwise> decode(conduit::io::BitReader& r, uint8 len) {
        Packet_payloadOtherwise result;
        {
            auto nbytes_ = static_cast<size_t>((len - 2));
            auto span = r.read_bytes(nbytes_);
            if (!span) return std::unexpected(span.error().with_context("field 'data'"));
            result.data_.assign(span->begin(), span->end());
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "Packet_payloadOtherwise{"
            << "data=[bytes]"
            << "}";
        return oss.str();
    }

private:
    std::vector<uint8_t> data_{};
};

using payloadVariant = std::variant<DataA, DataB, Packet_payloadOtherwise>;
class Packet {
public:
    const uint8& tag() const { return tag_; }
    uint8& mutable_tag() { return tag_; }
    void set_tag(const uint8& v) { tag_ = v; }

    const uint8& len() const { return len_; }
    uint8& mutable_len() { return len_; }
    void set_len(const uint8& v) { len_ = v; }

    const payloadVariant& payload() const { return payload_; }
    payloadVariant& mutable_payload() { return payload_; }
    void set_payload(const payloadVariant& v) { payload_ = v; }

    bool operator==(const Packet&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        auto struct_start_pos_ = w.size_bytes();
        w.write_u8(static_cast<uint8_t>(tag_));
        auto length_byte_pos_ = w.size_bytes();
        w.write_u8(static_cast<uint8_t>(0));
        {
            auto _sw = tag_;
            auto _idx = (payload_).index();
            if (_idx == 0) {
                if (_sw != static_cast<decltype(_sw)>(1))
                    return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                        "choice 'payload': variant does not match discriminator"));
            } else if (_idx == 1) {
                if (_sw != static_cast<decltype(_sw)>(2))
                    return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                        "choice 'payload': variant does not match discriminator"));
            } else if (_idx == 2) {
                if (_sw == static_cast<decltype(_sw)>(1) || _sw == static_cast<decltype(_sw)>(2))
                    return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                        "choice 'payload': variant does not match discriminator"));
            }
        }
        {
            auto _enc_r = std::visit([&w](const auto& v) -> conduit::VoidResult { return v.encode(w); }, payload_);
            if (!_enc_r) return std::unexpected(_enc_r.error());
        }
        if (!w.patch_u8(length_byte_pos_, static_cast<uint8_t>(w.size_bytes() - struct_start_pos_)))
            return std::unexpected(conduit::Error(conduit::ErrorCode::InvalidArgument, "failed to patch auto-length"));
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<Packet> decode(conduit::io::BitReader& r) {
        Packet result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'tag'"));
            result.tag_ = static_cast<uint8>(*val);
        }
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'len'"));
            result.len_ = static_cast<uint8>(*val);
        }
        {
            auto switch_val = result.tag_;
            if (switch_val == static_cast<decltype(switch_val)>(1)) {
                auto val = DataA::decode(r);
                if (!val) return std::unexpected(val.error().with_context("choice 'payload'"));
                result.payload_ = std::move(*val);
            } else if (switch_val == static_cast<decltype(switch_val)>(2)) {
                auto val = DataB::decode(r);
                if (!val) return std::unexpected(val.error().with_context("choice 'payload'"));
                result.payload_ = std::move(*val);
            } else {
                auto val = Packet_payloadOtherwise::decode(r, result.len_);
                if (!val) return std::unexpected(val.error().with_context("choice 'payload'"));
                result.payload_ = std::move(*val);
            }
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "Packet{"
            << "tag=" << +(tag_)
            << ", len=" << +(len_)
            << ", payload=" << std::visit([](const auto& v) { return v.to_string(); }, payload_)
            << "}";
        return oss.str();
    }

private:
    uint8 tag_{};
    uint8 len_{};
    payloadVariant payload_{};
};

} // namespace outer_scope

