// Generated by bgen - DO NOT EDIT
#pragma once

#include "structs.hpp"
#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <any>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string_view>
#include <variant>
#include <vector>

namespace enum_arrays {

class EnumArrayMsg;

class EnumArrayMsg {
public:
    static constexpr uint64_t TYPE_ID = 0x6da41f1b8e6d1efaULL;
    static constexpr std::string_view TYPE_NAME = "EnumArrayMsg";

    const uint8& count() const { return count_; }
    uint8& mutable_count() { return count_; }
    void set_count(const uint8& v) { count_ = v; }

    const std::vector<command_code>& commands() const { return commands_; }
    std::vector<command_code>& mutable_commands() { return commands_; }
    void set_commands(const std::vector<command_code>& v) { commands_ = v; }

    const std::vector<command_code>& dynamic_commands() const { return dynamic_commands_; }
    std::vector<command_code>& mutable_dynamic_commands() { return dynamic_commands_; }
    void set_dynamic_commands(const std::vector<command_code>& v) { dynamic_commands_ = v; }

    const std::vector<uint8_t>& bcd_values() const { return bcd_values_; }
    std::vector<uint8_t>& mutable_bcd_values() { return bcd_values_; }
    void set_bcd_values(const std::vector<uint8_t>& v) { bcd_values_ = v; }

    bool operator==(const EnumArrayMsg&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(count_));
        for (const auto& elem : commands_) {
            CONDUIT_TRY(encode_command_code(elem, w));
        }
        for (const auto& elem : dynamic_commands_) {
            CONDUIT_TRY(encode_command_code(elem, w));
        }
        for (const auto& elem : bcd_values_) {
            w.write_bcd(elem, 8);
        }
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<EnumArrayMsg> decode(conduit::io::BitReader& r) {
        EnumArrayMsg result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'count'"));
            result.count_ = static_cast<uint8>(*val);
        }
        result.commands_.reserve(4);
        for (int i = 0; i < 4; i++) {
            auto elem = decode_command_code(r);
            if (!elem) return std::unexpected(elem.error().with_context("array 'commands'"));
            result.commands_.push_back(std::move(*elem));
        }
        {
            auto count = static_cast<size_t>(result.count_);
            if (count > static_cast<size_t>(INT32_MAX)) return std::unexpected(conduit::Error(conduit::ErrorCode::InvalidArgument, "invalid array count").with_context("array 'dynamic-commands'"));
            result.dynamic_commands_.reserve(count);
            for (size_t i = 0; i < count; i++) {
                auto elem = decode_command_code(r);
                if (!elem) return std::unexpected(elem.error().with_context("array 'dynamic-commands'"));
                result.dynamic_commands_.push_back(std::move(*elem));
            }
        }
        result.bcd_values_.reserve(3);
        for (int i = 0; i < 3; i++) {
            auto elem = r.read_bcd(8);
            if (!elem) return std::unexpected(elem.error().with_context("array 'bcd-values'"));
            result.bcd_values_.push_back(static_cast<uint8_t>(*elem));
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "EnumArrayMsg{"
            << "count=" << +(count_)
            << ", commands=[" << commands_.size() << " items]"
            << ", dynamic-commands=[" << dynamic_commands_.size() << " items]"
            << ", bcd-values=[" << bcd_values_.size() << " items]"
            << "}";
        return oss.str();
    }

private:
    uint8 count_{};
    std::vector<command_code> commands_{};
    std::vector<command_code> dynamic_commands_{};
    std::vector<uint8_t> bcd_values_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<EnumArrayMsg> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

} // namespace enum_arrays

