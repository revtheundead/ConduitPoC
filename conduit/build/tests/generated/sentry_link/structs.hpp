// Generated by bgen - DO NOT EDIT
#pragma once

#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string>
#include <variant>
#include <vector>

namespace sentry_link {

class FirmwareVersion;
class SensorFlags;

class FirmwareVersion {
public:
    const uint8& major() const { return major_; }
    uint8& mutable_major() { return major_; }
    void set_major(const uint8& v) { major_ = v; }

    const uint8& minor() const { return minor_; }
    uint8& mutable_minor() { return minor_; }
    void set_minor(const uint8& v) { minor_ = v; }

    const uint16& patch() const { return patch_; }
    uint16& mutable_patch() { return patch_; }
    void set_patch(const uint16& v) { patch_ = v; }

    static constexpr size_t WIRE_SIZE = 4;

    bool operator==(const FirmwareVersion&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(major_));
        w.write_u8(static_cast<uint8_t>(minor_));
        w.write_u16(static_cast<uint16_t>(patch_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<FirmwareVersion> decode(conduit::io::BitReader& r) {
        FirmwareVersion result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'major'"));
            result.major_ = static_cast<uint8>(*val);
        }
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'minor'"));
            result.minor_ = static_cast<uint8>(*val);
        }
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'patch'"));
            result.patch_ = static_cast<uint16>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "FirmwareVersion{"
            << "major=" << +(major_)
            << ", minor=" << +(minor_)
            << ", patch=" << +(patch_)
            << "}";
        return oss.str();
    }

private:
    uint8 major_{};
    uint8 minor_{};
    uint16 patch_{};
};

class SensorFlags {
public:
    const uint8_t& channel() const { return channel_; }
    uint8_t& mutable_channel() { return channel_; }
    void set_channel(const uint8_t& v) { channel_ = v; }

    const uint8_t& precision() const { return precision_; }
    uint8_t& mutable_precision() { return precision_; }
    void set_precision(const uint8_t& v) { precision_ = v; }

    const uint8_t& saturated() const { return saturated_; }
    uint8_t& mutable_saturated() { return saturated_; }
    void set_saturated(const uint8_t& v) { saturated_ = v; }

    const uint8_t& valid() const { return valid_; }
    uint8_t& mutable_valid() { return valid_; }
    void set_valid(const uint8_t& v) { valid_ = v; }

    static constexpr size_t WIRE_SIZE = 1;

    bool operator==(const SensorFlags&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_bits(channel_, 4);
        w.write_bits(precision_, 2);
        w.write_bits(saturated_, 1);
        w.write_bits(valid_, 1);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<SensorFlags> decode(conduit::io::BitReader& r) {
        SensorFlags result;
        {
            auto val = r.read_bits(4);
            if (!val) return std::unexpected(val.error().with_context("field 'channel'"));
            result.channel_ = static_cast<uint8_t>(*val);
        }
        {
            auto val = r.read_bits(2);
            if (!val) return std::unexpected(val.error().with_context("field 'precision'"));
            result.precision_ = static_cast<uint8_t>(*val);
        }
        {
            auto val = r.read_bits(1);
            if (!val) return std::unexpected(val.error().with_context("field 'saturated'"));
            result.saturated_ = static_cast<uint8_t>(*val);
        }
        {
            auto val = r.read_bits(1);
            if (!val) return std::unexpected(val.error().with_context("field 'valid'"));
            result.valid_ = static_cast<uint8_t>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "SensorFlags{"
            << "channel=" << +(channel_)
            << ", precision=" << +(precision_)
            << ", saturated=" << +(saturated_)
            << ", valid=" << +(valid_)
            << "}";
        return oss.str();
    }

private:
    uint8_t channel_{};
    uint8_t precision_{};
    uint8_t saturated_{};
    uint8_t valid_{};
};

} // namespace sentry_link

