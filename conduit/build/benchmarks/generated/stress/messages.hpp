// Generated by bgen - DO NOT EDIT
#pragma once

#include "structs.hpp"
#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <any>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string_view>
#include <variant>
#include <vector>

namespace stress {

class Ping;
class TelemetryReport;
class SurveillanceRecord;

class Ping {
public:
    static constexpr uint64_t TYPE_ID = 0xe9bee119c6a37b89ULL;
    static constexpr std::string_view TYPE_NAME = "Ping";
    static constexpr uint8 ID_VALUE = 1;

    uint8 msg_type() const { return msg_type_; }
    [[deprecated("auto-managed: value is set automatically during frame encoding")]]
    void set_msg_type(uint8 v) { msg_type_ = v; }
    uint16 length() const { return length_; }
    [[deprecated("auto-managed: value is set automatically during frame encoding")]]
    void set_length(uint16 v) { length_ = v; }

    const uint32& sequence() const { return sequence_; }
    uint32& mutable_sequence() { return sequence_; }
    void set_sequence(const uint32& v) { sequence_ = v; }

    const uint32& timestamp() const { return timestamp_; }
    uint32& mutable_timestamp() { return timestamp_; }
    void set_timestamp(const uint32& v) { timestamp_ = v; }

    const priority_level& priority() const { return priority_; }
    priority_level& mutable_priority() { return priority_; }
    void set_priority(const priority_level& v) { priority_ = v; }

    const status_flags& flags() const { return flags_; }
    status_flags& mutable_flags() { return flags_; }
    void set_flags(const status_flags& v) { flags_ = v; }

    const uint16& tag() const { return tag_; }
    uint16& mutable_tag() { return tag_; }
    void set_tag(const uint16& v) { tag_ = v; }

    static constexpr size_t WIRE_SIZE = 12;

    bool operator==(const Ping&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u32(static_cast<uint32_t>(sequence_), conduit::io::Endian::Big);
        w.write_u32(static_cast<uint32_t>(timestamp_), conduit::io::Endian::Big);
        CONDUIT_TRY(encode_priority_level(priority_, w));
        CONDUIT_TRY(flags_.encode(w));
        w.write_u16(static_cast<uint16_t>(tag_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<Ping> decode(conduit::io::BitReader& r) {
        Ping result;
        {
            auto val = r.read_u32(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'sequence'"));
            result.sequence_ = static_cast<uint32>(*val);
        }
        {
            auto val = r.read_u32(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'timestamp'"));
            result.timestamp_ = static_cast<uint32>(*val);
        }
        {
            auto val = decode_priority_level(r);
            if (!val) return std::unexpected(val.error().with_context("field 'priority'"));
            result.priority_ = std::move(*val);
        }
        {
            auto val = status_flags::decode(r);
            if (!val) return std::unexpected(val.error().with_context("field 'flags'"));
            result.flags_ = std::move(*val);
        }
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'tag'"));
            result.tag_ = static_cast<uint16>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "Ping{"
            << "msg-type=" << +(msg_type_)
            << ", length=" << +(length_)
            << ", sequence=" << +(sequence_)
            << ", timestamp=" << +(timestamp_)
            << ", priority=" << ::stress::to_string(priority_)
            << ", flags=0x" << std::hex << flags_.raw() << std::dec
            << ", tag=" << +(tag_)
            << "}";
        return oss.str();
    }

private:
    friend class StressFrame;

    uint8 msg_type_{};
    uint16 length_{};
    uint32 sequence_{};
    uint32 timestamp_{};
    priority_level priority_{priority_level::low};
    status_flags flags_{};
    uint16 tag_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<Ping> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

class TelemetryReport {
public:
    static constexpr uint64_t TYPE_ID = 0x796540d1b59d827aULL;
    static constexpr std::string_view TYPE_NAME = "TelemetryReport";
    static constexpr uint8 ID_VALUE = 2;

    uint8 msg_type() const { return msg_type_; }
    [[deprecated("auto-managed: value is set automatically during frame encoding")]]
    void set_msg_type(uint8 v) { msg_type_ = v; }
    uint16 length() const { return length_; }
    [[deprecated("auto-managed: value is set automatically during frame encoding")]]
    void set_length(uint16 v) { length_ = v; }

    const uint32& sequence() const { return sequence_; }
    uint32& mutable_sequence() { return sequence_; }
    void set_sequence(const uint32& v) { sequence_ = v; }

    const uint32& timestamp() const { return timestamp_; }
    uint32& mutable_timestamp() { return timestamp_; }
    void set_timestamp(const uint32& v) { timestamp_ = v; }

    const uint16& source_id() const { return source_id_; }
    uint16& mutable_source_id() { return source_id_; }
    void set_source_id(const uint16& v) { source_id_ = v; }

    const sensor_type& sensor() const { return sensor_; }
    sensor_type& mutable_sensor() { return sensor_; }
    void set_sensor(const sensor_type& v) { sensor_ = v; }

    const Coordinate& position() const { return position_; }
    Coordinate& mutable_position() { return position_; }
    void set_position(const Coordinate& v) { position_ = v; }

    const Velocity& velocity() const { return velocity_; }
    Velocity& mutable_velocity() { return velocity_; }
    void set_velocity(const Velocity& v) { velocity_ = v; }

    const ::stress::temperature& temperature() const { return temperature_; }
    ::stress::temperature& mutable_temperature() { return temperature_; }
    void set_temperature(const ::stress::temperature& v) { temperature_ = v; }

    const pressure_hpa& pressure() const { return pressure_; }
    pressure_hpa& mutable_pressure() { return pressure_; }
    void set_pressure(const pressure_hpa& v) { pressure_ = v; }

    const angle_deg& heading() const { return heading_; }
    angle_deg& mutable_heading() { return heading_; }
    void set_heading(const angle_deg& v) { heading_ = v; }

    const status_flags& status() const { return status_; }
    status_flags& mutable_status() { return status_; }
    void set_status(const status_flags& v) { status_ = v; }

    const std::string& label() const { return label_; }
    std::string& mutable_label() { return label_; }
    void set_label(const std::string& v) { label_ = v; }

    static constexpr size_t WIRE_SIZE = 50;

    bool operator==(const TelemetryReport&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u32(static_cast<uint32_t>(sequence_), conduit::io::Endian::Big);
        w.write_u32(static_cast<uint32_t>(timestamp_), conduit::io::Endian::Big);
        w.write_u16(static_cast<uint16_t>(source_id_), conduit::io::Endian::Big);
        CONDUIT_TRY(encode_sensor_type(sensor_, w));
        CONDUIT_TRY(position_.encode(w));
        CONDUIT_TRY(velocity_.encode(w));
        CONDUIT_TRY(temperature_.encode(w));
        CONDUIT_TRY(pressure_.encode(w));
        CONDUIT_TRY(heading_.encode(w));
        CONDUIT_TRY(status_.encode(w));
        w.write_string(label_, 16, '\0');
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<TelemetryReport> decode(conduit::io::BitReader& r) {
        TelemetryReport result;
        {
            auto val = r.read_u32(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'sequence'"));
            result.sequence_ = static_cast<uint32>(*val);
        }
        {
            auto val = r.read_u32(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'timestamp'"));
            result.timestamp_ = static_cast<uint32>(*val);
        }
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'source-id'"));
            result.source_id_ = static_cast<uint16>(*val);
        }
        {
            auto val = decode_sensor_type(r);
            if (!val) return std::unexpected(val.error().with_context("field 'sensor'"));
            result.sensor_ = std::move(*val);
        }
        {
            auto val = Coordinate::decode(r);
            if (!val) return std::unexpected(val.error().with_context("field 'position'"));
            result.position_ = std::move(*val);
        }
        {
            auto val = Velocity::decode(r);
            if (!val) return std::unexpected(val.error().with_context("field 'velocity'"));
            result.velocity_ = std::move(*val);
        }
        {
            auto val = temperature::decode(r);
            if (!val) return std::unexpected(val.error().with_context("field 'temperature'"));
            result.temperature_ = std::move(*val);
        }
        {
            auto val = pressure_hpa::decode(r);
            if (!val) return std::unexpected(val.error().with_context("field 'pressure'"));
            result.pressure_ = std::move(*val);
        }
        {
            auto val = angle_deg::decode(r);
            if (!val) return std::unexpected(val.error().with_context("field 'heading'"));
            result.heading_ = std::move(*val);
        }
        {
            auto val = status_flags::decode(r);
            if (!val) return std::unexpected(val.error().with_context("field 'status'"));
            result.status_ = std::move(*val);
        }
        {
            auto val = r.read_string(16);
            if (!val) return std::unexpected(val.error().with_context("field 'label'"));
            result.label_ = std::move(*val);
            while (!result.label_.empty() && (result.label_.back() == '\0' || result.label_.back() == ' '))
                result.label_.pop_back();
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "TelemetryReport{"
            << "msg-type=" << +(msg_type_)
            << ", length=" << +(length_)
            << ", sequence=" << +(sequence_)
            << ", timestamp=" << +(timestamp_)
            << ", source-id=" << +(source_id_)
            << ", sensor=" << ::stress::to_string(sensor_)
            << ", position=" << position_.to_string()
            << ", velocity=" << velocity_.to_string()
            << ", temperature=" << temperature_.value()
            << ", pressure=" << pressure_.value()
            << ", heading=" << heading_.value()
            << ", status=0x" << std::hex << status_.raw() << std::dec
            << ", label=" << label_
            << "}";
        return oss.str();
    }

private:
    friend class StressFrame;

    uint8 msg_type_{};
    uint16 length_{};
    uint32 sequence_{};
    uint32 timestamp_{};
    uint16 source_id_{};
    sensor_type sensor_{sensor_type::temperature};
    Coordinate position_{};
    Velocity velocity_{};
    ::stress::temperature temperature_{};
    pressure_hpa pressure_{};
    angle_deg heading_{};
    status_flags status_{};
    std::string label_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<TelemetryReport> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

class items {
public:
    bool has_source_id() const { return source_id_.has_value(); }
    const uint16& source_id() const { return source_id_.value(); }
    uint16& mutable_source_id() { if (!source_id_) source_id_.emplace(); return *source_id_; }
    void set_source_id(const uint16& v) { source_id_ = v; }
    void clear_source_id() { source_id_.reset(); }

    bool has_geo_position() const { return geo_position_.has_value(); }
    const GeoPosition& geo_position() const { return geo_position_.value(); }
    GeoPosition& mutable_geo_position() { if (!geo_position_) geo_position_.emplace(); return *geo_position_; }
    void set_geo_position(const GeoPosition& v) { geo_position_ = v; }
    void clear_geo_position() { geo_position_.reset(); }

    bool has_cartesian() const { return cartesian_.has_value(); }
    const Coordinate& cartesian() const { return cartesian_.value(); }
    Coordinate& mutable_cartesian() { if (!cartesian_) cartesian_.emplace(); return *cartesian_; }
    void set_cartesian(const Coordinate& v) { cartesian_ = v; }
    void clear_cartesian() { cartesian_.reset(); }

    bool has_velocity() const { return velocity_.has_value(); }
    const Velocity& velocity() const { return velocity_.value(); }
    Velocity& mutable_velocity() { if (!velocity_) velocity_.emplace(); return *velocity_; }
    void set_velocity(const Velocity& v) { velocity_ = v; }
    void clear_velocity() { velocity_.reset(); }

    bool has_heading() const { return heading_.has_value(); }
    const angle_deg& heading() const { return heading_.value(); }
    angle_deg& mutable_heading() { if (!heading_) heading_.emplace(); return *heading_; }
    void set_heading(const angle_deg& v) { heading_ = v; }
    void clear_heading() { heading_.reset(); }

    bool has_altitude_rate() const { return altitude_rate_.has_value(); }
    const altitude_fl& altitude_rate() const { return altitude_rate_.value(); }
    altitude_fl& mutable_altitude_rate() { if (!altitude_rate_) altitude_rate_.emplace(); return *altitude_rate_; }
    void set_altitude_rate(const altitude_fl& v) { altitude_rate_ = v; }
    void clear_altitude_rate() { altitude_rate_.reset(); }

    bool has_quality() const { return quality_.has_value(); }
    const TrackQuality& quality() const { return quality_.value(); }
    TrackQuality& mutable_quality() { if (!quality_) quality_.emplace(); return *quality_; }
    void set_quality(const TrackQuality& v) { quality_ = v; }
    void clear_quality() { quality_.reset(); }

    bool has_status() const { return status_.has_value(); }
    const ExtendedStatus& status() const { return status_.value(); }
    ExtendedStatus& mutable_status() { if (!status_) status_.emplace(); return *status_; }
    void set_status(const ExtendedStatus& v) { status_ = v; }
    void clear_status() { status_.reset(); }

    bool has_label() const { return label_.has_value(); }
    const std::string& label() const { return label_.value(); }
    std::string& mutable_label() { if (!label_) label_.emplace(); return *label_; }
    void set_label(const std::string& v) { label_ = v; }
    void clear_label() { label_.reset(); }

    bool has_temperature() const { return temperature_.has_value(); }
    const ::stress::temperature& temperature() const { return temperature_.value(); }
    ::stress::temperature& mutable_temperature() { if (!temperature_) temperature_.emplace(); return *temperature_; }
    void set_temperature(const ::stress::temperature& v) { temperature_ = v; }
    void clear_temperature() { temperature_.reset(); }

    bool has_pressure() const { return pressure_.has_value(); }
    const pressure_hpa& pressure() const { return pressure_.value(); }
    pressure_hpa& mutable_pressure() { if (!pressure_) pressure_.emplace(); return *pressure_; }
    void set_pressure(const pressure_hpa& v) { pressure_ = v; }
    void clear_pressure() { pressure_.reset(); }

    bool has_sensor() const { return sensor_.has_value(); }
    const sensor_type& sensor() const { return sensor_.value(); }
    sensor_type& mutable_sensor() { if (!sensor_) sensor_.emplace(); return *sensor_; }
    void set_sensor(const sensor_type& v) { sensor_ = v; }
    void clear_sensor() { sensor_.reset(); }

    bool has_readings() const { return readings_.has_value(); }
    const ReadingsItem& readings() const { return readings_.value(); }
    ReadingsItem& mutable_readings() { if (!readings_) readings_.emplace(); return *readings_; }
    void set_readings(const ReadingsItem& v) { readings_ = v; }
    void clear_readings() { readings_.reset(); }

    bool has_raw_data() const { return raw_data_.has_value(); }
    const std::array<uint8_t, 16>& raw_data() const { return raw_data_.value(); }
    std::array<uint8_t, 16>& mutable_raw_data() { if (!raw_data_) raw_data_.emplace(); return *raw_data_; }
    void set_raw_data(const std::array<uint8_t, 16>& v) { raw_data_ = v; }
    void clear_raw_data() { raw_data_.reset(); }

    bool operator==(const items&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        int last_octet = 0;
        if (source_id_.has_value() && 0 > last_octet) last_octet = 0;
        if (geo_position_.has_value() && 0 > last_octet) last_octet = 0;
        if (cartesian_.has_value() && 0 > last_octet) last_octet = 0;
        if (velocity_.has_value() && 0 > last_octet) last_octet = 0;
        if (heading_.has_value() && 0 > last_octet) last_octet = 0;
        if (altitude_rate_.has_value() && 0 > last_octet) last_octet = 0;
        if (quality_.has_value() && 0 > last_octet) last_octet = 0;
        if (status_.has_value() && 1 > last_octet) last_octet = 1;
        if (label_.has_value() && 1 > last_octet) last_octet = 1;
        if (temperature_.has_value() && 1 > last_octet) last_octet = 1;
        if (pressure_.has_value() && 1 > last_octet) last_octet = 1;
        if (sensor_.has_value() && 1 > last_octet) last_octet = 1;
        if (readings_.has_value() && 1 > last_octet) last_octet = 1;
        if (raw_data_.has_value() && 1 > last_octet) last_octet = 1;
        std::array<uint8_t, 2> fspec{};
        if (source_id_.has_value()) fspec[0] |= (1 << 7);
        if (geo_position_.has_value()) fspec[0] |= (1 << 6);
        if (cartesian_.has_value()) fspec[0] |= (1 << 5);
        if (velocity_.has_value()) fspec[0] |= (1 << 4);
        if (heading_.has_value()) fspec[0] |= (1 << 3);
        if (altitude_rate_.has_value()) fspec[0] |= (1 << 2);
        if (quality_.has_value()) fspec[0] |= (1 << 1);
        if (status_.has_value()) fspec[1] |= (1 << 7);
        if (label_.has_value()) fspec[1] |= (1 << 6);
        if (temperature_.has_value()) fspec[1] |= (1 << 5);
        if (pressure_.has_value()) fspec[1] |= (1 << 4);
        if (sensor_.has_value()) fspec[1] |= (1 << 3);
        if (readings_.has_value()) fspec[1] |= (1 << 2);
        if (raw_data_.has_value()) fspec[1] |= (1 << 1);
        for (size_t i = 0; i < static_cast<size_t>(last_octet); i++) fspec[i] |= (1 << 0);
        w.write_bytes(std::span<const uint8_t>(fspec.data(), static_cast<size_t>(last_octet) + 1));
        if (source_id_.has_value()) {
            w.write_bits(*source_id_, 16);
        }
        if (geo_position_.has_value()) {
            CONDUIT_TRY(geo_position_->encode(w));
        }
        if (cartesian_.has_value()) {
            CONDUIT_TRY(cartesian_->encode(w));
        }
        if (velocity_.has_value()) {
            CONDUIT_TRY(velocity_->encode(w));
        }
        if (heading_.has_value()) {
            CONDUIT_TRY(heading_->encode(w));
        }
        if (altitude_rate_.has_value()) {
            CONDUIT_TRY(altitude_rate_->encode(w));
        }
        if (quality_.has_value()) {
            CONDUIT_TRY(quality_->encode(w));
        }
        if (status_.has_value()) {
            CONDUIT_TRY(status_->encode(w));
        }
        if (label_.has_value()) {
            w.write_string(*label_, 8);
        }
        if (temperature_.has_value()) {
            CONDUIT_TRY(temperature_->encode(w));
        }
        if (pressure_.has_value()) {
            CONDUIT_TRY(pressure_->encode(w));
        }
        if (sensor_.has_value()) {
            CONDUIT_TRY(encode_sensor_type(*sensor_, w));
        }
        if (readings_.has_value()) {
            CONDUIT_TRY(readings_->encode(w));
        }
        if (raw_data_.has_value()) {
            w.write_bytes(std::span<const uint8_t>(raw_data_->data(), 16));
        }
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<items> decode(conduit::io::BitReader& r) {
        items result;

        // Read FSPEC bitmap
        std::array<uint8_t, 2> fspec{};
        size_t fspec_len = 0;
        while (true) {
            auto byte = r.read_u8();
            if (!byte) return std::unexpected(byte.error());
            if (fspec_len < 2) fspec[fspec_len] = *byte;
            fspec_len++;
            if (!(*byte & (1 << 0))) break;
        }

        if (fspec_len > 0 && (fspec[0] & (1 << 7))) {
            auto val = r.read_bits(16);
            if (!val) return std::unexpected(val.error());
            result.source_id_ = static_cast<uint16>(*val);
        }
        if (fspec_len > 0 && (fspec[0] & (1 << 6))) {
            auto val = GeoPosition::decode(r);
            if (!val) return std::unexpected(val.error());
            result.geo_position_ = std::move(*val);
        }
        if (fspec_len > 0 && (fspec[0] & (1 << 5))) {
            auto val = Coordinate::decode(r);
            if (!val) return std::unexpected(val.error());
            result.cartesian_ = std::move(*val);
        }
        if (fspec_len > 0 && (fspec[0] & (1 << 4))) {
            auto val = Velocity::decode(r);
            if (!val) return std::unexpected(val.error());
            result.velocity_ = std::move(*val);
        }
        if (fspec_len > 0 && (fspec[0] & (1 << 3))) {
            auto val = angle_deg::decode(r);
            if (!val) return std::unexpected(val.error());
            result.heading_ = std::move(*val);
        }
        if (fspec_len > 0 && (fspec[0] & (1 << 2))) {
            auto val = altitude_fl::decode(r);
            if (!val) return std::unexpected(val.error());
            result.altitude_rate_ = std::move(*val);
        }
        if (fspec_len > 0 && (fspec[0] & (1 << 1))) {
            auto val = TrackQuality::decode(r);
            if (!val) return std::unexpected(val.error());
            result.quality_ = std::move(*val);
        }
        if (fspec_len > 1 && (fspec[1] & (1 << 7))) {
            auto val = ExtendedStatus::decode(r);
            if (!val) return std::unexpected(val.error());
            result.status_ = std::move(*val);
        }
        if (fspec_len > 1 && (fspec[1] & (1 << 6))) {
            {
                auto val = r.read_string(8);
                if (!val) return std::unexpected(val.error());
                result.label_ = std::move(*val);
                while (!(*result.label_).empty() && ((*result.label_).back() == '\0' || (*result.label_).back() == ' '))
                    (*result.label_).pop_back();
            }
        }
        if (fspec_len > 1 && (fspec[1] & (1 << 5))) {
            auto val = ::stress::temperature::decode(r);
            if (!val) return std::unexpected(val.error());
            result.temperature_ = std::move(*val);
        }
        if (fspec_len > 1 && (fspec[1] & (1 << 4))) {
            auto val = pressure_hpa::decode(r);
            if (!val) return std::unexpected(val.error());
            result.pressure_ = std::move(*val);
        }
        if (fspec_len > 1 && (fspec[1] & (1 << 3))) {
            {
                auto val = decode_sensor_type(r);
                if (!val) return std::unexpected(val.error());
                result.sensor_ = std::move(*val);
            }
        }
        if (fspec_len > 1 && (fspec[1] & (1 << 2))) {
            auto val = ReadingsItem::decode(r);
            if (!val) return std::unexpected(val.error());
            result.readings_ = std::move(*val);
        }
        if (fspec_len > 1 && (fspec[1] & (1 << 1))) {
            {
                auto val = r.read_bytes(16);
                if (!val) return std::unexpected(val.error());
                result.raw_data_.emplace();
                std::copy(val->begin(), val->end(), result.raw_data_->begin());
            }
        }

        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "items{";
        bool first = true;
        if (source_id_.has_value()) {
            if (!first) oss << ", ";
            first = false;
            oss << "source-id=" << static_cast<uint64_t>(*source_id_);
        }
        if (geo_position_.has_value()) {
            if (!first) oss << ", ";
            first = false;
            oss << "geo-position=" << (*geo_position_).to_string();
        }
        if (cartesian_.has_value()) {
            if (!first) oss << ", ";
            first = false;
            oss << "cartesian=" << (*cartesian_).to_string();
        }
        if (velocity_.has_value()) {
            if (!first) oss << ", ";
            first = false;
            oss << "velocity=" << (*velocity_).to_string();
        }
        if (heading_.has_value()) {
            if (!first) oss << ", ";
            first = false;
            oss << "heading=" << (*heading_).value();
        }
        if (altitude_rate_.has_value()) {
            if (!first) oss << ", ";
            first = false;
            oss << "altitude-rate=" << (*altitude_rate_).value();
        }
        if (quality_.has_value()) {
            if (!first) oss << ", ";
            first = false;
            oss << "quality=" << (*quality_).to_string();
        }
        if (status_.has_value()) {
            if (!first) oss << ", ";
            first = false;
            oss << "status=" << (*status_).to_string();
        }
        if (label_.has_value()) {
            if (!first) oss << ", ";
            first = false;
            oss << "label=" << *label_;
        }
        if (temperature_.has_value()) {
            if (!first) oss << ", ";
            first = false;
            oss << "temperature=" << (*temperature_).value();
        }
        if (pressure_.has_value()) {
            if (!first) oss << ", ";
            first = false;
            oss << "pressure=" << (*pressure_).value();
        }
        if (sensor_.has_value()) {
            if (!first) oss << ", ";
            first = false;
            oss << "sensor=" << ::stress::to_string(*sensor_);
        }
        if (readings_.has_value()) {
            if (!first) oss << ", ";
            first = false;
            oss << "readings=" << (*readings_).to_string();
        }
        if (raw_data_.has_value()) {
            if (!first) oss << ", ";
            first = false;
            oss << "raw-data=[bytes]";
        }
        oss << "}";
        return oss.str();
    }

private:
    std::optional<uint16> source_id_;
    std::optional<GeoPosition> geo_position_;
    std::optional<Coordinate> cartesian_;
    std::optional<Velocity> velocity_;
    std::optional<angle_deg> heading_;
    std::optional<altitude_fl> altitude_rate_;
    std::optional<TrackQuality> quality_;
    std::optional<ExtendedStatus> status_;
    std::optional<std::string> label_;
    std::optional<::stress::temperature> temperature_;
    std::optional<pressure_hpa> pressure_;
    std::optional<sensor_type> sensor_;
    std::optional<ReadingsItem> readings_;
    std::optional<std::array<uint8_t, 16>> raw_data_;
};

using payloadVariant = std::variant<PeriodicPayload, EventPayload, OnDemandPayload>;
class SurveillanceRecord {
public:
    static constexpr uint64_t TYPE_ID = 0x1b75cf79c6c4bc45ULL;
    static constexpr std::string_view TYPE_NAME = "SurveillanceRecord";
    static constexpr uint8 ID_VALUE = 3;

    uint8 msg_type() const { return msg_type_; }
    [[deprecated("auto-managed: value is set automatically during frame encoding")]]
    void set_msg_type(uint8 v) { msg_type_ = v; }
    uint16 length() const { return length_; }
    [[deprecated("auto-managed: value is set automatically during frame encoding")]]
    void set_length(uint16 v) { length_ = v; }

    const uint16& track_id() const { return track_id_; }
    uint16& mutable_track_id() { return track_id_; }
    void set_track_id(const uint16& v) { track_id_ = v; }

    const uint32& timestamp() const { return timestamp_; }
    uint32& mutable_timestamp() { return timestamp_; }
    void set_timestamp(const uint32& v) { timestamp_ = v; }

    const ::stress::report_class& report_class() const { return report_class_; }
    ::stress::report_class& mutable_report_class() { return report_class_; }
    void set_report_class(const ::stress::report_class& v) { report_class_ = v; }

    const ::stress::items& items() const { return items_; }
    ::stress::items& mutable_items() { return items_; }
    void set_items(const ::stress::items& v) { items_ = v; }

    const payloadVariant& payload() const { return payload_; }
    payloadVariant& mutable_payload() { return payload_; }
    void set_payload(const payloadVariant& v) { payload_ = v; }

    bool operator==(const SurveillanceRecord&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u16(static_cast<uint16_t>(track_id_), conduit::io::Endian::Big);
        w.write_u32(static_cast<uint32_t>(timestamp_), conduit::io::Endian::Big);
        CONDUIT_TRY(encode_report_class(report_class_, w));
        CONDUIT_TRY(items_.encode(w));
        {
            auto _sw = report_class_;
            auto _idx = (payload_).index();
            if (_idx == 0) {
                if (_sw != static_cast<decltype(_sw)>(RPT_PERIODIC))
                    return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                        "choice 'payload': variant does not match discriminator"));
            } else if (_idx == 1) {
                if (_sw != static_cast<decltype(_sw)>(RPT_EVENT))
                    return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                        "choice 'payload': variant does not match discriminator"));
            } else if (_idx == 2) {
                if (_sw != static_cast<decltype(_sw)>(RPT_ON_DEMAND))
                    return std::unexpected(conduit::Error(conduit::ErrorCode::EncodeConstraintViolation,
                        "choice 'payload': variant does not match discriminator"));
            }
        }
        {
            auto _enc_r = std::visit([&w](const auto& v) -> conduit::VoidResult { return v.encode(w); }, payload_);
            if (!_enc_r) return std::unexpected(_enc_r.error());
        }
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<SurveillanceRecord> decode(conduit::io::BitReader& r) {
        SurveillanceRecord result;
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'track-id'"));
            result.track_id_ = static_cast<uint16>(*val);
        }
        {
            auto val = r.read_u32(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'timestamp'"));
            result.timestamp_ = static_cast<uint32>(*val);
        }
        {
            auto val = decode_report_class(r);
            if (!val) return std::unexpected(val.error().with_context("field 'report-class'"));
            result.report_class_ = std::move(*val);
        }
        {
            auto val = items::decode(r);
            if (!val) return std::unexpected(val.error());
            result.items_ = std::move(*val);
        }
        {
            auto switch_val = result.report_class_;
            if (switch_val == static_cast<decltype(switch_val)>(RPT_PERIODIC)) {
                auto val = PeriodicPayload::decode(r);
                if (!val) return std::unexpected(val.error().with_context("choice 'payload'"));
                result.payload_ = std::move(*val);
            } else if (switch_val == static_cast<decltype(switch_val)>(RPT_EVENT)) {
                auto val = EventPayload::decode(r);
                if (!val) return std::unexpected(val.error().with_context("choice 'payload'"));
                result.payload_ = std::move(*val);
            } else if (switch_val == static_cast<decltype(switch_val)>(RPT_ON_DEMAND)) {
                auto val = OnDemandPayload::decode(r);
                if (!val) return std::unexpected(val.error().with_context("choice 'payload'"));
                result.payload_ = std::move(*val);
            } else {
                return std::unexpected(conduit::Error(conduit::ErrorCode::UnknownDiscriminator,
                    "choice 'payload': no case matched switch value"));
            }
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "SurveillanceRecord{"
            << "msg-type=" << +(msg_type_)
            << ", length=" << +(length_)
            << ", track-id=" << +(track_id_)
            << ", timestamp=" << +(timestamp_)
            << ", report-class=" << ::stress::to_string(report_class_)
            << ", items=" << items_.to_string()
            << ", payload=" << std::visit([](const auto& v) { return v.to_string(); }, payload_)
            << "}";
        return oss.str();
    }

private:
    friend class StressFrame;

    uint8 msg_type_{};
    uint16 length_{};
    uint16 track_id_{};
    uint32 timestamp_{};
    ::stress::report_class report_class_{report_class::periodic};
    ::stress::items items_{};
    payloadVariant payload_{};
public:
    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<SurveillanceRecord> decode_bytes(std::span<const uint8_t> data, size_t max_bytes = 0) {
        if (max_bytes > 0 && data.size() > max_bytes) {
            return std::unexpected(conduit::Error(conduit::ErrorCode::MaxLengthExceeded,
                "message size " + std::to_string(data.size()) + " exceeds limit " + std::to_string(max_bytes)));
        }
        conduit::io::BitReader r(data);
        return decode(r);
    }
};

class StressFrame {
public:
    using PayloadVariant = std::variant<Ping, TelemetryReport, SurveillanceRecord>;

    uint8 msg_type() const { return msg_type_; }
    void set_msg_type(uint8 v) { msg_type_ = v; }
    uint16 length() const { return length_; }
    void set_length(uint16 v) { length_ = v; }

    const PayloadVariant& payload() const { return payload_; }
    PayloadVariant& payload() { return payload_; }

    static StressFrame wrap(const Ping& msg) {
        StressFrame frame;
        frame.msg_type_ = Ping::ID_VALUE;
        frame.payload_ = msg;
        return frame;
    }

    static StressFrame wrap(const TelemetryReport& msg) {
        StressFrame frame;
        frame.msg_type_ = TelemetryReport::ID_VALUE;
        frame.payload_ = msg;
        return frame;
    }

    static StressFrame wrap(const SurveillanceRecord& msg) {
        StressFrame frame;
        frame.msg_type_ = SurveillanceRecord::ID_VALUE;
        frame.payload_ = msg;
        return frame;
    }

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        auto frame_start_pos_ = w.size_bytes();
        w.write_u8(static_cast<uint8_t>(msg_type_));
        auto length_byte_pos_ = w.size_bytes();
        w.write_u16(static_cast<uint16_t>(0), conduit::io::Endian::Big);
        CONDUIT_TRY(std::visit([&w](const auto& m) -> conduit::VoidResult {
            return m.encode(w);
        }, payload_));
        if (!w.patch_u16(length_byte_pos_, static_cast<uint16_t>(w.size_bytes() - frame_start_pos_), conduit::io::Endian::Big))
            return std::unexpected(conduit::Error(conduit::ErrorCode::InvalidArgument, "failed to patch frame length"));
        return {};
    }

    static conduit::Result<StressFrame> decode(conduit::io::BitReader& r) {
        StressFrame result;
        auto msg_type_val_ = r.read_u8();
        if (!msg_type_val_) return std::unexpected(msg_type_val_.error());
        result.msg_type_ = *msg_type_val_;
        auto length_val_ = r.read_u16(conduit::io::Endian::Big);
        if (!length_val_) return std::unexpected(length_val_.error());
        result.length_ = *length_val_;
        switch (static_cast<uint8>(result.msg_type_)) {
            case 1: {
                auto payload_val_ = Ping::decode(r);
                if (!payload_val_) return std::unexpected(payload_val_.error());
                payload_val_->msg_type_ = result.msg_type_;
                payload_val_->length_ = result.length_;
                result.payload_ = std::move(*payload_val_);
                break;
            }
            case 2: {
                auto payload_val_ = TelemetryReport::decode(r);
                if (!payload_val_) return std::unexpected(payload_val_.error());
                payload_val_->msg_type_ = result.msg_type_;
                payload_val_->length_ = result.length_;
                result.payload_ = std::move(*payload_val_);
                break;
            }
            case 3: {
                auto payload_val_ = SurveillanceRecord::decode(r);
                if (!payload_val_) return std::unexpected(payload_val_.error());
                payload_val_->msg_type_ = result.msg_type_;
                payload_val_->length_ = result.length_;
                result.payload_ = std::move(*payload_val_);
                break;
            }
            default:
                return std::unexpected(conduit::Error(conduit::ErrorCode::UnknownDiscriminator,
                    "unknown frame message id: " + std::to_string(static_cast<int>(result.msg_type_))));
        }
        return result;
    }

    conduit::Result<std::vector<uint8_t>> encode_bytes() const {
        conduit::io::BitWriter w;
        CONDUIT_TRY(encode(w));
        return w.finish();
    }

    static conduit::Result<StressFrame> decode_bytes(std::span<const uint8_t> data) {
        conduit::io::BitReader r(data);
        return decode(r);
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "StressFrame{"
            << "msg-type=" << +(msg_type_)
            << ", length=" << +(length_)
            << ", payload=" << std::visit([](const auto& m) { return m.to_string(); }, payload_)
            << "}";
        return oss.str();
    }

private:
    uint8 msg_type_{};
    uint16 length_{};
    PayloadVariant payload_;
};

} // namespace stress

