// Generated by bgen - DO NOT EDIT
#pragma once

#include "types.hpp"
#include "constants.hpp"
#include <conduit/core/error.hpp>
#include <conduit/io/bit_reader.hpp>
#include <conduit/io/bit_writer.hpp>
#include <algorithm>
#include <array>
#include <cstdint>
#include <optional>
#include <span>
#include <sstream>
#include <string>
#include <variant>
#include <vector>

namespace stress {

class Coordinate;
class Velocity;
class GeoPosition;
class TrackQuality;
class ExtendedStatus;
class PeriodicPayload;
class EventPayload;
class OnDemandPayload;
class ReadingsItem;

class Coordinate {
public:
    const int32& x() const { return x_; }
    int32& mutable_x() { return x_; }
    void set_x(const int32& v) { x_ = v; }

    const int32& y() const { return y_; }
    int32& mutable_y() { return y_; }
    void set_y(const int32& v) { y_ = v; }

    const int16& z() const { return z_; }
    int16& mutable_z() { return z_; }
    void set_z(const int16& v) { z_ = v; }

    static constexpr size_t WIRE_SIZE = 10;

    bool operator==(const Coordinate&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_signed_bits(x_, 32);
        w.write_signed_bits(y_, 32);
        w.write_signed_bits(z_, 16);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<Coordinate> decode(conduit::io::BitReader& r) {
        Coordinate result;
        {
            auto val = ([&]() -> conduit::Result<int32_t> { auto v = r.read_u32(conduit::io::Endian::Big); if (!v) return std::unexpected(v.error()); return static_cast<int32_t>(*v); })();
            if (!val) return std::unexpected(val.error().with_context("field 'x'"));
            result.x_ = static_cast<int32>(*val);
        }
        {
            auto val = ([&]() -> conduit::Result<int32_t> { auto v = r.read_u32(conduit::io::Endian::Big); if (!v) return std::unexpected(v.error()); return static_cast<int32_t>(*v); })();
            if (!val) return std::unexpected(val.error().with_context("field 'y'"));
            result.y_ = static_cast<int32>(*val);
        }
        {
            auto val = ([&]() -> conduit::Result<int16_t> { auto v = r.read_u16(conduit::io::Endian::Big); if (!v) return std::unexpected(v.error()); return static_cast<int16_t>(*v); })();
            if (!val) return std::unexpected(val.error().with_context("field 'z'"));
            result.z_ = static_cast<int16>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "Coordinate{"
            << "x=" << +(x_)
            << ", y=" << +(y_)
            << ", z=" << +(z_)
            << "}";
        return oss.str();
    }

private:
    int32 x_{};
    int32 y_{};
    int16 z_{};
};

class Velocity {
public:
    const int16& vx() const { return vx_; }
    int16& mutable_vx() { return vx_; }
    void set_vx(const int16& v) { vx_ = v; }

    const int16& vy() const { return vy_; }
    int16& mutable_vy() { return vy_; }
    void set_vy(const int16& v) { vy_ = v; }

    const int16& vz() const { return vz_; }
    int16& mutable_vz() { return vz_; }
    void set_vz(const int16& v) { vz_ = v; }

    static constexpr size_t WIRE_SIZE = 6;

    bool operator==(const Velocity&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_signed_bits(vx_, 16);
        w.write_signed_bits(vy_, 16);
        w.write_signed_bits(vz_, 16);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<Velocity> decode(conduit::io::BitReader& r) {
        Velocity result;
        {
            auto val = ([&]() -> conduit::Result<int16_t> { auto v = r.read_u16(conduit::io::Endian::Big); if (!v) return std::unexpected(v.error()); return static_cast<int16_t>(*v); })();
            if (!val) return std::unexpected(val.error().with_context("field 'vx'"));
            result.vx_ = static_cast<int16>(*val);
        }
        {
            auto val = ([&]() -> conduit::Result<int16_t> { auto v = r.read_u16(conduit::io::Endian::Big); if (!v) return std::unexpected(v.error()); return static_cast<int16_t>(*v); })();
            if (!val) return std::unexpected(val.error().with_context("field 'vy'"));
            result.vy_ = static_cast<int16>(*val);
        }
        {
            auto val = ([&]() -> conduit::Result<int16_t> { auto v = r.read_u16(conduit::io::Endian::Big); if (!v) return std::unexpected(v.error()); return static_cast<int16_t>(*v); })();
            if (!val) return std::unexpected(val.error().with_context("field 'vz'"));
            result.vz_ = static_cast<int16>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "Velocity{"
            << "vx=" << +(vx_)
            << ", vy=" << +(vy_)
            << ", vz=" << +(vz_)
            << "}";
        return oss.str();
    }

private:
    int16 vx_{};
    int16 vy_{};
    int16 vz_{};
};

class GeoPosition {
public:
    const wgs84& latitude() const { return latitude_; }
    wgs84& mutable_latitude() { return latitude_; }
    void set_latitude(const wgs84& v) { latitude_ = v; }

    const wgs84& longitude() const { return longitude_; }
    wgs84& mutable_longitude() { return longitude_; }
    void set_longitude(const wgs84& v) { longitude_ = v; }

    const altitude_fl& altitude() const { return altitude_; }
    altitude_fl& mutable_altitude() { return altitude_; }
    void set_altitude(const altitude_fl& v) { altitude_ = v; }

    static constexpr size_t WIRE_SIZE = 10;

    bool operator==(const GeoPosition&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        CONDUIT_TRY(latitude_.encode(w));
        CONDUIT_TRY(longitude_.encode(w));
        CONDUIT_TRY(altitude_.encode(w));
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<GeoPosition> decode(conduit::io::BitReader& r) {
        GeoPosition result;
        {
            auto val = wgs84::decode(r);
            if (!val) return std::unexpected(val.error().with_context("field 'latitude'"));
            result.latitude_ = std::move(*val);
        }
        {
            auto val = wgs84::decode(r);
            if (!val) return std::unexpected(val.error().with_context("field 'longitude'"));
            result.longitude_ = std::move(*val);
        }
        {
            auto val = altitude_fl::decode(r);
            if (!val) return std::unexpected(val.error().with_context("field 'altitude'"));
            result.altitude_ = std::move(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "GeoPosition{"
            << "latitude=" << latitude_.value()
            << ", longitude=" << longitude_.value()
            << ", altitude=" << altitude_.value()
            << "}";
        return oss.str();
    }

private:
    wgs84 latitude_{};
    wgs84 longitude_{};
    altitude_fl altitude_{};
};

class TrackQuality {
public:
    const uint8_t& confidence() const { return confidence_; }
    uint8_t& mutable_confidence() { return confidence_; }
    void set_confidence(const uint8_t& v) { confidence_ = v; }

    const uint8_t& freshness() const { return freshness_; }
    uint8_t& mutable_freshness() { return freshness_; }
    void set_freshness(const uint8_t& v) { freshness_ = v; }

    const uint8_t& source() const { return source_; }
    uint8_t& mutable_source() { return source_; }
    void set_source(const uint8_t& v) { source_ = v; }

    static constexpr size_t WIRE_SIZE = 2;

    bool operator==(const TrackQuality&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_bits(confidence_, 4);
        w.write_bits(freshness_, 4);
        w.write_bits(source_, 3);
        w.write_bits(0, 5);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<TrackQuality> decode(conduit::io::BitReader& r) {
        TrackQuality result;
        {
            auto val = r.read_bits(4);
            if (!val) return std::unexpected(val.error().with_context("field 'confidence'"));
            result.confidence_ = static_cast<uint8_t>(*val);
        }
        {
            auto val = r.read_bits(4);
            if (!val) return std::unexpected(val.error().with_context("field 'freshness'"));
            result.freshness_ = static_cast<uint8_t>(*val);
        }
        {
            auto val = r.read_bits(3);
            if (!val) return std::unexpected(val.error().with_context("field 'source'"));
            result.source_ = static_cast<uint8_t>(*val);
        }
        CONDUIT_TRY(r.skip_bits(5));
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "TrackQuality{"
            << "confidence=" << +(confidence_)
            << ", freshness=" << +(freshness_)
            << ", source=" << +(source_)
            << "}";
        return oss.str();
    }

private:
    uint8_t confidence_{};
    uint8_t freshness_{};
    uint8_t source_{};
};

class ExtendedStatus {
public:
    const uint8_t& mode() const { return mode_; }
    uint8_t& mutable_mode() { return mode_; }
    void set_mode(const uint8_t& v) { mode_ = v; }

    const uint8_t& reliability() const { return reliability_; }
    uint8_t& mutable_reliability() { return reliability_; }
    void set_reliability(const uint8_t& v) { reliability_ = v; }

    bool has_secondary_mode() const { return secondary_mode_.has_value(); }
    const uint8_t& secondary_mode() const { return secondary_mode_.value(); }
    uint8_t& mutable_secondary_mode() { if (!secondary_mode_) secondary_mode_.emplace(); return *secondary_mode_; }
    void set_secondary_mode(const uint8_t& v) { secondary_mode_ = v; }
    void clear_secondary_mode() { secondary_mode_.reset(); }

    bool has_aux_flags() const { return aux_flags_.has_value(); }
    const uint8_t& aux_flags() const { return aux_flags_.value(); }
    uint8_t& mutable_aux_flags() { if (!aux_flags_) aux_flags_.emplace(); return *aux_flags_; }
    void set_aux_flags(const uint8_t& v) { aux_flags_ = v; }
    void clear_aux_flags() { aux_flags_.reset(); }

    bool operator==(const ExtendedStatus&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_bits(mode_, 3);
        w.write_bits(reliability_, 4);
        {
            bool fx_continue = false;
            if (secondary_mode_.has_value()) fx_continue = true;
            if (aux_flags_.has_value()) fx_continue = true;
            w.write_bits(fx_continue ? 1 : 0, 1);
            if (fx_continue) {
                w.write_bits(secondary_mode_.value_or(0), 3);
                w.write_bits(aux_flags_.value_or(0), 4);
                w.write_bits(0, 1); // Terminal FX=0
            }
        }
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<ExtendedStatus> decode(conduit::io::BitReader& r) {
        ExtendedStatus result;
        {
            auto val = r.read_bits(3);
            if (!val) return std::unexpected(val.error().with_context("field 'mode'"));
            result.mode_ = static_cast<uint8_t>(*val);
        }
        {
            auto val = r.read_bits(4);
            if (!val) return std::unexpected(val.error().with_context("field 'reliability'"));
            result.reliability_ = static_cast<uint8_t>(*val);
        }
        // FX extension
        {
            auto fx_bit = r.read_bits(1);
            if (!fx_bit) return std::unexpected(fx_bit.error());
            if (*fx_bit) {
                {
                    auto val = r.read_bits(3);
                    if (!val) return std::unexpected(val.error());
                    result.secondary_mode_ = static_cast<uint8_t>(*val);
                }
                {
                    auto val = r.read_bits(4);
                    if (!val) return std::unexpected(val.error());
                    result.aux_flags_ = static_cast<uint8_t>(*val);
                }
                CONDUIT_TRY(r.skip_bits(1)); // Terminal FX=0
            }
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "ExtendedStatus{"
            << "mode=" << +(mode_)
            << ", reliability=" << +(reliability_)
            << ", secondary-mode=" << (secondary_mode_.has_value() ? std::to_string(*secondary_mode_) : std::string("<none>"))
            << ", aux-flags=" << (aux_flags_.has_value() ? std::to_string(*aux_flags_) : std::string("<none>"))
            << "}";
        return oss.str();
    }

private:
    uint8_t mode_{};
    uint8_t reliability_{};
    std::optional<uint8_t> secondary_mode_;
    std::optional<uint8_t> aux_flags_;
};

class PeriodicPayload {
public:
    const uint16& interval_ms() const { return interval_ms_; }
    uint16& mutable_interval_ms() { return interval_ms_; }
    void set_interval_ms(const uint16& v) { interval_ms_ = v; }

    const uint32& counter() const { return counter_; }
    uint32& mutable_counter() { return counter_; }
    void set_counter(const uint32& v) { counter_ = v; }

    static constexpr size_t WIRE_SIZE = 6;

    bool operator==(const PeriodicPayload&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u16(static_cast<uint16_t>(interval_ms_), conduit::io::Endian::Big);
        w.write_u32(static_cast<uint32_t>(counter_), conduit::io::Endian::Big);
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<PeriodicPayload> decode(conduit::io::BitReader& r) {
        PeriodicPayload result;
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'interval-ms'"));
            result.interval_ms_ = static_cast<uint16>(*val);
        }
        {
            auto val = r.read_u32(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'counter'"));
            result.counter_ = static_cast<uint32>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "PeriodicPayload{"
            << "interval-ms=" << +(interval_ms_)
            << ", counter=" << +(counter_)
            << "}";
        return oss.str();
    }

private:
    uint16 interval_ms_{};
    uint32 counter_{};
};

class EventPayload {
public:
    const uint16& event_code() const { return event_code_; }
    uint16& mutable_event_code() { return event_code_; }
    void set_event_code(const uint16& v) { event_code_ = v; }

    const priority_level& severity() const { return severity_; }
    priority_level& mutable_severity() { return severity_; }
    void set_severity(const priority_level& v) { severity_ = v; }

    const std::string& description() const { return description_; }
    std::string& mutable_description() { return description_; }
    void set_description(const std::string& v) { description_ = v; }

    static constexpr size_t WIRE_SIZE = 35;

    bool operator==(const EventPayload&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u16(static_cast<uint16_t>(event_code_), conduit::io::Endian::Big);
        CONDUIT_TRY(encode_priority_level(severity_, w));
        w.write_string(description_, 32, '\0');
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<EventPayload> decode(conduit::io::BitReader& r) {
        EventPayload result;
        {
            auto val = r.read_u16(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'event-code'"));
            result.event_code_ = static_cast<uint16>(*val);
        }
        {
            auto val = decode_priority_level(r);
            if (!val) return std::unexpected(val.error().with_context("field 'severity'"));
            result.severity_ = std::move(*val);
        }
        {
            auto val = r.read_string(32);
            if (!val) return std::unexpected(val.error().with_context("field 'description'"));
            result.description_ = std::move(*val);
            while (!result.description_.empty() && (result.description_.back() == '\0' || result.description_.back() == ' '))
                result.description_.pop_back();
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "EventPayload{"
            << "event-code=" << +(event_code_)
            << ", severity=" << ::stress::to_string(severity_)
            << ", description=" << description_
            << "}";
        return oss.str();
    }

private:
    uint16 event_code_{};
    priority_level severity_{priority_level::low};
    std::string description_{};
};

class OnDemandPayload {
public:
    const uint32& request_id() const { return request_id_; }
    uint32& mutable_request_id() { return request_id_; }
    void set_request_id(const uint32& v) { request_id_ = v; }

    const std::array<uint8_t, 8>& params() const { return params_; }
    std::array<uint8_t, 8>& mutable_params() { return params_; }
    void set_params(const std::array<uint8_t, 8>& v) { params_ = v; }

    static constexpr size_t WIRE_SIZE = 12;

    bool operator==(const OnDemandPayload&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u32(static_cast<uint32_t>(request_id_), conduit::io::Endian::Big);
        w.write_bytes(std::span<const uint8_t>(params_.data(), params_.size()));
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<OnDemandPayload> decode(conduit::io::BitReader& r) {
        OnDemandPayload result;
        {
            auto val = r.read_u32(conduit::io::Endian::Big);
            if (!val) return std::unexpected(val.error().with_context("field 'request-id'"));
            result.request_id_ = static_cast<uint32>(*val);
        }
        {
            auto span = r.read_bytes(8);
            if (!span) return std::unexpected(span.error().with_context("field 'params'"));
            std::copy(span->begin(), span->end(), result.params_.begin());
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "OnDemandPayload{"
            << "request-id=" << +(request_id_)
            << ", params=[bytes]"
            << "}";
        return oss.str();
    }

private:
    uint32 request_id_{};
    std::array<uint8_t, 8> params_{};
};

class entriesElement {
public:
    const uint8& channel() const { return channel_; }
    uint8& mutable_channel() { return channel_; }
    void set_channel(const uint8& v) { channel_ = v; }

    const int16& value() const { return value_; }
    int16& mutable_value() { return value_; }
    void set_value(const int16& v) { value_ = v; }

    const uint8& quality() const { return quality_; }
    uint8& mutable_quality() { return quality_; }
    void set_quality(const uint8& v) { quality_ = v; }

    bool operator==(const entriesElement&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(channel_));
        w.write_signed_bits(value_, 16);
        w.write_u8(static_cast<uint8_t>(quality_));
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<entriesElement> decode(conduit::io::BitReader& r) {
        entriesElement result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'channel'"));
            result.channel_ = static_cast<uint8>(*val);
        }
        {
            auto val = ([&]() -> conduit::Result<int16_t> { auto v = r.read_u16(conduit::io::Endian::Big); if (!v) return std::unexpected(v.error()); return static_cast<int16_t>(*v); })();
            if (!val) return std::unexpected(val.error().with_context("field 'value'"));
            result.value_ = static_cast<int16>(*val);
        }
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'quality'"));
            result.quality_ = static_cast<uint8>(*val);
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "entriesElement{"
            << "channel=" << +(channel_)
            << ", value=" << +(value_)
            << ", quality=" << +(quality_)
            << "}";
        return oss.str();
    }

private:
    uint8 channel_{};
    int16 value_{};
    uint8 quality_{};
};

class ReadingsItem {
public:
    const uint8& rep() const { return rep_; }
    uint8& mutable_rep() { return rep_; }
    void set_rep(const uint8& v) { rep_ = v; }

    const std::vector<entriesElement>& entries() const { return entries_; }
    std::vector<entriesElement>& mutable_entries() { return entries_; }
    void set_entries(const std::vector<entriesElement>& v) { entries_ = v; }

    bool operator==(const ReadingsItem&) const = default;

    conduit::VoidResult encode(conduit::io::BitWriter& w) const {
        w.write_u8(static_cast<uint8_t>(rep_));
        for (const auto& elem : entries_) {
            CONDUIT_TRY(elem.encode(w));
        }
        if (w.has_error()) return std::unexpected(w.error());
        return {};
    }

    static conduit::Result<ReadingsItem> decode(conduit::io::BitReader& r) {
        ReadingsItem result;
        {
            auto val = r.read_u8();
            if (!val) return std::unexpected(val.error().with_context("field 'rep'"));
            result.rep_ = static_cast<uint8>(*val);
        }
        {
            auto count = static_cast<size_t>(result.rep_);
            if (count > static_cast<size_t>(INT32_MAX)) return std::unexpected(conduit::Error(conduit::ErrorCode::InvalidArgument, "invalid array count").with_context("array 'entries'"));
            result.entries_.reserve(count);
            for (size_t i = 0; i < count; i++) {
                auto elem = entriesElement::decode(r);
                if (!elem) return std::unexpected(elem.error().with_context("array 'entries'"));
                result.entries_.push_back(std::move(*elem));
            }
        }
        return result;
    }

    std::string to_string() const {
        std::ostringstream oss;
        oss << "ReadingsItem{"
            << "rep=" << +(rep_)
            << ", entries=["
            ;
            for (size_t i = 0; i < entries_.size(); ++i) {
                if (i > 0) oss << ", ";
                oss << entries_[i].to_string();
            }
            oss << "]"
            << "}";
        return oss.str();
    }

private:
    uint8 rep_{};
    std::vector<entriesElement> entries_{};
};

} // namespace stress

