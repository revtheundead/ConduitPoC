[project]
name = "conduit-example-xcvr-python"
version = "0.1.0"
description = "ASTERIX Transceiver Example — Python"
requires-python = ">=3.10"

dependencies = [
    "conduit>=0.1.0",
]

[project.scripts]
poc-app = "src.poc_app:main"
dummy-peer = "src.dummy_peer:main"
