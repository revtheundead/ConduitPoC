// Generated by bgen - DO NOT EDIT
#pragma once

#include "messages.hpp"
#include <conduit/traits/session_traits.hpp>
#include <conduit/logging/logger.hpp>
#include <any>
#include <memory>
#include <span>
#include <string_view>
#include <vector>

namespace asterix {

// Session for frame: AsterixDataBlock
class AsterixDataBlockSession : public conduit::traits::ISession {
public:
    [[nodiscard]] conduit::Result<std::vector<conduit::traits::DecodedMessage>>
    decode_frame(std::span<const uint8_t> data) override {
        auto frame = AsterixDataBlock::decode_bytes(data);
        if (!frame) return std::unexpected(frame.error());
        std::vector<conduit::traits::DecodedMessage> messages;
        for (const auto& item : frame->payload()) {
            std::visit([&messages](const auto& msg) {
                conduit::traits::DecodedMessage dm;
                dm.type_id = std::decay_t<decltype(msg)>::TYPE_ID;
                dm.type_name = std::decay_t<decltype(msg)>::TYPE_NAME;
                dm.payload = msg;
                messages.push_back(std::move(dm));
            }, item);
        }
        for (const auto& dm : messages) {
            if (dm.type_id == 0x8d0fb175aeb7de98ULL) {
                LOG_WARNF("Received send-only message type '{}' (type_id=0x{:016x})", "Cat007UplinkRecord", 0x8d0fb175aeb7de98ULL);
            }
        }
        return messages;
    }

    [[nodiscard]] conduit::Result<std::vector<uint8_t>>
    encode_wrap(uint64_t type_id, const std::any& payload) override {
        if (type_id == 0x526296abfa532d01ULL) {
            auto* msg = std::any_cast<Cat007DownlinkRecord>(&payload);
            if (!msg) return std::unexpected(conduit::Error(conduit::ErrorCode::InvalidArgument,
                "payload type mismatch for Cat007DownlinkRecord"));
            auto frame = AsterixDataBlock::wrap(*msg);
            return frame.encode_bytes();
        } else if (type_id == 0x8d0fb175aeb7de98ULL) {
            auto* msg = std::any_cast<Cat007UplinkRecord>(&payload);
            if (!msg) return std::unexpected(conduit::Error(conduit::ErrorCode::InvalidArgument,
                "payload type mismatch for Cat007UplinkRecord"));
            auto frame = AsterixDataBlock::wrap(*msg);
            return frame.encode_bytes();
        } else if (type_id == 0xd597a1ab87dbb779ULL) {
            auto* msg = std::any_cast<Cat021Record>(&payload);
            if (!msg) return std::unexpected(conduit::Error(conduit::ErrorCode::InvalidArgument,
                "payload type mismatch for Cat021Record"));
            auto frame = AsterixDataBlock::wrap(*msg);
            return frame.encode_bytes();
        } else if (type_id == 0x85be2a9ab4cf6c50ULL) {
            auto* msg = std::any_cast<Cat048Record>(&payload);
            if (!msg) return std::unexpected(conduit::Error(conduit::ErrorCode::InvalidArgument,
                "payload type mismatch for Cat048Record"));
            auto frame = AsterixDataBlock::wrap(*msg);
            return frame.encode_bytes();
        } else if (type_id == 0x7f7b50444b289e0eULL) {
            auto* msg = std::any_cast<Cat253Record>(&payload);
            if (!msg) return std::unexpected(conduit::Error(conduit::ErrorCode::InvalidArgument,
                "payload type mismatch for Cat253Record"));
            auto frame = AsterixDataBlock::wrap(*msg);
            return frame.encode_bytes();
        } else {
            return std::unexpected(conduit::Error(conduit::ErrorCode::UnknownTypeId,
                "unknown type_id: " + std::to_string(type_id)));
        }
    }

    [[nodiscard]] std::span<const uint8_t> sync_pattern() const override {
        return {};
    }

    [[nodiscard]] size_t min_frame_header_size() const override {
        return 3;
    }

    [[nodiscard]] size_t extract_frame_length(std::span<const uint8_t> header) const override {
        if (header.size() < 3) return 0;
        conduit::io::BitReader r(header);
        if (!r.skip_bits(8)) return 0;
        auto val = r.read_u16(conduit::io::Endian::Big);
        if (!val) return 0;
        return static_cast<size_t>(*val);
    }

    [[nodiscard]] std::span<const uint64_t> leaf_type_ids() const override {
        static constexpr uint64_t ids[] = {
            0x526296abfa532d01ULL, // Cat007DownlinkRecord
            0x8d0fb175aeb7de98ULL, // Cat007UplinkRecord
            0xd597a1ab87dbb779ULL, // Cat021Record
            0x85be2a9ab4cf6c50ULL, // Cat048Record
            0x7f7b50444b289e0eULL // Cat253Record
        };
        return ids;
    }

    [[nodiscard]] std::string_view type_name(uint64_t type_id) const override {
        switch (type_id) {
            case 0x526296abfa532d01ULL: return "Cat007DownlinkRecord";
            case 0x8d0fb175aeb7de98ULL: return "Cat007UplinkRecord";
            case 0xd597a1ab87dbb779ULL: return "Cat021Record";
            case 0x85be2a9ab4cf6c50ULL: return "Cat048Record";
            case 0x7f7b50444b289e0eULL: return "Cat253Record";
        }
        return "unknown";
    }

    [[nodiscard]] bool is_receive_only(uint64_t type_id) const override {
        switch (type_id) {
            case 0x526296abfa532d01ULL: return true; // Cat007DownlinkRecord
        }
        return false;
    }

    void reset() override {
    }
private:
};

inline std::unique_ptr<conduit::traits::ISession> create_asterix_data_block_session() {
    return std::make_unique<AsterixDataBlockSession>();
}

} // namespace asterix

